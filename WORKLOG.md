# Worklog (Phased Reimplementation)

This worklog tracks Therion Studio reimplementation progress against `QtReimplementationSpecification.md` using implementation phases.

Detailed chronological history has been preserved in `WORKLOG_ARCHIVE_2026-05-13.md`.

## Current Focus

### In progress

- Phase 6: encoding detection/conversion and encoding-preserving save semantics.
- Phase 10: interactive map drawing/insertion workflow implementation (first slice active).
- Phase 11: text-editor structured block-canvas authoring.

### Next up

- Start Phase 9 map-editor parity polish and interaction refinement.
- Continue Phase 10 interactive map drawing/insertion workflow (smart trace and mode-aware undo polish).
- Continue Phase 11 structured block-canvas authoring (richer block configuration, safer insertion guards, and broader directive coverage).
- ~~Map editor selection behavior polish: in `Select` mode, clicking an area border should select the referenced `line`; clicking inside area fill should select the `area` object.~~
- Continue drag-and-drop reordering/moving in TH2 Visual `Inspector -> Objects`: movable rows expose grip handles, hover/drag cursors, row-to-row and scrap-row drops have slim drop-target feedback, and edge auto-scroll is implemented; remaining polish is manual cross-platform verification.
- Later refactor phase: unify undo/redo into one document-level command stack per editor session, covering raw text edits, visual map mutations, inspector setting changes, object/background edits, and structured blocks changes instead of arbitrating between `QTextDocument` undo and the map `QUndoStack`.
- ~~Files sidebar refresh fix: after delete (file/folder via context menu), refresh the tree/model view immediately so removed items disappear without manual re-open/refresh.~~
- Nice-to-have: TH2 Visual grid fallback for documents without parseable geometry or valid source bounds, e.g. user-defined grid origin/extent so the grid can be shown before map objects/backgrounds exist.
- Continue implementation work; defer Phase 6 manual verification pass.

### Risks / blockers

- Parser/serializer round-trip coverage is still incomplete for full Therion corpus-level confidence.
- Non-UTF decoding is now implemented with Qt-supported codecs, but broader legacy-encoding coverage still needs corpus validation.
- Most map behaviors are covered by unit/smoke checks; broader cross-platform GUI automation is still limited beyond focused smoke coverage.

### Text/Map Refactor Roadmap

- Keep `StructureConstraintsTest` as mandatory guardrail on every refactor slice; ratchet limits downward immediately after verified line-count reductions.
- Track A (Text editor): keep `TextEditorTab` as a thin orchestration shell, continue narrowing raw completion/catalog services, and reduce remaining friend/controller boundaries without merging responsibilities back into the tab.
- Track B (Map editor): extract `MapEditorTab` responsibilities into focused controllers in this order: interactive drawing, inspector objects, inspector backgrounds, selection details, scene lifecycle, undo/snapshot orchestration.
- Track C (Shared core): consolidate shared source-edit/rewrite primitives used by Raw/Block/Map modes into focused non-UI services to reduce duplicate mutation logic and improve round-trip testability.
- Slice policy: each slice shall own one responsibility, keep behavior stable, pass build + full tests + `StructureConstraintsTest`, and be logged in `Completed` with verification commands.
- Milestones: reduce `TextEditorTab.cpp` and `MapEditorTab.cpp` below current ratchets first, then continue toward sub-5k-line translation units without introducing catch-all classes.

### Remaining BlockEditor Extraction Plan

- Canvas rebuild/rendering: extract `rebuildBlocksCanvasFromText(...)` first into a focused `BlockEditorCanvasRebuildController`; preserve encoding-root normalization, card creation, parent/child layout, boundary guide generation, and post-rebuild selection/details refresh behavior.
- Toolbox and scope logic: extract `populateBlockToolbox(...)`, `populateBlockToolboxScopeCombo(...)`, `selectedBlockInsertionContextToken(...)`, and `updateBlockToolboxScopeLabel(...)` after canvas rebuild, because scope auto-detection depends on selected canvas context and source outline rules.
- Selection/details glue: keep the existing details controllers, but later consolidate the remaining thin delegates and canvas item helpers (`selectBlockInCanvasAndDetails`, `refreshBlockDetailsSelectionFromScene`, `resolveBlockCanvasItem`, `selectBlockCanvasItem`, etc.) into a selection coordinator if the API boundary remains stable.
- Low-level wrappers: keep tiny `TextEditorTab` delegate methods for app-facing APIs (`configureCommandAtLine`, `deleteCommandAtLine`, raw/map source rewrite calls) until MapEditor ownership is reviewed, so BlockEditor extraction does not destabilize map/raw workflows.
- Next implementation slice: selection/details glue consolidation, with no behavior changes and a structure ratchet only after build + full CTest + `StructureConstraintsTest` pass.

### Completed

- ~~Hardened TH2 Visual `Inspector -> Objects` row drag/drop feedback and undo routing: the drop target is now a slim highlighted line aligned to the target row edge, active row dragging uses a closed-hand cursor, object moves run under the map-command apply guard so they do not leave duplicate raw-text undo entries, and `MapEditorDragUndoRedoSmokeTest` now asserts indicator geometry, drag cursor, and clean undo/redo stack state. Updated the user manual. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|MapEditorObjectMovePlannerTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|MapEditorObjectMovePlannerTest|StructureConstraintsTest"`, and `python3 scripts/check_structure_constraints.py`.~~
- ~~Polished TH2 Visual `Inspector -> Objects` drag affordances: movable row labels/grips now use an open-hand hover cursor, active dragging keeps the closed-hand cursor even over non-target gaps, and dragging near the top/bottom of the object tree auto-scrolls the viewport. Expanded `MapEditorDragUndoRedoSmokeTest` to assert mouse tracking and hover cursor behavior. Updated the user manual. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Completed explicit scrap-row drop targets for TH2 Visual `Inspector -> Objects`: dropping a movable `point`, `line`, or `area` row onto a scrap parent row now moves the full source span into that scrap before `endscrap`, with undo/redo and selection restoration through the existing object-move command path. Added pure `MapEditorObjectMovePlanner` coverage for `IntoTargetScrap`, added GUI smoke coverage for dragging a point into another scrap, fixed object-tree category detection to use the existing plural `ProjectStructureEntry` categories, and updated the user manual. Verified with `cmake --build build --target MapEditorObjectMovePlannerTest MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorObjectMovePlannerTest MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure`, `ctest --test-dir build-strict --output-on-failure`, and `git diff --check`.~~
- ~~Added visible drag affordances to TH2 Visual `Inspector -> Objects`: source-linked `point`, `line`, and `area` rows now show a narrow grip-handle column between the object label and visibility/delete actions, and row dragging starts from either the handle or the label. Added the Lucide `grip-vertical` resource, updated the manual wording, and expanded `MapEditorDragUndoRedoSmokeTest` so move scenarios assert the handle icon/tooltip exists and drag from the handle cell. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, and `ctest --test-dir build-strict --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`.~~
- ~~Completed the second Phase 9 `Inspector -> Objects` drag/drop implementation slice: added a visible horizontal drop indicator and drag cursor/status feedback while dragging source-linked object rows, broadened the GUI smoke coverage from one same-scrap after-target move to before-target and between-scrap move scenarios, and documented the row-drag workflow in the user manual. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|MapEditorObjectMovePlannerTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|MapEditorObjectMovePlannerTest|StructureConstraintsTest"`, `ctest --test-dir build --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure`, `ctest --test-dir build-strict --output-on-failure`, and `git diff --check`.~~
- ~~Completed the first Phase 9 `Inspector -> Objects` drag/drop implementation slice: wired object-row press/release handling through `MapEditorInspectorObjectController::moveInspectorObject`, added undoable source snapshots for object moves, refreshed map/inspector selection after rewrites, and extracted object-tree viewport event handling into `MapEditorTabInspectorObjectEvents.cpp` so `MapEditorTab.cpp` stays below the structure ratchet. Expanded `MapEditorDragUndoRedoSmokeTest` with a GUI row-drag smoke that reveals the Objects tab, expands nested rows, drags a point after a line block, verifies the rewritten TH2 source order and cursor line, and checks undo/redo restoration. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|MapEditorObjectMovePlannerTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|MapEditorObjectMovePlannerTest|StructureConstraintsTest"`, `ctest --test-dir build --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure`, `ctest --test-dir build-strict --output-on-failure`, and `git diff --check`.~~
- ~~Completed the Phase 9 object-reordering foundation slice: added pure `MapEditorObjectMovePlanner` logic for future `Inspector -> Objects` drag/drop source rewrites. The planner resolves movable `point`, `line`, and `area` source spans, supports before/after target insertion, preserves line endings and full block contents, reports no-op moves, and rejects non-object source/target lines with actionable errors. Added `MapEditorObjectMovePlannerTest` coverage for point-after-line moves, full line-block moves with comments, area moves between scraps, CRLF preservation, no-op moves, and invalid-source rejection. Wired the planner into `TherionStudio`, map-editor UI test sources, and the unit test list. Verified with `cmake -S . -B build`, `cmake -S . -B build-strict -DTHERION_ENABLE_STRICT_WARNINGS=ON`, `cmake --build build --target MapEditorObjectMovePlannerTest`, `cmake --build build-strict --target MapEditorObjectMovePlannerTest`, `ctest --test-dir build --output-on-failure -R MapEditorObjectMovePlannerTest`, `ctest --test-dir build-strict --output-on-failure -R MapEditorObjectMovePlannerTest`, `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorObjectMovePlannerTest|MapEditor|MapGeometryFeatureParsingTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Completed the second Phase 9 map-editor selection parity slice: clearing the graphical map selection now also clears the `Inspector -> Objects` tree selection, preventing stale object-row selection after an empty-canvas click. Added a stable `mapObjectsTree` object name for targeted UI tests and extended `MapEditorDragUndoRedoSmokeTest` to verify border/fill object selection selects the corresponding inspector row and that clicking empty canvas space clears both graphical and inspector selections. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, and `ctest --test-dir build-strict --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`.~~
- ~~Completed the first Phase 9 map-editor selection parity slice: referenced area fills now carry an explicit area-fill selection subtype, generic closed line paths are treated as stroked-border hits instead of filled polygon hits, and Select-mode path clicks are resolved directly through `MapEditorViewportInputController` so area-border clicks select the referenced `line` while interior area-fill clicks select the `area`. Added `MapEditorDragUndoRedoSmokeTest` coverage with a referenced-border area document that asserts exact selected source-line sets and text-cursor routing for border-vs-fill clicks. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `cmake --build build-strict --target MapEditorDragUndoRedoSmokeTest TherionStudio`, `ctest --test-dir build --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build --output-on-failure -R "MapEditor|MapGeometryFeatureParsingTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditor|MapGeometryFeatureParsingTest"`, and `git diff --check`.~~
- ~~Stabilized the completed editor friend-web cleanup milestone: full normal and strict builds now pass, full normal and strict CTest suites pass 21/21, and `REFACTOR_CLAUDE.md` has been updated from its original review snapshot to record the current checkpoint: `therion_core` / `therion_platform` / `therion_app` libraries are present, C++20 and strict Qt ASCII conversion macros are enabled, `TextEditorTab.h` and `MapEditorTab.h` have no `friend class` declarations, `MapEditorTab.cpp` is ratcheted to 687 lines, and the next priorities are CI, Phase 9 map-editor parity polish, and later `MainWindow` decomposition. Verified with `cmake --build build`, `cmake --build build-strict`, `ctest --test-dir build --output-on-failure`, and `ctest --test-dir build-strict --output-on-failure`.~~
- ~~Completed the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced the final `MapEditorCanvasEditController` broad `MapEditorTab *` owner/friend dependency with an explicit `MapEditorCanvasEditContext`. Added focused `MapEditorTabCanvasEditContext.cpp` wiring for text editor access, scene/undo/draft state, map item lookup, selection restoration state, pending click state, source-coordinate/bounds callbacks, status refresh callbacks, delayed selection restoration callbacks, and draft item move/visibility callbacks while preserving card move/visibility undo commands, point/line/area geometry rewrite commands, source text snapshots, line vertex insert/delete/smooth actions, draft geometry add/remove/move/visibility handling, draft source-vertex conversion, and map selection restoration after edits. Removed the last `MapEditorTab` friend declaration, moved canvas-edit delegates out of `MapEditorTab.cpp`, ratcheted `MapEditorTab.cpp` to 687 lines, and extended `StructureConstraintsTest` so the converted canvas-edit controller cannot regain `MapEditorTab` owner pointers or direct `MapEditorTab.h` includes. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Continued the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced `MapEditorViewportInputController`'s broad `MapEditorTab *` owner/friend dependency with an explicit `MapEditorViewportInputContext`. Added focused `MapEditorTabViewportInputContext.cpp` wiring for viewport/scene pointers, pointer/touch/native-gesture state, pan/auto-fit state, pending click selection state, interactive drawing state, draw-mode access, source-coordinate mapping, draw-session callbacks, zoom/fit callbacks, and selected-vertex edit shortcuts while preserving mouse/tablet/touch/wheel/native-gesture handling, freehand stroke capture, line/area bezier control dragging, pending selection hit disambiguation, pan/zoom behavior, resize auto-fit behavior, and map keyboard editing shortcuts. Removed `friend class MapEditorViewportInputController` and extended `StructureConstraintsTest` so the converted viewport-input controller cannot regain `MapEditorTab` owner pointers or direct `MapEditorTab.h` includes. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Continued the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced `MapEditorSelectionController`'s broad `MapEditorTab *` owner/friend dependency with an explicit `MapEditorSelectionContext`. Added focused `MapEditorTabSelectionContext.cpp` wiring for scene/view selection state, map item lookup, hidden/suppressed object-line state, pending click disambiguation, selected-object details state, text cursor sync state, source-coordinate mapping, and UI refresh callbacks while preserving scene-selection handling, text-cursor-to-map synchronization, geometry selection presentation, multi-line selection, pending click priority resolution, and map centering behavior. Removed `friend class MapEditorSelectionController`, moved its thin `MapEditorTab` delegate methods out of `MapEditorTab.cpp`, ratcheted `MapEditorTab.cpp` to 798 lines, and extended `StructureConstraintsTest` so the converted selection controller cannot regain `MapEditorTab` owner pointers or direct `MapEditorTab.h` includes. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Continued the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced `MapEditorInteractiveDrawController`'s broad `MapEditorTab *` owner/friend dependency with an explicit `MapEditorInteractiveDrawContext`. Added focused `MapEditorTabInteractiveDrawContext.cpp` wiring for draw-mode state, preview scene items, interactive line/area draft state, text insertion, source snapshots, status/help updates, and controller delegate methods while preserving point insertion, line/area completion, cancel-to-select behavior, preview marker rendering, and draft session cleanup. Removed `friend class MapEditorInteractiveDrawController`, moved its thin `MapEditorTab` delegate methods out of `MapEditorTab.cpp`, and extended `StructureConstraintsTest` so the converted interactive-draw controller cannot regain `MapEditorTab` owner pointers or direct `MapEditorTab.h` includes. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Continued the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced `MapEditorInspectorObjectController`'s broad `MapEditorTab *` owner/friend dependency with an explicit `MapEditorInspectorObjectContext`. Added focused `MapEditorTabInspectorObjectContext.cpp` wiring for inspector tree/model state, scene visibility state, selection suppression state, selected-object reset state, text navigation, map-selection sync, object visibility updates, object-tree rebuild placeholders, and subtree hide/delete actions while preserving inspector object rebuild, tree column setup, source-line selection sync, repeated-click deselection suppression, object visibility toggles, source deletion, and map visibility refresh behavior. Removed `friend class MapEditorInspectorObjectController` and extended `StructureConstraintsTest` so the converted inspector-object controller cannot regain `MapEditorTab` owner pointers or direct `MapEditorTab.h` includes. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Continued the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced `MapEditorObjectDetailsPanelController` and `MapEditorObjectDetailsEditController` broad `MapEditorTab *` owner/friend dependencies with a shared explicit `MapEditorObjectDetailsContext`. Added focused `MapEditorTabObjectDetailsContext.cpp` wiring for object-details widgets, selected-object state, toolbar/status state, source-edit callbacks, line-option rewrite callbacks, vertex-edit callbacks, translation, and source-bound access while preserving object details panel refresh, quick-field editing, scrap projection/scale editing, point/line orientation editing, line closed/reversed toggles, object deletion, and selected-vertex actions. Removed both object-details friend declarations from `MapEditorTab` and extended `StructureConstraintsTest` so the converted object-details controllers cannot regain `MapEditorTab` owner pointers or direct `MapEditorTab.h` includes. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Continued the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced `MapEditorSceneLifecycleController` and `MapEditorSceneRefreshController` broad `MapEditorTab *` owner/friend dependencies with explicit `MapEditorSceneLifecycleContext` and `MapEditorSceneRefreshContext` objects. Added focused `MapEditorTabSceneContexts.cpp` wiring for scene/draft/background state, zoom/status callbacks, selection-change routing, document text/current-line access, map render command-recording callbacks, scene rebuild hooks, and refresh follow-up actions while preserving scene rebuild, preserved background/draft item restoration, map fitting/zoom updates, pending refresh flushing, and render command callback behavior. Removed the two corresponding `MapEditorTab` friend declarations and extended `StructureConstraintsTest` so the converted scene controllers cannot regain `MapEditorTab` owner pointers or direct `MapEditorTab.h` includes. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Started the MapEditor friend-web reduction from `REFACTOR_CLAUDE.md`: replaced `MapEditorInspectorBackgroundController`'s broad `MapEditorTab *` owner/friend dependency with an explicit `MapEditorInspectorBackgroundContext` carrying only background-layer tree/model widgets, control widgets, update guard state, translation, and background-layer operation callbacks. Added focused `MapEditorTabInspectorBackgroundContext.cpp` wiring, removed `friend class MapEditorInspectorBackgroundController`, lowered the `MapEditorTab.cpp` structure ratchet to 855 lines, and extended `StructureConstraintsTest` so this controller cannot regain `MapEditorTab` friendship or owner pointers. Verified with `cmake --build build --target TherionStudio`, `cmake --build build-strict --target TherionStudio`, `cmake --build build --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `cmake --build build-strict --target MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest`, `ctest --test-dir build --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `ctest --test-dir build-strict --output-on-failure -R "MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest|StructureConstraintsTest"`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Completed the BlockEditor shell-boundary cleanup after friend-web removal: moved the tab-owned Blocks panel construction method from `src/app/text_editor/block_editor/BlockEditorPanelBuilder.cpp` to `src/app/text_editor/TextEditorTabBlockEditorPanel.cpp`, updated app/test CMake source lists, and made the dependency guardrail strict so BlockEditor implementation files may no longer include `../TextEditorTab.h` directly. This keeps reusable BlockEditor components independent from the text-editor shell while preserving the existing Blocks toolbox/canvas/details widget construction and signal wiring. Verified with `cmake --build build`, `cmake --build build-strict`, `ctest --test-dir build --output-on-failure`, `ctest --test-dir build-strict --output-on-failure`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Guardrailed the completed BlockEditor friend-web milestone: extended `scripts/check_structure_constraints.py` so `StructureConstraintsTest` now fails if `TextEditorTab.h` regains any `friend class BlockEditor...` declaration, if BlockEditor controller files regain broad `TextEditorTab *owner` fields/constructors, or if BlockEditor implementation files directly include `../TextEditorTab.h`. Verified with `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure -R StructureConstraintsTest`, `ctest --test-dir build-strict --output-on-failure -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction batch from `REFACTOR_CLAUDE.md`: replaced the final broad BlockEditor `TextEditorTab *` owner dependencies in `BlockEditorDeleteExecutor`, `BlockEditorConfigureController`, `BlockEditorCommandOptionsDialog`, `BlockEditorDataBlockDialog`, `BlockEditorSingleValueCommandDialog`, `BlockEditorInsertionController`, `BlockEditorMoveController`, and `BlockEditorCanvasRebuildController` with explicit contexts carrying only dialog parent widgets, canvas/source pointers, command metadata, source-controller access, scope/directive predicates, ID-field predicates, planner/resolver contexts, canvas callback hooks, and translation callbacks. Removed all remaining BlockEditor friend declarations from `TextEditorTab`, wired the new contexts through focused tab context factories, and preserved block delete confirmation/range resolution, configure dialogs, canvas rebuild/rendering, drag/drop insertion, move preview cleanup, and block move source rewrite flows. Verified with `cmake --build build`, `cmake --build build-strict`, `ctest --test-dir build --output-on-failure`, `ctest --test-dir build-strict --output-on-failure`, `python3 scripts/check_structure_constraints.py`, `git diff --check`, and stale owner/friend scans.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorToolboxController`'s broad `TextEditorTab *` owner dependency with an explicit `BlockEditorToolboxContext` carrying toolbox widget pointers, block canvas/editor pointers, command metadata, completion-context normalization, child-kind compatibility, and translation callbacks. Removed `friend class BlockEditorToolboxController`, added focused `TextEditorTabBlockEditorToolboxController.cpp` context wiring that preserves pre-panel construction order, and kept toolbox population, scope-combo population, selected-block auto-scope resolution, context filtering, command compatibility filtering, and scope tooltip behavior stable. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorSelectionDetailsController`'s broad `TextEditorTab *` owner dependency with an explicit `BlockEditorSelectionDetailsContext` carrying only details-pane state pointers, widget pointers, command metadata, normalized source-line loading, directive normalization, details-mode mapping, command metadata predicates, readings-tag/editor completer callbacks, and apply/help refresh callbacks. Removed `friend class BlockEditorSelectionDetailsController`, added focused context wiring to `TextEditorTabBlockEditorSelectionContexts.cpp`, and preserved selection-driven details population for unsupported commands, structured option commands, simple value commands, and `data` headers, including inline-comment capture, option-table population, readings-order editor setup, contextual help refresh, and apply-state refresh behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction batch from `REFACTOR_CLAUDE.md`: replaced `BlockEditorInsertionPlanner` and `BlockEditorMovePlanner` broad `const TextEditorTab *` owner dependencies with explicit planner contexts carrying only block-canvas item line lookup, child-kind compatibility checks, and the existing drop-target resolver context for move planning. Removed the two corresponding `friend class` declarations and kept planner context wiring in `TextEditorTabBlockEditorResolverContexts.cpp` while preserving block insertion placement, parent compatibility fallback, move target resolution, subtree exclusion, and destination-parent planning behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction batch from `REFACTOR_CLAUDE.md`: replaced `BlockEditorDetailsPaneController`, `BlockEditorToolboxDetailsController`, and `BlockEditorCanvasSelectionController` broad `TextEditorTab *` owner dependencies with explicit context objects for details-pane reset widgets/state callbacks, toolbox command-summary presentation, block canvas scene/view selection, details-selection loading, and fallback selection state updates. Removed the three corresponding `friend class` declarations, added focused `TextEditorTabBlockEditorSelectionContexts.cpp` context wiring, and preserved details-pane clearing, toolbox command help preview, canvas selection/focus resolution, selected-block centering, and selection-driven details refresh behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction batch from `REFACTOR_CLAUDE.md`: replaced `BlockEditorMovePreviewController`, `BlockEditorDropTargetResolver`, and `BlockEditorDocumentOutlineBuilder` broad `TextEditorTab *` / `const TextEditorTab *` owner dependencies with explicit context objects for block canvas scene access, block item resolution, block line lookup, container-boundary move-preview state, document outline scope predicates, and insertion/move drop-target resolution. Removed the three corresponding `friend class` declarations, added focused `TextEditorTabBlockEditorResolverContexts.cpp` context wiring, and routed insertion/move callers through narrow context factories while preserving move-preview guide rendering, scene-item collection, vertical drop placement, target-item resolution, and document outline construction behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorLineBuildService`'s broad `const TextEditorTab *` owner dependency with an explicit `BlockEditorLineBuildContext` carrying only selected-line/kind state, details edit widgets, option table, comment marker, command metadata, source-context callback, readings-order callback, directive normalization, required-ID predicate, mode predicates, and translation. Removed `friend class BlockEditorLineBuildService`, added focused `TextEditorTabBlockEditorLineBuildService.cpp` context wiring, and preserved details-pane source-line rebuilding for simple values, data headers, structured options, option validation/failing-row selection, additional positional tokens, and inline comments. Also corrected block-controller construction order so widget-caching context controllers are built after `buildBlockEditorPanel()` creates their widgets while `BlockEditorToolboxController` remains available during toolbox population. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorDetailsHelpController`'s broad `TextEditorTab *` owner dependency with an explicit `BlockEditorDetailsHelpContext` carrying only teardown state, help/detail widgets, comment marker, command metadata, selected-kind/normalization callbacks, required-argument/readings-focus predicates, mode predicates, and translation. Removed `friend class BlockEditorDetailsHelpController`, added focused `TextEditorTabBlockEditorDetailsHelpController.cpp` context wiring, and preserved block details contextual help rendering for command help, ID fields, data headers, extra arguments, readings tags, and inline comments. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorOptionArgsController`'s broad `TextEditorTab *` owner dependency with an explicit `BlockEditorOptionArgsContext` carrying only option-argument widgets/layout, option table, sync flag, argument editor list, command metadata, structured-mode/selected-kind accessors, directive normalization, apply/help refresh callbacks, and translation. Removed `friend class BlockEditorOptionArgsController`, added focused `TextEditorTabBlockEditorOptionArgsController.cpp` context wiring, and preserved selected-option multi-value editor creation, table editability updates, serialized option-value writes, apply-state refresh, and contextual-help refresh behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorApplyExecutor`'s broad `TextEditorTab *` owner dependency with an explicit `BlockEditorApplyExecutorContext` carrying only dialog parent, teardown state, selected-line state, source-context callback, updated-line build callback, canvas/details selection callback, apply-state refresh callback, and translation callback. Removed `friend class BlockEditorApplyExecutor`, added focused `TextEditorTabBlockEditorApplyExecutor.cpp` context wiring, and preserved block details apply validation, warning, line replacement, selection refresh, and apply-state refresh behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorApplyStateController`'s broad `TextEditorTab *` owner dependency with an explicit `BlockEditorApplyStateContext` carrying only teardown/populating flags, apply/status widgets, selected line/base-status state, source-context callback, and updated-line build callback. Removed `friend class BlockEditorApplyStateController`, added focused `TextEditorTabBlockEditorApplyStateController.cpp` context wiring, and preserved Apply-button enablement plus validation status text behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorCanvasBoundaryController`'s broad `TextEditorTab *` owner dependency with an explicit `BlockEditorCanvasBoundaryContext` carrying only the block canvas scene and container-boundary guide items needed for end-hint hit testing. Removed `friend class BlockEditorCanvasBoundaryController`, added focused `TextEditorTabBlockEditorCanvasBoundaryController.cpp` context wiring, and preserved direct item hit testing plus guide-line proximity fallback behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest|MapEditorDetachedPaneTest|MapEditorDragUndoRedoSmokeTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `BlockEditorSourceController`'s `TextEditorTab *` / `const TextEditorTab *` owner constructors with an explicit `BlockEditorSourceContext` carrying only the raw editor pointer, editability flag, source replacement callback, and translation callback. Removed `friend class BlockEditorSourceController`, added focused `TextEditorTabBlockEditorSourceController.cpp` context wiring, and routed existing BlockEditor source readers/writers through the narrow context while preserving source replacement, direct line edit, insertion, deletion, and normalized-line behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorContextHelpController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorContextHelpContext` carrying only the root widget, editor/help-panel widget pointers, command metadata, help-collapse state, validation tooltip key, translation, directive/completion lookup callbacks, completion metadata callbacks, and toolbox-scope refresh callback. Removed `friend class TextEditorContextHelpController`, added focused `TextEditorTabContextHelpController.cpp` wiring, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorDocumentController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorDocumentContext` carrying only editor/document state, encoding snapshots, project path state, block-selection reset state, translation, and narrow lifecycle callbacks for mode availability, block canvas/details refresh, title/highlight refresh, dirty-state notification, context-help refresh, and status refresh. Removed `friend class TextEditorDocumentController`, added focused `TextEditorTabDocumentController.cpp` wiring after raw-editor construction, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorSourceRewriteController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorSourceRewriteContext` carrying only the raw editor, loading/current-line state, and narrow callbacks for dirty-state recomputation, optional block-canvas rebuild, document-text-change notification, and context-help refresh. Removed `friend class TextEditorSourceRewriteController`, added focused `TextEditorTabSourceRewriteController.cpp` wiring, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorModeController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorModeContext` carrying only file/encoding state pointers, mode widgets, editor focus surface, and narrow callbacks for find-bar hiding, source replacement, block-canvas rebuild, toolbox refresh, and mode-change notification. Removed `friend class TextEditorModeController`, added focused `TextEditorTabModeController.cpp` wiring, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorAppearanceController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorAppearanceContext` carrying only source-surface widgets, block mode state, and narrow callbacks for context-help refresh, block-details help refresh, and block canvas rebuild. Removed `friend class TextEditorAppearanceController`, added focused `TextEditorTabAppearanceController.cpp` wiring, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorEncodingController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorEncodingContext` carrying only the dialog parent, encoding state pointers, and narrow callbacks for block-mode support, encoding-root enforcement, dirty-state refresh, and status refresh. Removed `friend class TextEditorEncodingController`, added focused `TextEditorTabEncodingController.cpp` wiring, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorStatusController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorStatusContext` carrying only status widgets, file/encoding/dirty state pointers, and narrow title/dirty signal callbacks. Removed `friend class TextEditorStatusController`, added focused `TextEditorTabStatusController.cpp` wiring, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C `TextEditorTab` friend-web reduction slice from `REFACTOR_CLAUDE.md`: replaced `TextEditorCursorController`'s broad `TextEditorTab *` owner dependency with an explicit `TextEditorCursorContext` carrying only the raw editor, search-bar widgets, cursor/highlight state pointers, and narrow callbacks for mode switching, context help, validation tooltip refresh, and cursor signals. Removed `friend class TextEditorCursorController` from `TextEditorTab`, added focused `TextEditorTabCursorController.cpp` wiring, and kept `TextEditorTab.cpp` at the existing 1087-line structure ratchet. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `cmake --build build-strict --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R "TextEditorCaretInteractionTest|TextEditorCompletionHighlightTest"`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C testability slice from `REFACTOR_CLAUDE.md`: removed the remaining app-layer direct static `CommandCatalogService::...` usages by routing map-editor derived catalog caches through default `CommandCatalogStore` instances. This leaves `CommandCatalogService` as a compatibility facade in `src/core` only while preserving existing resource-backed map inspector/type/projection behavior. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, `rg -n "CommandCatalogService::" src tests`, and `git diff --check`.~~
- ~~Phase C testability slice from `REFACTOR_CLAUDE.md`: migrated `TextEditorContextHelpController` from direct static `CommandCatalogService::catalogObject()` access to an injected `CommandCatalogStore` value while preserving the default resource-backed constructor path used by `TextEditorTab`. This makes context-help metadata loading testable with explicit catalog objects/JSON in a future focused test without Qt resource setup. Remaining static catalog consumers are isolated to map-editor derived catalog helper caches. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C testability slice from `REFACTOR_CLAUDE.md`: introduced injectable `CommandCatalogStore` alongside the existing static `CommandCatalogService` facade so catalog consumers can keep using the resource-backed production path while tests can construct catalogs from explicit `QJsonObject`, JSON bytes, or temporary files. Added `CommandCatalogServiceTest` as a pure `therion_core` unit test covering injected objects, invalid/non-object JSON rejection, valid file loading, and missing-file behavior without requiring Qt resources. Verified with `cmake --build build --target CommandCatalogServiceTest -j4`, `cmake --build build-strict --target CommandCatalogServiceTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R CommandCatalogServiceTest`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R CommandCatalogServiceTest`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C testability slice from `REFACTOR_CLAUDE.md`: migrated map-editor session persistence from static `SessionStore::...` calls to `SessionSettingsStore` injection. `MapEditorTab` now supports both a default production-owned session store and an injected store, `MainWindow::openMapEditorTab` passes its `sessionStore_` into new map tabs, and touch-friendly controls plus per-document background-layer metadata now persist through the injected store. This removes remaining static session calls under `src/app` while preserving standalone `MapEditorTab` construction for existing UI tests. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C testability slice from `REFACTOR_CLAUDE.md`: migrated `MainWindow` session restore/persist paths from direct static `SessionStore::...` calls to an injected `SessionSettingsStore` member while preserving the existing `MainWindow(QWidget *)` production constructor. Added a move-injection constructor for future GUI/session tests and routed window geometry/state, project path, open/active documents, structure-name overrides, and Therion runner console defaults through `sessionStore_`. Remaining map-editor static session calls are intentionally left for a separate map-context plumbing slice. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C testability slice from `REFACTOR_CLAUDE.md`: introduced instance-backed `SessionSettingsStore` as a narrow `QSettings` wrapper while preserving the existing static `SessionStore` facade for production call sites. Updated `SessionStoreTest` to use a temporary INI file through the injected store instead of mutating global `QSettings` paths/application identity, and expanded coverage across persisted session paths, window state, Therion runner settings, touch controls, background layer metadata, and defaults. Verified with `cmake --build build --target SessionStoreTest -j4`, `cmake --build build-strict --target SessionStoreTest -j4`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C CMake maintainability slice from `REFACTOR_CLAUDE.md`: introduced focused `therion_add_text_editor_ui_test(...)` and `therion_add_map_editor_ui_test(...)` helpers for the four QWidget-based UI tests, preserving each test target name, source list expansion, link libraries, resource target name, and resource file set while removing repeated `qt_add_executable` / `target_link_libraries` / `qt_add_resources` boilerplate. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C CMake maintainability slice from `REFACTOR_CLAUDE.md`: extracted the shared text/block/raw editor production source prefix from the text-editor and map-editor UI test source lists into `THERION_EDITOR_UI_TEST_BASE_SOURCES`. `THERION_TEXT_EDITOR_UI_TEST_SOURCES` now adds only the text-editor shell/highlighter tail, while `THERION_MAP_EDITOR_UI_TEST_SOURCES` adds map-editor sources plus the same tail, preserving expanded source ordering for all four UI test targets. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C CMake maintainability slice from `REFACTOR_CLAUDE.md`: extracted the duplicated map-editor UI test production source list into `THERION_MAP_EDITOR_UI_TEST_SOURCES` and rewired `MapEditorDetachedPaneTest` / `MapEditorDragUndoRedoSmokeTest` to add only their test driver source plus the shared list. Preserved separate resource targets, links, CTest registration, labels, environment, and compile definitions. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C CMake maintainability slice from `REFACTOR_CLAUDE.md`: extracted the duplicated text-editor UI test production source list into `THERION_TEXT_EDITOR_UI_TEST_SOURCES` and rewired `TextEditorCaretInteractionTest` / `TextEditorCompletionHighlightTest` to add only their test driver source plus the shared list. Preserved separate resources, links, CTest registration, labels, environment, and compile definitions. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C CMake maintainability slice from `REFACTOR_CLAUDE.md`: split test metadata into `THERION_UNIT_TEST_TARGETS` and `THERION_UI_TEST_TARGETS`, derived `THERION_TEST_EXECUTABLE_TARGETS` from those lists, loop-generated executable `add_test(...)` registrations, and reused the same lists for environment and label properties. This removes the repeated test-name blocks while preserving the existing executable tests, `StructureConstraintsTest` registration, offscreen UI environment, and unit/UI labels. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C CMake maintainability slice from `REFACTOR_CLAUDE.md`: introduced a focused `therion_add_core_unit_test(...)` helper for pure `therion_core` unit tests and converted the seven one-source core test targets (`TherionDocumentEditorTest`, `TherionProjectStructureIndexTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, `TherionXviParserTest`, `DocumentFileEncodingTest`, and `SessionStoreTest`) to use it. Mixed widget/source-list tests remain explicit. This removes repeated `qt_add_executable`/`target_link_libraries(... therion_core)` boilerplate while preserving existing CTest registration, labels, environment, and compile definitions. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C CMake maintainability slice from `REFACTOR_CLAUDE.md`: introduced a focused `therion_add_app_unit_test(...)` helper for QtCore-only `therion_app` unit tests and converted the six app-service test targets (`ProjectStructureScannerTest`, runner config/display tests, executable selection, presenter, and service tests) to use it. This removes repeated `qt_add_executable`/`target_link_libraries(... therion_app)` boilerplate while preserving existing CTest registration, labels, environment, and compile definitions. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C app-layer testability slice from `REFACTOR_CLAUDE.md`: added `ProjectStructureScannerTest` as a QtCore-only unit test linked against `therion_app`, covering asynchronous scanner signal delivery, generation/project-root propagation, filesystem project scan results, and in-memory source overlay behavior using the normalized file-path key expected by `ProjectStructureIndex`. This gives the remaining app-service target direct headless coverage without involving `MainWindow` or widgets. Verified with `cmake --build build --target ProjectStructureScannerTest -j4`, `cmake --build build-strict --target ProjectStructureScannerTest -j4`, `ctest --test-dir build --output-on-failure --timeout 300 -R ProjectStructureScannerTest`, `ctest --test-dir build-strict --output-on-failure --timeout 300 -R ProjectStructureScannerTest`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C app-layer testability slice from `REFACTOR_CLAUDE.md`: added `TherionRunnerServiceTest` as a QtCore-only unit test linked against `therion_app`, covering start validation for missing working directory, missing working-directory path, missing executable, successful process launch, concurrent-start rejection, stdout/stderr forwarding, normal finish reporting, running-state signals, and final stopped state. The process path uses the test executable itself as a cross-platform child fixture, avoiding shell/tool dependencies while exercising the real `QProcess` service path. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C app-layer testability slice from `REFACTOR_CLAUDE.md`: added `TherionRunnerPresenterTest` as a QtCore-only unit test linked against `therion_app`, covering start-result handling for started/already-running/missing-working-directory/missing-executable outcomes, start-success presentation including platform fallback executable text updates, and lifecycle presentation for stop, normal finish, crash finish, and runner error states. Wired the test into CTest, unit labels, and shared compile definitions so runner presentation policy remains verifiable without widgets. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C app-layer testability slice from `REFACTOR_CLAUDE.md`: added `TherionExecutableSelectionControllerTest` as a QtCore-only unit test linked against `therion_app`, covering initial browse-path resolution for empty/missing/file/directory inputs, empty selection cancellation, non-executable selection warnings, and accepted executable selection using the running test binary as the cross-platform executable fixture. Wired the test into CTest, unit labels, and shared compile definitions so executable-selection policy remains verifiable without widgets. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C app-layer testability slice from `REFACTOR_CLAUDE.md`: added `TherionRunnerConfigDisplayControllerTest` as a QtCore-only unit test linked against `therion_app`, covering empty resolved-config cleanup, explicit config argument behavior, blank/project-root working-directory auto-update, and preservation of manually edited working directories while still refreshing the auto-resolved marker. Wired the test into CTest, unit labels, and shared compile definitions so app-service policy remains testable without launching widgets. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C app-layer CMake extraction slice from `REFACTOR_CLAUDE.md`: introduced a new `therion_app` static library for QtCore/QtConcurrent application services (`ProjectStructureScanner`, Therion runner service, runner config resolver/display policy, executable selection policy, and runner presenters), linked it against `therion_core` plus `therion_platform`, enabled strict warnings for the new non-UI target, and stopped compiling those service sources directly into `TherionStudio`. Added `TherionRunnerConfigResolverTest` as a QtCore-only unit test linked against `therion_app`, covering working-directory fallback, explicit `-c`/`--config` detection, explicit config path resolution, active `.thconfig` preference, and default `thconfig` lookup. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, and `git diff --check`.~~
- ~~Phase C friend-web reduction slice from `REFACTOR_CLAUDE.md`: removed the final raw-editor friend, `RawEditorCompletionController`, from the `TextEditorTab` friend list by introducing `RawEditorCompletionControllerContext` with raw editor widgets, completion metadata, dynamic project/file path callbacks, directive/scope callbacks, and an event receiver. Moved completion-controller construction into `RawEditorPanelBuilder.cpp` after the raw editor widgets/completer are created, removed direct `TextEditorTab` owner access from the controller/context implementation, and lowered `StructureConstraintsTest` limits for `src/app/text_editor/TextEditorTab.cpp` from `1088` to `1087` lines and `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from `314` to `267` lines. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `git diff --check`, and `rg -n "friend class RawEditor|owner_" src/app/text_editor/TextEditorTab.h src/app/text_editor/raw_editor/RawEditorCompletionController.* src/app/text_editor/raw_editor/RawEditorCompletionControllerContext.cpp`.~~
- ~~Phase C friend-web reduction slice from `REFACTOR_CLAUDE.md`: removed `RawEditorCompletionPopupController` from the `TextEditorTab` friend list by introducing `RawEditorCompletionPopupContext` with only editor, completer/model, metadata, project/file paths, and directive/scope callbacks. Rewired popup construction behind `RawEditorCompletionControllerContext.cpp`, replaced owner `tr()` calls with stable `QCoreApplication::translate(...)` context strings, preserved completion-popup filtering, required-argument tooltip, and scope tooltip behavior, and lowered the `StructureConstraintsTest` limit for `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from `320` to `314` lines. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase C friend-web reduction slice from `REFACTOR_CLAUDE.md`: removed `RawEditorCompletionSuggestionBuilder` from the `TextEditorTab` friend list by introducing `RawEditorCompletionSuggestionContext` with only editor, metadata, project/file paths, and directive/scope callbacks. Rewired completion popup and completion-controller suggestion calls to construct the explicit context while preserving command, option, value, and project `input` file suggestions. Lowered the `StructureConstraintsTest` limit for `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from `331` to `320` lines. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase C friend-web reduction slice from `REFACTOR_CLAUDE.md`: removed `RawEditorCompletionInsertionController` from the `TextEditorTab` friend list by introducing `RawEditorCompletionInsertionContext` with only the raw editor pointer and directive-normalization/closing-token callbacks. Moved insertion-controller context construction behind `RawEditorCompletionControllerContext.cpp`, preserved completion-token replacement plus automatic closing-directive insertion behavior, and lowered the `StructureConstraintsTest` limit for `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from `336` to `331` lines. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase C friend-web reduction slice from `REFACTOR_CLAUDE.md`: removed `RawEditorCompletionContextAnalyzer` from the `TextEditorTab` friend list by introducing an explicit `RawEditorCompletionContext` carrying only editor, metadata, and directive/scope callbacks. Moved completion-controller context construction into `RawEditorCompletionControllerContext.cpp`, rewired popup/suggestion call sites to pass analyzer context explicitly, and kept completion scope/command behavior unchanged. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase C friend-web reduction slice from `REFACTOR_CLAUDE.md`: removed `RawEditorCommandMetadataLoader` from the `TextEditorTab` friend list by introducing `RawEditorCommandMetadataContext` with only mutable command metadata, the completion model, and directive-normalization callback access. Rewired `RawEditorCompletionController` to construct the loader from that context while preserving command-catalog metadata loading and completion-model rebuild behavior. Lowered the `StructureConstraintsTest` limit for `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from `338` to `336` lines. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase C friend-web reduction slice from `REFACTOR_CLAUDE.md`: removed `RawEditorFindController` from the `TextEditorTab` friend list by introducing an explicit `RawEditorFindContext` with only the raw editor/search widgets and a mode-switch callback required by find/replace behavior. Moved find-controller construction into the raw-editor panel builder so `TextEditorTab.cpp` stayed within the structure ratchet, removed the now-unused `replaceMode_` tab member, and lowered the `StructureConstraintsTest` limit for `src/app/text_editor/TextEditorTab.cpp` from `1089` to `1088` lines. Verified with `cmake --build build -j4`, `cmake --build build-strict -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase A CI and strict-warning baseline slice from `REFACTOR_CLAUDE.md`: added `.github/workflows/build-and-test.yml` with a Windows/macOS/Linux Debug+Release matrix, Qt 6.7.3 setup, unit-test and UI-smoke-test CTest passes, and opt-in `THERION_ENABLE_STRICT_WARNINGS=ON` configuration. Added the CMake `THERION_ENABLE_STRICT_WARNINGS` option and applied warnings-as-errors only to non-UI library targets (`therion_core`, `therion_platform`) to keep the first CI gate focused. Removed four unused private core helpers exposed by the strict build (`formatVerticesInline`, stale `TherionDocumentEditor` `optionValue`, `sectionEndDirectiveForCategory`, `isSectionCategory`) and ignored generated `build-*/` verification directories. Verified with `cmake -S . -B build`, `cmake -S . -B build-strict -DTHERION_ENABLE_STRICT_WARNINGS=ON`, `cmake --build build -j4`, `cmake --build build-strict -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build-strict --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase B modern C++ baseline slice from `REFACTOR_CLAUDE.md`: raised the project language baseline from C++17 to C++20 and added `[[nodiscard]]` to the public rewrite/edit result APIs in `TherionDocumentEditor` plus public parser result APIs in `TherionDocumentParser`, making ignored parse/edit outcomes visible to callers. Reviewed the narrow `std::span<const T>` recommendation but deferred conversion because the touched core APIs still intentionally exchange Qt string/container types and no high-value pure-transform `QVector<T>` copy was present in this slice. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase B localization scaffold slice from `REFACTOR_CLAUDE.md`: added quiet optional `Qt6LinguistTools` discovery, conditional `qt_add_translations(TherionStudio ... RESOURCE_PREFIX "/i18n")` wiring for `translations/therion_studio_cs.ts`, and a startup `QTranslator` hook that loads `therion_studio_<locale>.qm` from `:/i18n` when compiled translation resources are available. Added the initial Czech TS catalog placeholder. Local Qt does not currently provide `Qt6LinguistTools`/`lupdate`, so `.qm` generation and string extraction are disabled locally until that package is installed; normal app builds continue to work. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase B macOS bundle metadata slice from `REFACTOR_CLAUDE.md`: added checked-in `cmake/macos/Info.plist.in` and wired `TherionStudio` to use it through `MACOSX_BUNDLE_INFO_PLIST`, including explicit `LSMinimumSystemVersion` (`THERION_MACOS_MINIMUM_SYSTEM_VERSION`, default `12.0`), `NSHighResolutionCapable`, `CFBundleShortVersionString`, `CFBundleVersion`, bundle identifier, display name, and standard Cocoa app keys. Verified generated `build/TherionStudio.app/Contents/Info.plist` with `plutil -p`, and verified code with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Phase B platform/service hygiene slice from `REFACTOR_CLAUDE.md`: introduced a new `therion_platform` static library with `src/platform/PlatformPathResolver.h` plus macOS/Windows/Linux implementations, moved macOS Homebrew Therion executable candidates out of `TherionRunnerService`, and rewired the runner service to consume `TherionStudio::Platform::therionExecutableCandidates()` for suggested/default executable fallback resolution. Renamed the runner fallback state from Homebrew-specific to platform-generic wording and removed the macOS-specific executable-path example from the generic missing-executable message. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion console bootstrap orchestration): introduced `src/app/MainWindowTherionConsoleBootstrapper.{h,cpp}` and delegated the remaining `MainWindowTherionRunner::buildConsole` post-build orchestration to it, including console-controller widget binding, sidebar-page widget attach, startup console banner lines, and initial config-display/runner-state refresh hooks. Kept lifecycle/run wiring behavior unchanged while reducing `buildConsole` to composition + helper delegation. Updated app build wiring (`CMakeLists.txt`) to compile/link the new bootstrap helper. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion console signal wiring): introduced `src/app/MainWindowTherionConsoleWiring.{h,cpp}` and delegated `MainWindowTherionRunner::buildConsole` signal/slot orchestration to it (console button actions, config-refresh text-change hooks, and Therion runner-service event wiring) using callback-based `WiringInput` while preserving existing behaviors and event flow. Updated app build wiring (`CMakeLists.txt`) to compile/link the new wiring helper. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion console UI builder): introduced `src/app/MainWindowTherionConsoleBuilder.{h,cpp}` and delegated `MainWindowTherionRunner::buildConsole` widget/layout construction to it (form rows, status label, button row, and console view defaults), while keeping signal wiring, runner-service hookup, and console-controller binding in `MainWindow` to preserve behavior. Updated app build wiring (`CMakeLists.txt`) to compile/link the new builder. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion executable selection flow): introduced `src/app/TherionExecutableSelectionController.{h,cpp}` and delegated `MainWindowTherionRunner::browseTherionExecutable` selection policy to it, covering initial browse-path resolution, selected executable validation, executable-path normalization, and status/warning message payloads while preserving existing dialog-driven behavior. Updated app build wiring (`CMakeLists.txt`) to compile/link the new controller. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion lifecycle status presentation): introduced `src/app/TherionRunnerLifecyclePresenter.{h,cpp}` and delegated `MainWindowTherionRunner` stop/finished/error lifecycle UI policy to it, covering stop-request behavior (`Therion is not running.` vs stop-in-progress messaging), run-finished status text rendering, and runner-error status text rendering while preserving existing console/status-label behavior. Updated app build wiring (`CMakeLists.txt`) to compile/link the new presenter. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion start-success presentation): introduced `src/app/TherionRunnerStartSuccessPresenter.{h,cpp}` and delegated `MainWindowTherionRunner::runTherion` successful-start UI policy to it, covering executable-field fallback update decision, `Running ...` console line rendering, and `Starting Therion...` status-label text while preserving existing run-start behavior. Updated app build wiring (`CMakeLists.txt`) to compile/link the new presenter. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion start-result presentation): introduced `src/app/TherionRunnerStartResultPresenter.{h,cpp}` and delegated `MainWindowTherionRunner::runTherion` start-result UI policy to it, covering `AlreadyRunning`, `MissingWorkingDirectory`, `WorkingDirectoryNotFound`, and `ExecutableNotFound` presentation actions (warning dialog payload, console/status-bar/status-label updates) while preserving behavior for successful starts. Updated app build wiring (`CMakeLists.txt`) to compile/link the new presenter. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion config display policy): introduced `src/app/TherionRunnerConfigDisplayController.{h,cpp}` and delegated `MainWindowTherionRunner::refreshTherionConfigDisplay` policy decisions to it, including resolved-config presentation state, auto-resolved working-directory update rules, and auto-resolved working-directory clear/update decisions while preserving existing UI text/tooltip behavior in `MainWindow`. Completed build wiring by adding `TherionRunnerConfigDisplayController` sources and `MainWindowTherionConsoleController.h` to `CMakeLists.txt`. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion config/working-directory resolution): introduced `src/app/TherionRunnerConfigResolver.{h,cpp}` and delegated `MainWindowTherionRunner` resolution helpers to it, covering working-directory fallback resolution, explicit `-c/--config` argument detection, and resolved config-path selection from arguments/active `.thconfig`/default `thconfig` lookup. Updated app build wiring (`CMakeLists.txt`) and preserved existing UI behavior and status messaging. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase C extraction slice (Therion console pane state/output): introduced `src/app/MainWindowTherionConsoleController.h` and delegated `MainWindow` console-pane responsibilities to it, including console append behavior, process stdout/stderr append formatting, copy-output clipboard flow result handling, and runner-control enabled/disabled state application. Rewired `MainWindowTherionRunner.cpp` to bind the controller to console widgets and use controller delegation from `appendConsoleLine`, `copyTherionConsoleOutput`, `updateTherionRunnerState`, and runner output handlers while preserving existing user-facing status messages and behavior. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase B step 2 (structure-scan extraction slice): moved structure sidebar scan orchestration (debounce timer, async `QtConcurrent` run, queued rescan behavior, and watcher completion routing) out of `MainWindow` into new `src/app/ProjectStructureScanner.{h,cpp}` service. Rewired `MainWindow` to collect in-memory source snapshots, request scans through the service, and apply scan results via signal-driven callback handling while preserving existing structure sidebar behavior and stale-project-root guards. Updated app build wiring (`CMakeLists.txt`) to compile/link the new scanner service. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Phase B step 1 (first extraction slice): moved Therion process orchestration out of `MainWindow` into new `src/app/TherionRunnerService.{h,cpp}` (`QObject` + internal `QProcess`), including executable path resolution/fallback, start/stop lifecycle, stdout/stderr forwarding, finish/error signals, and running-state change notifications. Rewired `MainWindowTherionRunner` UI to consume the service signals and map start-result codes to existing user-facing messages while preserving run-policy behavior and session-driven executable defaults. Updated app build wiring (`CMakeLists.txt`) to compile/link the new service. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Started Phase A roadmap implementation: introduced `therion_core` static library and moved app/core-focused test targets to link it instead of compiling duplicated `src/core` translation units; re-enabled previously disabled UI tests (`TextEditorCaretInteractionTest`, `TextEditorCompletionHighlightTest`), applied `QT_NO_CAST_FROM_ASCII` / `QT_NO_CAST_TO_ASCII` to all test executable targets, added `unit`/`ui` CTest labels, and stabilized offscreen UI test behavior by making block-selection probing in `TextEditorCaretInteractionTest` efficient and by relaxing brittle help-content assertions in `TextEditorCompletionHighlightTest` to verify non-empty/validation-surface behavior. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, and `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Refined `REFACTOR_REVIEW.md` `Recommended Alternative Roadmap` into an execution-ready three-phase plan with scoped work items, explicit exit criteria, CI sequencing, and packaging/signing hardening order.~~
- ~~Delivered architecture review document `REFACTOR.md` covering flaw analysis, layered architecture/abstraction design, cross-platform Qt best practices, modern CMake + dependency-management guidance, testability strategy, and a phased refactoring roadmap for Windows/macOS/Linux portability.~~
- ~~Continued TextEditor structure cleanup (slice 97): extracted document load/save/project-root lifecycle handling from `TextEditorTab.cpp` into `text_editor/TextEditorDocumentController.{h,cpp}`, moving `DocumentFile` I/O and encoding snapshot updates out of the tab shell while preserving public `TextEditorTab` delegates. Ratcheted `src/app/text_editor/TextEditorTab.cpp` from 1149 to 1089 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, `ctest --test-dir build --output-on-failure --timeout 300`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 96): extracted map viewport input-event routing from `MapEditorTab.cpp` into `map_editor/MapEditorViewportInputController.{h,cpp}`, covering mouse, tablet, wheel, native gesture, touch, key, and resize handling while leaving app/inspector event filtering in the tab shell. Removed duplicate viewport hit-testing helpers from `MapEditorTab.cpp` and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 1518 to 860 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, `ctest --test-dir build --output-on-failure --timeout 300`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 95): extracted map scene construction, refresh orchestration, render callback wiring, undo-preserving refresh, and pending refresh flushing from `MapEditorTab.cpp` into `map_editor/MapEditorSceneRefreshController.{h,cpp}`. Kept renderer callbacks bound to the owning `MapEditorTab`, cleaned extracted includes, and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 1633 to 1518 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 94): extracted map scene selection handling, text-cursor-to-map selection synchronization, gated geometry presentation, and line/multi-line selection helpers from `MapEditorTab.cpp` into `map_editor/MapEditorSelectionController.{h,cpp}`. Removed now-dead vertex lookup helpers from the tab and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 2119 to 1633 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 93): extracted canvas-originated edit recording, source mutation undo commands, line-vertex insert/delete/smooth actions, selection restore, and draft geometry item lifecycle from `MapEditorTab.cpp` into `map_editor/MapEditorCanvasEditController.{h,cpp}` plus focused `map_editor/MapEditorCanvasEditCommandFactory.{h,cpp}`. Kept stored callbacks capturing `MapEditorTab` rather than temporary controller instances, and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 2992 to 2119 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 92): extracted scene lifecycle and view fitting/zoom orchestration from `MapEditorTab.cpp` into new `map_editor/MapEditorSceneLifecycleController.{h,cpp}`, covering scene clear/restore preservation of draft/background items, geometry preview bounds, fit-to-view, zoom synchronization, point-centered zoom, and zoom adjustment delegates. Ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 3160 to 2992 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 91): completed the object-details edit extraction by moving scrap-scale populate/apply, object settings routing, and line close/reverse toggles from `MapEditorTab.cpp` into `map_editor/MapEditorObjectDetailsEditController.{h,cpp}`. Ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 3330 to 3160 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 90): extracted TH2 Visual object-details edit actions from `MapEditorTab.cpp` into new `map_editor/MapEditorObjectDetailsEditController.{h,cpp}`, covering orientation/line-point option application, details toggle state, selected-object deletion, quick field rewrites, scrap projection rewrites, subtype choice refresh, and line-vertex action delegates. Ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 3553 to 3330 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 89): extracted TH2 Visual object selection/details panel refresh from `MapEditorTab.cpp` into new `map_editor/MapEditorObjectDetailsPanelController.{h,cpp}`, covering selected-object fallback resolution, quick fields, line vertex actions, line options, scrap scale fields, and point/line orientation editor visibility/state. Kept source rewrite/edit actions in `MapEditorTab` for this slice and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 3865 to 3553 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 88): extracted TH2 Visual inspector background-layer panel orchestration from `MapEditorTab.cpp` into new `map_editor/MapEditorInspectorBackgroundController.{h,cpp}`, covering background-layer tree column setup, layer selection/click routing, visibility/delete actions, and background-layer controls refresh. Kept existing `MapEditorTab` private methods as stable delegates and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 3979 to 3865 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 87): extracted TH2 Visual inspector object tree orchestration from `MapEditorTab.cpp` into new `map_editor/MapEditorInspectorObjectController.{h,cpp}`, covering object-tree rebuild, object column configuration, line-to-tree selection sync, object-row selection/click behavior, object visibility toggles, and scene visibility application. Kept existing `MapEditorTab` private methods as stable delegates for event handlers and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 4295 to 3979 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 86): extracted stateful interactive drawing mode/session/preview/cancel workflow from `MapEditorTab.cpp` into new `map_editor/MapEditorInteractiveDrawController.{h,cpp}` while keeping existing `MapEditorTab` private delegate methods stable for event handlers. Added the controller to app and map-editor test targets and ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 4718 to 4295 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 85): extracted pure interactive drawing draft helpers from `MapEditorTab.cpp` into new `map_editor/MapEditorInteractiveDrawLogic.{h,cpp}`, covering interactive line draft vertex storage, line/area coordinate row serialization, anchor capture with Bezier drag controls, control-handle hit testing, and control-handle updates with mirrored smooth controls. Ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 4853 to 4718 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Renamed recently extracted MapEditor modules to responsibility-specific names: `MapEditorInspectorSupport` -> `MapEditorInspectorData`, `MapEditorObjectDetailsSupport` -> `MapEditorObjectDetailsLogic`, and `MapEditorSourceReferenceSupport` -> `MapEditorSourceReferenceResolver`. Updated includes, CMake wiring, worklog references, and `AGENTS.md` naming guidance to avoid generic `Support` naming. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 84): extracted map source-reference and coordinate serialization helpers from `MapEditorTab.cpp` into new `map_editor/MapEditorSourceReferenceResolver.{h,cpp}`, covering source line feature lookup, source coordinate formatting, line coordinate row serialization, cursor-to-source-vertex resolution, selection-to-source-vertex lookup, scrap object line collection, and selected point source-line matching. Ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 5416 to 4853 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued MapEditor structure cleanup (slice 83): extracted map object details support from `MapEditorTab.cpp` into new `map_editor/MapEditorObjectDetailsLogic.{h,cpp}`, covering configurable object-kind checks, orientation support metadata, point orientation parsing, line-point orientation/left-size lookup, orientation normalization, and source-point movement comparison. Ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 5884 to 5416 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Started MapEditor structure cleanup (slice 82): extracted inspector object labels, quick-field parsing/catalog choices, action icons, scrap-scale parsing/defaults, and map source-units helpers from `MapEditorTab.cpp` into new `map_editor/MapEditorInspectorData.{h,cpp}`. `MapEditorTab.cpp` now keeps inspector orchestration while the data module owns reusable inspector parsing/formatting/catalog logic. Ratcheted `src/app/text_editor/map_editor/MapEditorTab.cpp` from 6468 to 5884 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 81): extracted raw completion token insertion and automatic closing-directive pairing from `RawEditorCompletionController` into new `raw_editor/RawEditorCompletionInsertionController.{h,cpp}`. `RawEditorCompletionController` now keeps event routing plus focused metadata/context/suggestion/popup/insertion delegates. Ratcheted `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from 400 to 338 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 80): removed redundant raw-completion-only bridge wrappers from `TextEditorTab` after the raw completion extraction, keeping only the `TextEditorTab` methods still consumed by BlockEditor, context help, and raw panel signal wiring. Ratcheted `src/app/text_editor/TextEditorTab.cpp` from 1205 to 1149 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 79): extracted raw completion popup presentation into new `raw_editor/RawEditorCompletionPopupController.{h,cpp}`, covering popup suggestion display, input-path popup prefix normalization, required-argument tooltip feedback, popup sizing, and completion-scope tooltip display. `RawEditorCompletionController` now keeps event routing, metadata/context/suggestion delegates, and insertion behavior only. Ratcheted `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from 516 to 400 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 78): extracted raw completion candidate generation into new `raw_editor/RawEditorCompletionSuggestionBuilder.{h,cpp}`, covering input-file path candidate discovery, command/option/value-context suggestion assembly, input-prefix filtering, duplicate filtering, and candidate sorting. `RawEditorCompletionController` now keeps popup/event/insertion behavior with thin suggestion-builder delegates. Ratcheted `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from 920 to 516 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 77): extracted raw completion context analysis into new `raw_editor/RawEditorCompletionContextAnalyzer.{h,cpp}`, covering prefix detection, active scope stack calculation, current command/scope label resolution, BlockEditor child/scope compatibility checks, and line-scope resolution. `RawEditorCompletionController` now keeps thin compatibility delegates while focusing on event handling, popup behavior, input-file candidates, and suggestion assembly. Ratcheted `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` from 1151 to 920 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 76): extracted Therion command catalog metadata loading from `RawEditorCompletionController` into new `raw_editor/RawEditorCommandMetadataLoader.{h,cpp}`, leaving the completion controller focused on runtime completion behavior and stable public delegation for context-help callers. Added the new loader to all app/test CMake targets and added a `StructureConstraintsTest` ratchet for `src/app/text_editor/raw_editor/RawEditorCompletionController.cpp` at 1151 lines. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 75): extracted remaining Block Details support helpers from `TextEditorTab.cpp` into new `block_editor/BlockEditorDetailsSupport.{h,cpp}`, reused the support module from `BlockEditorSelectionDetailsController` and `BlockEditorPanelBuilder`, removed duplicate local data-header/argument-label parsing helpers, and cleaned stale includes from `TextEditorTab.cpp`. `TextEditorTab.cpp` is now down to 1205 lines. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `1392` to `1205` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 74): added explicit `TextEditorTab` metadata accessors and routed BlockEditor/context-help metadata reads through the const `commandMetadata()` boundary, while localizing RawEditor completion/catalog mutation behind RawEditorCompletionController metadata helpers. Kept the `TextEditorTab.cpp` ratchet unchanged by inlining the trivial accessors. Verified with `cmake --build build -j4`, `python3 scripts/check_structure_constraints.py`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 73): moved shared Therion command/help/completion metadata out of individual `TextEditorTab` fields into new `TextEditorCommandMetadata`, then rewired RawEditor, BlockEditor, and context-help controllers to access the focused metadata store. This keeps parsing/behavior unchanged while reducing `TextEditorTab.h` ownership sprawl. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 72): extracted raw-editor panel construction, find/replace UI wiring, raw text edit setup, completion popup binding, status row construction, and raw help splitter setup from `TextEditorTab` into new `raw_editor/RawEditorPanelBuilder.cpp`. `TextEditorTab` now keeps only orchestration-level construction and public/editor-mode delegates for the raw surface. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 71): extracted Blocks-mode panel construction and Blocks-specific signal wiring from `TextEditorTab` into new `block_editor/BlockEditorPanelBuilder.cpp`, preserving toolbox/canvas/details widget ownership on `TextEditorTab` while moving the large Blocks UI setup under the BlockEditor directory. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 70): extracted UTF-8 conversion confirmation and in-memory encoding-state transition from `TextEditorTab::handleConvertToUtf8Triggered` into new `TextEditorEncodingController.{h,cpp}` while keeping load/save ownership in `TextEditorTab`. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `1919` to `1392` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 69): extracted raw editor cursor navigation, line/column reporting, cursor-position signal synchronization, contextual-help refresh on cursor movement, and current-line highlight updates from `TextEditorTab` into new `TextEditorCursorController.{h,cpp}`. `TextEditorTab` keeps the public navigation/query wrappers used by `MainWindow`, `MapEditor`, and tests, while the controller now owns the cursor/highlight workflow. Removed a stale `QTextBlock` include from `TextEditorTab.cpp`. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `1989` to `1919` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 68): extracted document status, display-name/path formatting, inline encoding status visibility, and dirty-state synchronization from `TextEditorTab` into new `TextEditorStatusController.{h,cpp}`. `TextEditorTab` keeps the public status/name wrappers used by `MainWindow` and `MapEditor`, while the controller now owns title/status refresh emission and current-state dirty comparison. Removed a stale `QTextDocument` include from `TextEditorTab.h`. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `2036` to `1989` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 67): extracted text-editor appearance refresh and source-surface color resolution from `TextEditorTab` into new `TextEditorAppearanceController.{h,cpp}`. The controller now owns palette-change synchronization for the raw help panel, BlockEditor details panels, QTextBrowser surfaces, block canvas background updates, and the conditional Blocks canvas rebuild after appearance changes; `TextEditorTab` keeps the public `sourceSurfaceColor()` wrapper used by `MainWindow`. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `2078` to `2036` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 66): moved contextual-help panel construction, Therion command/help metadata loading, and help collapse sizing from `TextEditorTab` into `TextEditorContextHelpController`. `TextEditorTab` now keeps thin delegates while the help controller owns the full raw contextual-help lifecycle alongside cursor help and validation tooltips. Removed stale command-catalog/JSON includes from `TextEditorTab.cpp`. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `2151` to `2078` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 65): extracted shared text-editor panel/text-browser surface styling from `TextEditorTab.cpp` into new `TextEditorSurfaceStyler.{h,cpp}`. The helper now owns palette synchronization, QTextBrowser viewport/document stylesheet synchronization, and left-border panel styling used by raw help and BlockEditor details panels, while `TextEditorTab` keeps only the calls at construction and appearance-refresh sites. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `2286` to `2151` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 64): extracted raw/block editor mode availability, mode switching, mode UI refresh, and Blocks-mode encoding-root enforcement from `TextEditorTab` into new `TextEditorModeController.{h,cpp}`. `TextEditorTab` now keeps thin compatibility wrappers while the controller owns the Raw/Blocks mode boundary and reuses the existing BlockEditor source-text helpers for line-ending-preserving normalization. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `2357` to `2286` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Reviewed remaining BlockEditor/TextEditor split after extracting apply/configure/delete/insert/move flows. Grouped the remaining surface into canvas rebuild/rendering, toolbox/scope logic, selection/details glue, and low-level app-facing wrappers, and documented the next extraction order in `Remaining BlockEditor Extraction Plan`. Verified with `git diff --check`.~~
- ~~Continued TextEditor structure cleanup (slice 63): extracted the public Therion source rewrite/insert API implementation from `TextEditorTab` into new `TextEditorSourceRewriteController.{h,cpp}` while preserving the existing `TextEditorTab` public methods used by MapEditor and other callers. The controller now owns cursor-preserving document replacement, inserted-line selection, dirty-state synchronization, document-change signaling, and optional Blocks canvas rebuild after command text replacement. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `2712` to `2357` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued TextEditor structure cleanup (slice 62): extracted the raw editor line-number gutter and current-line highlight text edit from `TextEditorTab.cpp` into new `raw_editor/RawEditorTextEdit.{h,cpp}`. The extracted widget now owns line-number sizing/painting, gutter scroll updates, and highlighted-line painting, while `TextEditorTab` only constructs the raw editor and updates the highlighted line number. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `2967` to `2712` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 61): extracted the Blocks Details option/value table delegate from `TextEditorTab.cpp` into new `BlockEditorOptionTableDelegate.{h,cpp}`. The delegate now owns per-cell completer editor creation while `TextEditorTab` only provides the option/value suggestion lambda when building the details table. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `3004` to `2967` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 60): extracted the Blocks toolbox drag list and Blocks canvas drag/drop view from `TextEditorTab.cpp` into new `BlockEditorToolboxList.{h,cpp}` and `BlockEditorCanvasView.{h,cpp}`, with the shared drag MIME string in `BlockEditorDragMime.h`. `TextEditorTab` now only constructs these BlockEditor widgets and wires their callbacks. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `3128` to `3004` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 59): extracted the custom Blocks Details readings-order token/chip editor from `TextEditorTab.cpp` into new `BlockEditorTokenTagEditor.{h,cpp}`. The widget now owns token parsing, chip rendering/removal, completion handling, and focus-change callbacks, while `TextEditorTab` keeps only construction and existing details-panel wiring. Removed the stale unused token completer helper during the move. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `3521` to `3128` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 58): moved Blocks canvas item helper logic (`resolveBlockCanvasItem`, `blockCanvasItemLineNumber`, `blockCanvasItemKind`, and `selectBlockCanvasItem`) from `TextEditorTab` into `BlockEditorCanvasSelectionController`. `TextEditorTab` keeps thin compatibility delegates for existing controllers while canvas-item selection behavior now lives with the selection controller. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `3533` to `3521` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 57): extracted Blocks toolbox list population, scope-combo population, selected-block auto-scope resolution, and scope tooltip updates from `TextEditorTab` into new `BlockEditorToolboxController`. The existing `BlockEditorToolboxDetailsController` remains focused on showing details for a selected toolbox command, while `TextEditorTab` now delegates toolbox/scope behavior through the new controller. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `3780` to `3533` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 56): extracted Blocks canvas rebuild/rendering orchestration from `TextEditorTab::rebuildBlocksCanvasFromText(...)` into new `BlockEditorCanvasRebuildController`, preserving encoding-root normalization, directive/comment card creation, parent/child layout, closure boundary guide solving, selected-line restoration, and details refresh behavior. `TextEditorTab` now delegates rebuilds through a narrow controller boundary. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4346` to `3780` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 55): extracted the Blocks canvas card item, directive-card color helper, and end-hint scene data role from `TextEditorTab.cpp` into new `BlockEditorCanvasItem.{h,cpp}`. Rewired canvas rebuild and boundary-guide code to share the extracted item/role definitions, and added build wiring across app/test targets. This is the prerequisite split for moving `rebuildBlocksCanvasFromText(...)` into `BlockEditorCanvasRebuildController`. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4588` to `4346` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 54): extracted Blocks drag/drop insertion orchestration from `TextEditorTab::handleCanvasDrop(...)` into new `BlockEditorInsertionController`, leaving `TextEditorTab` as a thin delegate while preserving mode guards, target resolution, insertion planning, template building, source insertion, and user-facing warnings. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4652` to `4588` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 53): extracted Blocks move/reorder orchestration from `TextEditorTab::handleBlockMoveRequest(...)` into new `BlockEditorMoveController`, leaving `TextEditorTab` as a thin delegate while preserving preview cleanup, move planning, compatibility warnings, source rewriting, and source application behavior. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4724` to `4652` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 52): extracted Blocks configure routing from `TextEditorTab::handleBlockConfigureRequest(...)` into new `BlockEditorConfigureController`, leaving `TextEditorTab` as a thin delegate while preserving command-options, `data`, single-value, and unsupported-configuration behavior. Added build wiring across app/test targets. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4811` to `4724` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 51): rewired all remaining Blocks configuration source replacement paths in `TextEditorTab::handleBlockConfigureRequest` to use `BlockEditorSourceController::replaceWithLines(...)` instead of calling `TextEditorTab::replaceTextForCommand(...)` directly. Dialog/configuration behavior stayed in place; only the final source-apply boundary changed. `TextEditorTab.cpp` stayed at the existing 4811-line ratchet, so no structure-limit change was needed. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 50): added `BlockEditorSourceController::replaceWithLines(...)` for line-ending-preserving full-source replacement and rewired Blocks move/reorder application to use that source-controller boundary instead of directly calling `TextEditorTab::replaceTextForCommand(...)`. `TextEditorTab.cpp` stayed at the existing 4811-line ratchet, so no structure-limit change was needed. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 49): moved Blocks-mode source line-range deletion from the private `TextEditorTab::removeSourceLineRange(...)` helper into `BlockEditorSourceController::removeLineRange(...)`, removed the TextEditor-owned helper, and kept `BlockEditorDeleteExecutor` using the source-controller boundary. This centralizes delete-source mutation with the rest of the BlockEditor source edit API. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4843` to `4811` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 48): moved Blocks canvas insertion source mutation from `TextEditorTab::insertLinesBefore(...)` into `BlockEditorSourceController::insertLinesBefore(...)`, deleted the TextEditor-owned insertion helper, and rewired `TextEditorTab::handleCanvasDrop` to delegate the source edit after planning the drop. This keeps the insertion workflow behavior stable while narrowing the remaining BlockEditor write boundary. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4903` to `4843` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 47): moved Block Details single-line apply source mutation behind `BlockEditorSourceController::replaceLine(...)` and rewired `BlockEditorApplyExecutor` to use that boundary instead of directly manipulating `TextEditorTab::editor_`. This keeps apply execution behavior stable while narrowing another BlockEditor dependency on TextEditor internals. Verified with `cmake --build build -j4`, `ctest --test-dir build --output-on-failure --timeout 300`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `git diff --check`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 46): introduced `src/app/text_editor/block_editor/BlockEditorSourceController` as the first source-session wrapper around `TextEditorTab` text access and source-range removal. Rewired `BlockEditorDeleteExecutor`, `BlockEditorLineBuildService`, `BlockEditorSelectionDetailsController`, and `BlockEditorApplyStateController` to read normalized source lines through this controller instead of reaching directly into `TextEditorTab::editor_`. This keeps editor ownership in `TextEditorTab` for now while narrowing the API boundary used by BlockEditor helpers. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `4944` to `4903` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued BlockEditor/TextEditor decoupling (slice 45): introduced shared `src/app/text_editor/block_editor/BlockEditorSourceText` source-text primitives for line-ending preservation, normalized line splitting, line joining, and source-line range replacement. Rewired `TextEditorTab`, block document outline building, block details loading/apply-state checks, line building, and delete execution to use the shared helpers instead of duplicating raw text splitting and CR stripping. This is a preparatory boundary for moving more source mutation out of `TextEditorTab` without changing editor ownership or user-visible behavior. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `5066` to `4944` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 44): extracted Blocks-mode delete execution from `TextEditorTab::handleBlockDeleteRequest` into new `src/app/text_editor/block_editor/BlockEditorDeleteExecutor`. The executor now owns delete-range resolution for comments, containers, data blocks, and leaf directives, plus user confirmation/error messaging, while `TextEditorTab` keeps the source-range removal primitive and delegates delete requests. This brings `src/app/text_editor/TextEditorTab.cpp` below the 5k-line milestone. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `5730` to `5066` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 43): extracted the Blocks-mode `data` block row configuration flow from `TextEditorTab::handleBlockConfigureRequest` into new `src/app/text_editor/block_editor/BlockEditorDataBlockDialog`. The helper now owns data-scope resolution, field parsing, mixed data/directive/comment row classification, row-table/delegate UI behavior, directive suggestions, and row-line serialization, while `TextEditorTab` keeps final document rewrite and schema-mismatch user notification. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `5791` to `5730` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 42): extracted the BlockEditor single-value command configuration dialog from `TextEditorTab::handleBlockConfigureRequest` into new `src/app/text_editor/block_editor/BlockEditorSingleValueCommandDialog`. The helper now owns value editing, contextual help fallback for closing directives, empty-value validation, and updated line serialization, while `TextEditorTab` only applies the returned source line. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6084` to `5791` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 41): extracted the legacy BlockEditor command-options configuration dialog from `TextEditorTab::handleBlockConfigureRequest` into new `src/app/text_editor/block_editor/BlockEditorCommandOptionsDialog`. The helper now owns dialog UI, ID/advanced positional-field handling, contextual help rendering, option-row validation, and updated command-line serialization, while `TextEditorTab` only invokes the dialog and applies the returned source line. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6174` to `6084` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 40): extracted shared BlockEditor command-option parsing into new `src/app/text_editor/block_editor/BlockEditorCommandOptionParser`. The parser now owns option-token boundary detection, bracketed value scanning, leading ID/value extraction, extra positional preservation, fixed-arity multi-value display serialization, and option-entry collection. Rewired both the legacy command configure dialog path in `TextEditorTab::handleBlockConfigureRequest` and structured Block Details loading in `BlockEditorSelectionDetailsController` to use the shared parser. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6190` to `6174` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 39): extracted Blocks-mode move source-line rewrite mechanics from `TextEditorTab::handleBlockMoveRequest` into new `src/app/text_editor/block_editor/BlockEditorMoveSourceRewriter`. The helper now owns source-span validation, moved-chunk removal, destination-index adjustment after removal, and reinsertion of the moved chunk, while `TextEditorTab` keeps move validation and document text application. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6225` to `6190` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 38): extracted Blocks-mode insertion source-line template generation from `TextEditorTab::handleCanvasDrop` into new `src/app/text_editor/block_editor/BlockEditorInsertionTemplateBuilder`. The builder now owns indentation lookup, comment/data/leaf command line creation, and container opening/closing template generation, while `TextEditorTab` keeps drop validation and source insertion. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6301` to `6225` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 37): extracted Blocks-mode move destination planning from `TextEditorTab::handleBlockMoveRequest` into new `src/app/text_editor/block_editor/BlockEditorMovePlanner`. The planner now owns move destination resolution for before/after-all, explicit end-hint targets, direct/nearest target cards, drop-into-compatible-container behavior, and destination parent-kind lookup, while `TextEditorTab` keeps user-facing move validation, no-op checks, and source chunk rewrite. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6389` to `6301` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 36): extracted Blocks-mode insertion destination planning from `TextEditorTab::handleCanvasDrop` into new `src/app/text_editor/block_editor/BlockEditorInsertionPlanner`. The planner now owns target-entry interpretation, parent selection, compatible-parent promotion, and append/root fallback planning, while `TextEditorTab` keeps user-facing warnings, inserted-line template generation, and source insertion. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6501` to `6389` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 35): extracted block-canvas scene item indexing, vertical drop-bound detection, and nearest/direct target resolution from `TextEditorTab.cpp` into new `src/app/text_editor/block_editor/BlockEditorDropTargetResolver`. Rewired `handleCanvasDrop` and `handleBlockMoveRequest` to share the resolver while keeping insertion/move parent rules unchanged in `TextEditorTab`. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `6817` to `6501` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 34): extracted shared block-document outline parsing from `TextEditorTab.cpp` into new `src/app/text_editor/block_editor/BlockEditorDocumentOutlineBuilder` (`BlockEditorDocumentEntry`/`BlockEditorDocumentOutline` + builder), covering line normalization, block/comment/directive entry detection, container parent relationships, container/data end-line resolution, and command-in-scope entry classification. Rewired both `handleCanvasDrop` and `handleBlockMoveRequest` to consume the shared outline builder, removing duplicated parsing/stack logic while preserving behavior. Added build wiring across app/test targets. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7032` to `6817` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 33): extracted block-directive and block-context rule helpers from `TextEditorTab.cpp` into new `src/app/text_editor/block_editor/BlockEditorDirectiveRules` (`blockDisplayName`, block open/close metadata registry + catalog override application, directive normalization, container/open/close classification, encoding/comment helpers, completion-context label/normalization helpers, and block-end matching). Rewired `TextEditorTab` to consume the shared block-editor rules module and added build wiring across app/test targets. This is a behavior-preserving preparatory split for larger block-canvas extraction. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7084` to `7032` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 32): moved completion helper implementations (`mergeHelpEntry`, `registerCompletionToken`, and completion-model rebuild) from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController`, rewired `TextEditorTab::loadHelpMetadata` to delegate completion-model rebuild to the controller, and updated controller-internal metadata paths to use controller-owned helper calls. This keeps completion metadata lifecycle logic co-located in the raw completion component and further narrows `TextEditorTab` to orchestration. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7141` to `7084` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Started block-canvas extraction track (slice 31): moved block-canvas boundary/end-hint hit-testing logic from `TextEditorTab::resolveEndHintContainerStartLineAtScenePos` into new `src/app/text_editor/block_editor/BlockEditorCanvasBoundaryController` and rewired `TextEditorTab` to delegate resolution through the controller. Added build wiring for the new controller across app/test targets. This is a behavior-preserving preparatory split for larger `rebuildBlocksCanvasFromText` extraction. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7198` to `7141` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 30): extracted command-catalog loop orchestration from `TextEditorTab::loadHelpMetadataFromCommandCatalog` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`applyCatalogCommandsMetadata`), preserving per-command syntax capture and delegate flow for argument metadata, options/type/subtype metadata, context wiring, command registration, and alias propagation. `TextEditorTab` now keeps only catalog object retrieval, block-directive metadata application, and one controller delegate call for command entries. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7207` to `7198` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 29): extracted command-level completion/metadata registration and help-entry merge from `TextEditorTab::loadHelpMetadataFromCommandCatalog` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`applyCommandRegistrationMetadata`), preserving command token registration, required-positional/signature metadata wiring, accepted-value completion registration, and command help-entry merge behavior before alias propagation. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7299` to `7207` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 28): extracted command options/value-arity/help/type/subtype catalog parsing from `TextEditorTab::loadHelpMetadataFromCommandCatalog` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`applyCommandOptionCatalogMetadata`), preserving option token indexing, option-value arity/argument-label/fixed-arity metadata, option help HTML generation, accepted values, `type_values`, and `subtype_by_type` indexing. `TextEditorTab` now keeps orchestration flow for argument/context/alias delegates and command-level merge/registration. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Documented stable editor-mode file/directory layout contract in `AGENTS.md` (`src/app/text_editor`, `raw_editor`, `block_editor`, `map_editor`) and explicitly prohibited drift back to legacy top-level map/text editor paths without intentional migration updates.~~
- ~~Enforced stable editor-mode layout in `scripts/check_structure_constraints.py` / `StructureConstraintsTest` by adding required-path presence checks for the stable layout and forbidden-path checks for legacy pre-reorganization files under `src/app/`. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7356` to `7299` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 27): extracted alias metadata propagation from `TextEditorTab::loadHelpMetadataFromCommandCatalog` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`applyCommandAliasMetadata`), preserving alias completion registration, required-argument/signature propagation, option/value/arity/help metadata propagation, subtype/type propagation, context alias registration, and block-context alias mapping. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Added explicit `TextEditor`/`MapEditor` refactor roadmap to `WORKLOG.md` under `Current Focus`, defining Track A (`TextEditorTab` split completion), Track B (`MapEditorTab` split sequencing), Track C (shared source-rewrite core extraction), plus per-slice verification/ratchet policy and line-count milestones.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7383` to `7356` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 26): extracted command-context and inline-command metadata parsing branch from `TextEditorTab::loadHelpMetadataFromCommandCatalog` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`applyCommandContextMetadata`), preserving context normalization, per-context command registration, block-context indexing, and inline-command completion registration fallback to `all` context when command contexts are absent. Kept alias propagation in `TextEditorTab` using the normalized context output from the controller. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7406` to `7383` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 25): extracted command-argument metadata parsing branch from `TextEditorTab::loadHelpMetadataFromCommandCatalog` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`applyCommandArgumentMetadata`), preserving command argument signature capture, required positional counting, `<person>` detection, and per-argument allowed-values indexing. Kept remaining catalog parsing flow (options/contexts/aliases) in `TextEditorTab`. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7409` to `7406` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 24): extracted command-signature helpers from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`commandArgumentSignaturesFor`, `commandHasRequiredIdArgument`, and `commandSupportsInlineIdField`), and rewired `TextEditorTab` to delegate these methods to the controller. Preserved required-argument detection semantics (`<...>` required and bracketed optional exclusion) in the controller implementation. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7492` to `7409` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 23): extracted completion-context normalization and scope-compatibility helpers from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`normalizeCompletionContext`, `isCompatibleChildKindForBlocks`, `isCommandDirectiveInScope`, `primaryInsertionScopeForCommand`, and `resolveScopeForCommandAtLine`). `TextEditorTab` now delegates these methods to the controller, while keeping fallback normalization behavior when controller is unavailable. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7567` to `7492` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 22): extracted raw input-file completion candidate collection from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`projectInputFileCompletionCandidates`) and rewired completion suggestion logic to use controller-owned candidate discovery. `TextEditorTab::projectInputFileCompletionCandidates` now delegates to the controller; removed now-unused local `normalizeInputSuggestionPath` helper and stale `QDirIterator` include from `TextEditorTab.cpp`. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7622` to `7567` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 21): extracted raw completion context/scope resolution from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`activeCompletionScopeStack`, `currentCompletionCommand`, and `currentCompletionScopeLabel`) and updated completion suggestion/popup paths to use controller-owned resolvers directly. `TextEditorTab` now delegates these methods and remains orchestration-only. Added narrow wrapper `isContainerDirectiveInstanceForParsedLine` on `TextEditorTab` to preserve container-instance semantics (including inline `source` behavior) without widening controller ownership. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `7919` to `7622` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 20): extracted raw completion suggestion-building from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (`buildCompletionSuggestionsForCursor`), including scope-aware command completion, option/value/subtype completion, input-path activation filtering, fixed/multi-arity option behavior, and fallback candidate filtering/sorting. `TextEditorTab::buildCompletionSuggestionsForCursor` now delegates to the controller; removed now-unused local input-prefix helpers from `TextEditorTab.cpp`. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `8073` to `7919` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 19): extracted raw completion event-filter handling from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController` via explicit tri-state event handling (`NotHandled` / `PassToBase` / `Consumed`), covering completion popup/editor key routing, popup accept/escape behavior, tab indentation path, trigger-key scheduling, and popup hide paths. `TextEditorTab::eventFilter` now delegates completion-specific handling and remains an orchestration shell for application palette/style events + base fallback. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `8128` to `8073` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 18): extracted raw completion token-apply insertion flow from `TextEditorTab` into `src/app/text_editor/raw_editor/RawEditorCompletionController` (token-boundary replacement, optional container-closing pair insertion, and cursor-restoration behavior). `TextEditorTab::insertCompletionToken` now delegates to the controller while preserving existing completion semantics. Added focused `TextEditorTab` wrapper `closingDirectiveForOpeningToken` to keep controller access narrow and explicit. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `8247` to `8128` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 17): extracted raw completion popup flow from `TextEditorTab` into new `src/app/text_editor/raw_editor/RawEditorCompletionController` (completion-token prefix detection, popup suggestion orchestration, scope tooltip display, and required-positional-argument tooltip when no candidates). `TextEditorTab::currentCompletionPrefix` and `TextEditorTab::triggerCompletionPopup` now delegate to the controller, while keeping completion suggestion generation and insertion semantics unchanged. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/text_editor/TextEditorTab.cpp` from `8352` to `8247` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 16): extracted raw find/replace/search workflow from `TextEditorTab` into new `src/app/text_editor/raw_editor/RawEditorFindController` (`show/hide` find bar, next/previous wrapped search, replace current/all, search option handling, and status messaging). `TextEditorTab` now delegates find/search public methods and search-bar slots to the controller. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Completed editor-mode folder reorganization under `src/app/text_editor`: moved raw editor files to `src/app/text_editor/`, Block editor components to `src/app/text_editor/block_editor/`, and map editor components to `src/app/text_editor/map_editor/`; updated `CMakeLists.txt`, app includes, and test includes accordingly. Retargeted `scripts/check_structure_constraints.py` tracked paths to the moved `TextEditorTab.cpp`/`MapEditorTab.cpp` while preserving ratchet limits (`8352`/`6468`). Verified with `cmake --build build -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `8517` to `8352` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 15): extracted block-canvas move-preview behavior from `TextEditorTab` into new `src/app/BlockEditorMovePreviewController` (container-end hint drop preview, before/after-all insertion preview, nearest-card insertion guide selection, and preview-line lifecycle). `TextEditorTab::updateBlockMovePreview` and `TextEditorTab::clearBlockMovePreview` now delegate to the controller, while retaining existing end-hint resolution semantics. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Continued `TextEditorTab` structure split (slice 14): extracted block-canvas selection/details sync from `TextEditorTab` into new `src/app/BlockEditorCanvasSelectionController` (line-targeted canvas selection, focus/centering behavior, and scene-selection to details-pane synchronization). `TextEditorTab::selectBlockInCanvasAndDetails` and `TextEditorTab::refreshBlockDetailsSelectionFromScene` now delegate to the controller via new thin block-canvas item wrappers that preserve `BlockCanvasItem` encapsulation in the translation unit. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `8613` to `8517` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 13): extracted toolbox-driven details-pane preview state from `TextEditorTab` into new `src/app/BlockEditorToolboxDetailsController` (toolbox command normalization, scene selection clear, unsupported-mode details reset, summary-help rendering, and apply/options disable state). `TextEditorTab::showBlockDetailsForToolboxCommand` now delegates to the controller. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `8693` to `8613` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 12): extracted block-details pane reset/empty-state UI setup from `TextEditorTab` into new `src/app/BlockEditorDetailsPaneController` (selection-clear state, default labels/placeholders, options/action reset state, and help empty-state message). `TextEditorTab::clearBlockDetailsPane` now delegates to the controller. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `9149` to `8693` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 11): extracted block-details selection loading into new `src/app/BlockEditorSelectionDetailsController` (selected-line parse/setup, mode-specific details-pane population for structured options/simple value/data header, unsupported-mode fallback state, and post-population help/apply-state refresh). `TextEditorTab::loadBlockDetailsForSelection` now delegates to the controller via focused owner wrappers for tag-editor and line-edit completion helpers. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Naming clarity cleanup: renamed `BlockEditorApplyController` to `BlockEditorApplyExecutor` across source/build/worklog references to clearly separate apply-action execution from `BlockEditorApplyStateController` state computation responsibilities, with no behavior change. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Updated `AGENTS.md` naming conventions to explicitly forbid near-duplicate/misleading type names and require concrete role-distinguishing suffixes when responsibilities differ (for example, `*Executor` vs `*StateController`).~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `9171` to `9149` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 10): extracted block-details apply execution from `TextEditorTab` into new `src/app/BlockEditorApplyExecutor` (line-build validation routing with warning messaging, target-line rewrite edit block, post-apply selection sync, and apply-state refresh). `TextEditorTab::applyBlockDetailsChanges` now delegates to the controller. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `9202` to `9171` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 9): extracted block-details apply-state computation from `TextEditorTab` into new `src/app/BlockEditorApplyStateController` (candidate-line validation pass, current-line comparison, apply-button enablement, and inline status/error text updates). `TextEditorTab::refreshBlockDetailsApplyState` now delegates to the controller. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `9353` to `9202` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 8): extracted block-details source-line synthesis from `TextEditorTab` into new `src/app/BlockEditorLineBuildService` (mode-specific line construction for `SimpleValue`/`DataHeader`/`StructuredOptions`, option-table validation integration, and inline-comment serialization). `TextEditorTab::buildUpdatedLineFromBlockDetails` now delegates to the service via thin wrapper methods for readings-order token text and required-argument checks. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Ratcheted `StructureConstraintsTest` baseline after cleanup: lowered `scripts/check_structure_constraints.py` limit for `src/app/TextEditorTab.cpp` from `9802` to `9353` lines so future changes cannot regress this extraction progress. Verified with `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`.~~
- ~~Continued `TextEditorTab` structure split (slice 7): extracted block-details contextual-help focus resolution from `TextEditorTab` into new `src/app/BlockEditorDetailsHelpController` (command-level help rendering, ID/extra-arguments/comment focus variants, and data-header/readings token-help variants). `TextEditorTab::updateBlockDetailsHelpForCurrentFocus` now delegates to the controller, preserving existing focus-driven help behavior through thin owner wrappers. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Codified editor-mode product intent in documentation: added `Editing Model Principles` to `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md` clarifying that Therion artifacts remain plain-text source of truth, TH2 `Visual` is a specialized `.th2` geometry surface, and `Blocks` mode is a guided lower-barrier editor for newer users while preserving source round-trip fidelity.~~
- ~~Updated repository instructions with explicit naming conventions in `AGENTS.md`: `Raw`/`Block`/`Map` are now treated as peer editor-mode domains; mode-specific implementation types shall use `RawEditor*` / `BlockEditor*` / `MapEditor*` prefixes, shell/orchestration types remain `TextEditorTab*` / `MainWindow*`, and neutral names are reserved for intentionally shared cross-mode logic. Also codified filename-to-primary-type alignment.~~
- ~~Continued `TextEditorTab` structure split (slice 6): extracted block-details option-argument editor synchronization from `TextEditorTab` into a dedicated stateful `src/app/BlockEditorOptionArgsController` (multi-value option value-cell editability gating, parameter-row editor creation/teardown, and row-to-value-cell synchronization callbacks). `TextEditorTab::refreshBlockDetailsOptionArgumentEditors` now delegates to the controller. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Continued `TextEditorTab` structure split (slice 5): extracted duplicated command-option row validation/serialization from `TextEditorTab` into new `src/app/TextEditorOptionValidation` and reused it in both block-details apply (`buildUpdatedLineFromBlockDetails`) and command configure dialog apply paths. Also moved Therion argument quoting/serialization helpers (`quoteTherionArgument`, `shouldQuoteTherionArgument`, `isBracketedTherionValueGroup`, `serializeTherionArgumentToken`) into shared `src/core/TherionCommandSyntax` so option-validation code is reusable outside the tab translation unit. Updated build wiring for app/text/map targets that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Added automated structure guardrail to prevent further growth of key large UI translation units: introduced `scripts/check_structure_constraints.py` with ratcheted per-file line limits for `src/app/TextEditorTab.cpp`, `src/app/MapEditorTab.cpp`, and `src/app/MainWindow.cpp`, and wired it into CTest as `StructureConstraintsTest` in `CMakeLists.txt`. This creates an always-on regression check so extraction work can only move file size downward over time. Verified with `cmake -S . -B build`, `ctest --test-dir build --output-on-failure --timeout 300 -R StructureConstraintsTest`, and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Continued `TextEditorTab` structure split (slice 4): moved cursor token candidate resolution and validation-help computation (`helpCandidateTokens`, `currentHelpTokenForCursor`, `validationHelpHtmlForCursor`) from `TextEditorTab` into `TextEditorContextHelpController`, leaving `TextEditorTab` with thin directive-normalization/opening-resolution wrappers consumed by the controller. This keeps contextual-help/validation decision logic in one focused component while preserving behavior. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Continued `TextEditorTab` structure split (slice 3): extracted shared command/option token utilities out of `src/app/TextEditorTab.cpp` into new `src/core/TherionCommandSyntax` (`commandOptionValueKey`, option-key parsing/recognition, subtype type-token resolution, option-arity normalization, and option-value tokenization). Rewired `TextEditorTab` to consume the shared core module and updated build wiring for app/text/map targets that compile the text editor. This is a no-behavior-change structure refactor that reduces local parser/validation utility coupling inside the UI translation unit. Verified with `cmake -S . -B build`, `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4`, and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Continued `TextEditorTab` structure split (slice 2): introduced a stateful `TextEditorContextHelpController` that now owns contextual-help pane refresh and validation-tooltip update flows (`updateContextHelp`, `updateValidationTooltipForCursor`) while reusing existing token-resolution/validation providers in `TextEditorTab`. This reduces direct UI-help coupling inside `TextEditorTab` without changing help behavior. Updated build wiring for app and UI test executables that compile `TextEditorTab`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4` and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Started `TextEditorTab` structure split (slice 1): extracted contextual-help HTML rendering into dedicated `src/app/ContextHelpController` (help list rendering, full command help rendering, summary preview rendering, validation-help rendering) and rewired `TextEditorTab` call sites to use the controller while preserving behavior. Updated build wiring for app and text/map UI test executables that compile `TextEditorTab`. Verified with `cmake -S . -B build`, `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4`, and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Extracted shared command-catalog loading into `src/core/CommandCatalogService` and rewired both editors to consume the same cached catalog object instead of re-opening/parsing `:/resources/therion_command_catalog.json` in multiple places (`TextEditorTab::loadHelpMetadataFromCommandCatalog`, `MapEditorTab` inspector symbol catalog loader, and orientation restriction loader). This is a no-behavior-change structure refactor that removes duplicated resource I/O/parsing paths and prepares further editor-split work. Verified with `cmake -S . -B build`, `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest MapEditorDetachedPaneTest -j4`, and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Stabilized core reliability/verification slice: (1) hardened `DocumentFile::writeTextFile` to encode-before-write and persist via `QSaveFile` commit semantics so encoder failures no longer truncate existing files, (2) moved structure-sidebar project scans off the UI thread with debounce + queued async refresh (`QtConcurrent` + `QFutureWatcher`) so live typing no longer triggers synchronous full-project rescans, (3) fixed project-structure numeric token recognition for scientific notation (`1e2`, `-2.5E-1`) and added regression coverage in `ProjectStructureIndexTest`, and (4) wired executable tests into CTest with default offscreen environment and disabled the two currently unstable headless text-editor GUI tests for deterministic CI baseline (`TextEditorCaretInteractionTest`, `TextEditorCompletionHighlightTest`). Verified with `cmake -S . -B build`, `cmake --build build -j4`, `QT_QPA_PLATFORM=offscreen ./build/TherionProjectStructureIndexTest`, and `ctest --test-dir build --output-on-failure --timeout 300`.~~
- ~~Added direct map-canvas orientation editing for orientable point symbols: selecting a point whose catalog metadata supports `-orientation` now shows a compact red orientation handle, dragging it writes the point `-orientation` option through the safe source snapshot path, and selection is restored after the scene refresh. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `git diff --check`.~~
- ~~Fixed TH2 Visual direct `line slope` handle drag release preserving selection: the handle now defers committing dragged `orientation`/`l-size` values until after Qt finishes the mouse-release event, clears stale pending-click selection state, then rebuilds the map scene and explicitly makes/reselects the owner line anchor visible so gated vertex/line-point controls do not disappear on drop. Verified with `cmake --build build --target TherionStudio`, `cmake --build build --target MapEditorDragUndoRedoSmokeTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `git diff --check`.~~
- ~~Added direct XTherion-style map-canvas editing for `line slope` point `orientation` and `l-size`: selecting a slope line anchor now reveals a red left line-point handle, dragging it previews the handle direction/length, and release writes `orientation ...` plus `l-size ...` line-point rows through the safe source snapshot path. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest MapEditorDragUndoRedoSmokeTest`, `./build/TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `git diff --check`.~~
- ~~Added XTherion-compatible line-point `orientation`/`l-size` editing in TH2 Visual `Inspector -> Selection`: selected point/line anchors now read standalone XTherion option rows, selected `line slope` anchors expose `Left size (-l-size)`, and source rewrites insert/remove `orientation ...` and `l-size ...` rows attached to the selected coordinate. Added focused editor tests for standalone line-point option rewrite/remove behavior. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Added an XTherion-style selected-line direction tick in TH2 Visual: selecting a `line` now shows a yellow perpendicular tick at the first vertex, using the first outgoing control handle when available and flipping when `-reverse on` is set. Updated specification and user manual, including Therion's left-side/free-space rule for the tick and pit/pitch teeth. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `git diff --check`.~~
- ~~TH2 Visual `Inspector -> Selection` now places point `Name (-name)` below `Subtype`, keeping point quick fields ordered as `ID`, `Type`, `Subtype`, then `Name`. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Fixed point quick-field parsing for negative coordinates: `point 463.0 -495.75 station-name` now treats `-495.75` as a coordinate rather than an option, so the inspector shows type/subtype fields and source rewrites target the real point type. Added a regression test for negative-coordinate point type rewriting. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Fixed TH2 Visual `Inspector -> Selection` point quick fields disappearing when a point line has no type token yet: the inspector now still shows editable ID/type/subtype controls, and source rewrite can insert a missing point type after the coordinate pair. Hardened the core point-type token finder so option values such as `-id old` are not mistaken for point type. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Changed TH2 Visual `Inspector -> Selection` quick fields so point, line, and area selections always expose `ID (-id)`, type, and subtype where supported; station points now expose separate `Name (-name)` instead of using name as the ID field. Updated `rewriteMapObjectQuickFields` and focused editor tests for point ID/name separation and area ID/type/subtype rewrites. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~TH2 Visual `Inspector -> Selection` now titles the selected-object quick-edit section by object kind (`Scrap`, `Point`, `Line`, or `Area`) and reduces its metadata to `Source line N`, removing the duplicated object kind from the body. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Added TH2 Visual `Inspector -> Selection -> Object` scrap projection editing: scrap selections now show ID plus an editable `Projection (-projection)` pick list, keep type/subtype hidden, and rewrite/clear `-projection` through a tested safe source rewrite path that preserves comments and CRLF where practical. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Corrected TH2 Visual `Inspector -> Selection -> Object` for scraps: scrap selections now show only ID in quick identity fields because scraps do not have type/subtype, while point/line/area keep catalog-backed type/subtype pick lists. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Refined TH2 Visual `Inspector -> Selection` object layout: metadata is now a compact kind/source-line row inside `Object`, destructive deletion moved below `Edit Object Settings...` into an `Actions` section, and `Advanced` was renamed to `Actions`. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Restructured TH2 Visual `Inspector -> Selection -> Object`: removed `Show in Source` and the separate `Apply Object Fields` button, moved ID/name before type/subtype, made type/subtype editable catalog-backed pick lists, and applies field changes directly through the existing safe source rewrite path. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Aligned TH2 Visual `Inspector -> Backgrounds` section chrome with `Selection`: `Grid`, `Layers`, `Position`, and `Adjustments` now use framed sections with headings inside the boxes, and the `Layers` add button stays in the section header row. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Simplified the TH2 Visual map inspector chrome by removing the redundant standalone `Inspector` heading and reinforcing only the left edge with a separate overlay line; the tab pane keeps native border styling for all original edges. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Switched map-pane detach/return iconography to Lucide screen-share assets: `Separate Map` uses `screen-share`, return state uses `screen-share-off`, and the detached map-window `Return Map` control is now icon-only with a `Return Map` tooltip. Updated resources, specification, and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Restored the detached map titlebar/content separator with a subtler top command-bar line while keeping the normal bottom line above the canvas/inspector content. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Hid redundant main-window map command groups while a TH2 map pane is detached: zoom/drawing/selection tools now stay only in the detached map toolbar, while the main toolbar retains document actions and the map return/focus control. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Corrected detached map command-bar separators: removed the toolbar top border that doubled with window chrome and restored a single bottom border above the detached map canvas/inspector content. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Tightened the leading edge of top command bars: left internal padding now matches the 4px top/bottom padding in main and detached map command bars, while right padding remains wider for trailing controls. Updated user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Made top command-bar icon-only buttons square and smaller: main and detached map `QToolButton`s now use fixed 26x26px chrome (14px icons), rather than asymmetric min-size-only geometry. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Reduced top command-bar icon scale in main and detached map toolbars: icon buttons now use 14px icons with 28x26px minimum button chrome, and text toolbar buttons use matching 14px icons. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Changed right-aligned document command toolbar controls to icon-only buttons: TH2 `Visual`/`Raw`/map detach and text `Raw`/`Blocks` now use compact `QToolButton` controls with tooltips/accessibility labels instead of text `QPushButton`s. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Removed the bottom border from workspace command bars (main window and detached map toolbar) so the separator below the toolbar is owned by the native tabs/content edge instead of being drawn twice. Updated specification. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Aligned TH2 Visual editor chrome with the other editor modes: added the same embedded top content separator below the file tabs, kept detached map windows from drawing that extra separator, and made the Visual inspector panel borderless/native so its tabs rely on platform `QTabWidget` rendering. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Reduced the file-tab left inset to a minimal 4px so the first native tab no longer collides visually with the editor-left seam while avoiding the earlier wide gutter; the command toolbar keeps its internal button padding. Updated user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Adjusted main file-tab vertical integration so tabs sit directly on the editor/canvas top border: restored the native pane top border as the content edge and applied a 1px tab/pane overlap to remove the blank band under tabs. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Restored native main file-tab rendering after the overlap stylesheet degraded tab appearance: removed all `mainEditorTabs` pane/tab-bar geometry stylesheet overrides and now rely on the native `QTabWidget` pane/frame for tab-to-canvas alignment. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Reduced the main file-tab and command-toolbar left gutter from the earlier wide offset to a compact 8px inset while keeping the explicit tab-content separator and single editor-left seam. Updated user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Reworked the main editor-left chrome to avoid tab-line gaps: replaced the parent border/layout inset/overlay separator combination with a dedicated 1px left divider column, removed the custom `mainEditorTabsContentSeparator`, and restored the native `QTabWidget` pane top separator. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Reworked the main document chrome cleanly: moved the shared command bar into a normal layout row above file tabs, removed the old absolute-positioned toolbar/separator overlay, gave the editor area one continuous left seam, removed duplicate sidebar/canvas left-edge borders, and added an explicit `mainEditorTabsContentSeparator` line so the horizontal border above editor content is visible across the tab row. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Simplified TH2 Visual `Inspector -> Selection` by removing redundant explanatory labels from selected-object, Object, Point / Vertex, and Geometry sections; the panel now shows only section headings, actions, values, and state controls. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Removed coordinate editing fields from TH2 Visual `Inspector -> Selection`: the former `Vertex / Position` group is now `Point / Vertex`, keeps orientation and line-vertex actions where applicable, and directs exact point/vertex/control-point coordinate edits to Raw mode. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Added `Inspector -> Selection -> Object` quick-edit fields for common map-object identity attributes: scrap ID, point/line/area type, subtype, and applicable ID/name. Applying the form rewrites the selected command through a new tested `TherionDocumentEditor::rewriteMapObjectQuickFields` helper while preserving unrelated options/comments where practical and recording a map source snapshot for undo/redo. Updated specification and user manual. Verified with `cmake --build build --target TherionDocumentEditorTest TherionStudio`, `QT_QPA_PLATFORM=offscreen build/TherionDocumentEditorTest`, and `git diff --check`.~~
- ~~Extended TH2 Visual `Inspector -> Selection` editing actions: `Object` now includes `Show in Source` and confirmed `Delete`, and selected line vertices expose `Insert Vertex`, `Delete Vertex`, and `Toggle Smooth` buttons backed by the existing safe source rewrite paths. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Restructured TH2 Visual `Inspector -> Selection` into explicit `Object`, `Vertex / Position`, `Geometry`, and `Advanced` sections: object metadata and scrap scale stay in object context, point/vertex coordinates and orientation appear only when applicable, line/area geometry summary reports vertex/curve counts with line state controls, and catalog-driven settings are isolated in Advanced. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Fixed catalog-driven option parsing for TH2 object configure dialogs and Blocks details so bracketed multi-token values such as scrap `-scale [...]` remain one option value; negative numbers inside the bracketed value are no longer interpreted as option keys, and bracketed values are serialized back unquoted. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Hardened TH2 Visual `Inspector -> Selection` scrap-scale precision setup by reapplying picture-pixel `0` decimal precision and real-point `4` decimal precision on every inspector refresh and source-bounds fill, not only when constructing the widget. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Adjusted TH2 Visual `Inspector -> Selection` scrap-scale numeric presentation to match XTherion more closely: picture scale points are labeled as pixels and displayed without decimal places, while real scale points use compact four-decimal precision instead of six fixed decimals. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Made TH2 Visual `Inspector -> Selection` scrap-scale editor fit narrow inspector widths better by replacing the two-column form layout with stacked coordinate blocks, compact X/Y fields, hidden spinbox arrows, and a shorter `Use Bounds` action label. Updated user manual. Verified with `cmake --build build --target TherionStudio` and `git diff --check`.~~
- ~~Added manual XTherion-compatible scrap scale editing in TH2 Visual `Inspector -> Selection`: selecting a scrap now shows picture/real point calibration fields, unit selection, source-bounds defaulting, and an `Apply Scale` action that rewrites the selected scrap `-scale [...]` option while preserving other scrap options/comments where practical. Added focused `TherionDocumentEditorTest` coverage for appending/replacing scale metadata and rejecting non-scrap commands. Updated specification and user manual. Verified with `cmake --build build --target TherionDocumentEditorTest TherionStudio` and `QT_QPA_PLATFORM=offscreen build/TherionDocumentEditorTest`.~~
- ~~Added XTherion-compatible raster background metadata writes for TH2 Visual: adding a background image now inserts/maintains `##XTHERION## xth_me_area_adjust`, `##XTHERION## xth_me_area_zoom_to`, and `##XTHERION## xth_me_image_insert` in the TH2 source with document-relative paths where possible; position, visibility, and gamma edits update the matching metadata line; removing a metadata-backed layer removes the source metadata and refreshes area metadata. The metadata parser now tracks source line, visibility, and braced filenames with spaces. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio TherionBackgroundMetadataTest`, `QT_QPA_PLATFORM=offscreen build/TherionBackgroundMetadataTest`, and `QT_QPA_PLATFORM=offscreen build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Implemented Therion/XTherion-compatible scrap scale handling in TH2 Visual: `-scale` now parses numeric, unit, ratio, and 8/9-parameter calibration forms for metric grid/source-unit conversion, 8/9-parameter transforms apply declared units, and map-driven scrap insertion writes an XTherion-compatible default `-scale` from current source bounds. Updated specification and user manual. Verified with `cmake --build build --target MapGeometryFeatureParsingTest TherionDocumentEditorTest MapEditorDragUndoRedoSmokeTest`, `QT_QPA_PLATFORM=offscreen build/MapGeometryFeatureParsingTest`, `QT_QPA_PLATFORM=offscreen build/TherionDocumentEditorTest`, and `QT_QPA_PLATFORM=offscreen build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed TH2 Visual insert undo history being truncated to one step: source-changing visual insert paths now mark the map command as in progress before mutating text so `documentTextChanged` queues a preserving scene refresh instead of clearing the map undo stack. Added regression coverage for two consecutive point inserts with two-step undo/redo. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed undo/redo for TH2 Visual map objects created from the visual editor: scrap insertion, point placement, completed line/area drafts, freehand/smart-trace draft commits, and Esc-committed line/area drafts now record source snapshots on the map undo stack so the first Undo removes the newly inserted source object and Redo restores it. Extended map smoke coverage for point/line insertion undo/redo, and updated map smoke test targets to link the current inspector dependency. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest`, `QT_QPA_PLATFORM=offscreen build/MapEditorDragUndoRedoSmokeTest`, and `QT_QPA_PLATFORM=offscreen build/MapEditorDetachedPaneTest`.~~
- ~~Moved TH2 Visual map inspector `Selection` tab to the first position before `Objects` and `Backgrounds`, making selected-object editing the primary inspector context. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Restored TH2 Visual map inspector `Selection` tab and moved the selected-object details/editing controls out of `Objects`, leaving `Objects` focused on the scrap-grouped object tree. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed TH2 Visual `Inspector -> Objects` scrap expand/collapse after custom click handling by letting clicks in the tree branch/disclosure area pass through to native `QTreeView`, and corrected station row labels so positional station names are treated as identifiers rather than duplicated as symbol type. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Corrected TH2 Visual `Inspector -> Objects` row text semantics so `type` means the Therion symbol type (for example `wall`, `station`, `altitude`) and `subtype` comes from `-subtype` or inline `type:subtype`, with optional `-id`/station `-name` after `:`. Updated user manual wording. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Adjusted TH2 Visual `Inspector -> Objects` row text to `type subtype: id/name` where available while keeping object type icons in the name column. Updated user manual wording. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Changed TH2 Visual `Inspector -> Objects` type presentation from textual `Scrap`/`Point`/`Line`/`Area` prefixes to the same toolbar icons (`puzzle`, `locate-fixed`, `spline`, `pentagon`), leaving row text for subtype/name content such as `wall`, `altitude`, or `station 13@hp`. Updated user manual wording. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Standardized TH2 Visual `Inspector -> Objects` labels to `Scrap: name`, `Point: type`, `Line: type`, and `Area: type`, with station points shown as `Point: station name`. Updated user manual wording. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Moved TH2 Visual `Inspector -> Backgrounds` grid controls to the top of the tab before `Layers`, so grid visibility/spacing is the first background-related setting. Updated user manual wording. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed TH2 Visual `Inspector -> Objects` scrap deselection reselect loop by suppressing automatic reselect for every source line in the deselected inspector subtree and consuming the corresponding tree-view mouse release event so native release handling cannot restore selection. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed TH2 Visual `Inspector -> Objects` repeat-click deselection flicker/reselect loop by suppressing automatic inspector/map cursor resynchronization for the source line that was just manually deselected until the user selects another object or moves to another source line. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Made TH2 Visual `Inspector -> Objects` visibility re-show restore map highlighting for multi-line selections and scrap subtrees as well as single objects by reselecting the full inspector subtree without centering the map view. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed residual parent+child highlight in TH2 Visual `Inspector -> Objects` when switching from a selected scrap row to a child object row by fully clearing both selection and current index before selecting the new row, then repainting the tree viewport. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Restored map highlighting after re-showing a hidden single object from TH2 Visual `Inspector -> Objects`: visibility toggle now reselects the line on show without centering the map view, preserving highlight/handles for the current inspector selection. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed TH2 Visual map-object visibility toggles exposing all edit handles: `applyInspectorObjectVisibility()` now avoids forcing selection-gated edit/handle items visible for non-hidden lines, and `updateGeometrySelectionPresentation()` suppresses handles for hidden source lines. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Stopped TH2 Visual `Inspector -> Objects` from centering the list on object/visibility clicks by splitting inspector selection synchronization into scrolling and non-scrolling paths; external cursor sync now uses `EnsureVisible` instead of `PositionAtCenter`, while visibility rebuilds restore selection without changing scroll position. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Stabilized TH2 Visual `Inspector -> Objects` mouse handling by consuming left-clicks on object name/visibility/delete cells in the tree viewport event filter and routing them directly to the inspector click handler, preventing native `QTreeView` focus/current updates from racing with explicit row selection. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Restored map selection from TH2 Visual `Inspector -> Objects` row clicks by explicitly invoking text navigation and map-selection synchronization in the name-column click path, covering clicks where the text cursor was already on the target line and would not emit a new line-change signal. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Reworked TH2 Visual `Inspector -> Objects` repeat-click handling to capture pre-click selection state from the tree viewport event filter before Qt mutates selection, and centralized row selection normalization through explicit `clearSelection` / `select(row)` / `setCurrentIndex(NoUpdate)`. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed remaining TH2 Visual `Inspector -> Objects` dual-highlight path by changing cursor-driven inspector synchronization to set both current and selected row through `QItemSelectionModel::ClearAndSelect | Rows`, instead of setting only the tree view current index. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Refined TH2 Visual `Inspector -> Objects` single-row selection consistency after repeat-click deselect changes: name-column click now explicitly sets `current` and `selected` to the same row via `QItemSelectionModel::ClearAndSelect | Rows`, preventing dual highlighted rows (parent + child) in the tree view. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed TH2 Visual `Inspector -> Objects` repeat-click toggle cycle: row deselection now uses pre-click selection state captured on `pressed`, so the cycle remains stable (`select -> deselect -> select`) on repeated clicks of the same object row. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed TH2 Visual `Inspector -> Objects` selection stability for visibility toggles: clicking the per-row `eye` / `eye-off` action now re-selects the clicked object row after tree rebuild so highlight no longer jumps to a different item. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added a shared full-width document toolbar row under the tab strip for all document tabs with left-aligned `Save`, divider, `Undo`/`Redo`, divider, plus right-aligned mode controls (`Visual`/`Raw`/`Blocks`) using Lucide `pen-tool`, `code`, and `toy-brick` icons as applicable to the active tab type.~~
- ~~Styled the main content splitter as a persistent 1px vertical divider so the separator between sidebar content and editor content runs continuously from the top content edge down to the status-bar border.~~
- ~~Added left inset for the document tab bar (`QTabWidget::tab-bar { left: 8px; }`) so the first tab has visible margin from the vertical content separator.~~
- ~~Replaced hardcoded tab-pane offset with dynamic tab layout styling: `QTabWidget#mainEditorTabs::pane` now uses runtime `tabBarHeight + commandBarHeight`, preventing command-bar clipping/squashing across themes and DPI scales.~~
- ~~Added runtime metric-based minimum widths for command-bar buttons (`Save`, `Undo`, `Redo`, mode buttons, and `Separate Map` / `Return Map`) so icon+text labels remain fully visible across platform themes and DPI/font scaling.~~
- ~~Re-anchored the shared command bar from `QTabWidget` internals to the central content host and positioned it via mapped `editorTabs_` coordinates, ensuring the bar spans the full editor width during splitter moves and theme/layout changes.~~
- ~~Fixed command-bar horizontal placement after programmatic splitter sizing by forcing geometry refresh after sidebar `setSizes(...)` operations and adding a deferred startup refresh (`QTimer::singleShot(0, ...)`), so the bar aligns to the editor pane and never overlays the left sidebar.~~
- ~~Refined command-row framing to match tab integration: strengthened top/bottom command-bar separators (`palette(mid)`), disabled tab-bar base line (`qproperty-drawBase: 0`), and applied `QTabBar::tab { margin-bottom: -1px; }` so tabs visually break into the top separator line.~~
- ~~Improved document-tab readability and focus clarity: styled inactive tabs with subdued contrast and hover feedback, and styled the active tab with stronger border contrast, base background integration, and bold label so the focused file is immediately obvious.~~
- ~~Reverted custom QTabBar skinning back to native/default tab rendering (kept only tab-strip left inset and pane-top layout offset), because the platform-default tab look provided better visual quality and clearer overall UX.~~
- ~~Added extra vertical breathing room between window title bar and tabs by applying a small tab-bar top inset (`top: 4px`) and updating pane/command-bar geometry math to account for runtime tab-bar Y offset.~~
- ~~Moved TH2 zoom controls (`Zoom In`, `Zoom Out`, `Fit`, `Fit With Background`) from the floating map-canvas toolbar into the shared top document command toolbar as the next group after `Undo`/`Redo`; kept floating map toolbar focused on history, draw, selection/draft, and touch controls. Updated specification/manual wording accordingly.~~
- ~~Moved all remaining TH2 map actions (`Select`, `Complete Draft`, `Insert Scrap`, `Point`, `Line`, `Freehand`, `Smart Trace`, `Area`, `Touch Controls`) from the in-canvas floating map toolbar into the top command toolbar, removed the floating map-canvas toolbar entirely, reordered right-side map controls to `Visual`, `Raw`, then `Separate Map` (with `external-link` icon), and added an equivalent top command toolbar to detached map windows. Updated specification/manual wording accordingly.~~
- ~~Removed redundant `Visual`/`Raw` buttons from detached map-window toolbar; detached window now keeps only context-relevant commands plus `Return Map`. Updated specification/manual wording accordingly.~~
- ~~Changed `Save`, `Undo`, and `Redo` in main and detached top command toolbars to icon-only controls (tooltips retained for discoverability). Updated specification/manual wording accordingly.~~
- ~~Removed the `Map` entry from the left activity rail and removed the associated sidebar pane, since map object/background workflows now live in TH2 Visual-mode inspector panels. Updated specification/manual wording accordingly.~~
- ~~Removed residual vertical spacer under the shared top command toolbar by collapsing hidden inline mode rows to zero height and tightening top panel padding for raw/blocks editor surfaces so map/text/block canvases start directly below the toolbar.~~
- ~~Fixed persistent post-toolbar vertical gap caused by tab geometry feedback: `refreshWorkspaceModeSwitcherGeometry()` now anchors tab-bar top to a fixed inset baseline instead of reading live `tabBar()->geometry().top()`, preventing oversized pane offset growth.~~
- ~~Corrected `QTabWidget::pane` vertical offset math to avoid double-counting tab-bar height; pane top is now derived from command-bar height (+ inset) so content starts immediately under the toolbar without an extra blank band.~~
- ~~Removed remaining outer margins around main editing surfaces by setting map-pane, raw-editor, and blocks-panel container margins to zero so map canvas, text editor, and block canvas are edge-aligned. Updated user manual wording accordingly.~~
- ~~Reduced the raw-editor/contextual-help splitter gutter from a wide handle to a thin 1px divider to remove the large apparent gap between editor and help panel.~~
- ~~Standardized splitter-handle visuals across all major views (main sidebar/content, raw editor/help, blocks toolbox/canvas/details, and map workspace/details) to the same thin 1px divider style for consistent resizing affordance. Updated user manual wording accordingly.~~
- ~~Adjusted splitter handles to a wider native visible grab style (matching the requested reference look) across all major views: main sidebar/content, raw help split, blocks columns, and map splits. Updated user manual wording accordingly.~~
- ~~Applied splitter-pane inner seam borders for the main (non-nested) sidebar/content split only: sidebar pane uses right border, editor pane uses left border, and outer window edges stay borderless. Nested splitters intentionally deferred. Updated user manual wording accordingly.~~
- ~~Removed default raw text-editor viewport frame (`QPlainTextEdit` frame shape set to `NoFrame`) so the raw canvas no longer shows extra top/bottom border lines while preserving splitter seam styling.~~
- ~~Restored raw text-canvas left/right borders after frame removal by applying explicit side-only borders on the editor widget (`border-left`/`border-right`, no top/bottom).~~
- ~~Applied matching side-only border treatment to map canvas (`QGraphicsView`): keep left/right borders and remove top/bottom borders.~~
- ~~Changed contextual-help pane chrome to left-border-only (no top/bottom/right) by setting help panel frame to `NoFrame` and applying palette-synced left-only border styling.~~
- ~~Removed the top border on the shared workspace icon command bar (main window and detached map window), so there is no extra line above the toolbar icons.~~
- ~~Removed the visual line above document tabs by disabling tab-bar base drawing for `mainEditorTabs` and removing pane border in the runtime tab-layout stylesheet.~~
- ~~Removed the tab-strip left inset so the first tab starts flush at the content edge (`QTabWidget#mainEditorTabs::tab-bar { left: 0px; }`).~~
- ~~Restored main document-tab base line (`qproperty-drawBase: 1`) so tabs match inspector-style bottom border treatment.~~
- ~~Forced main tab-row bottom line to match inspector tabs by adding explicit `QTabWidget#mainEditorTabs::pane { border-top: 1px solid palette(mid); }` in runtime tab-layout stylesheet.~~
- ~~Reverted the main-tab border experiment after visual mismatch feedback: `mainEditorTabs` returned to no pane border and `QTabBar` base drawing disabled (`qproperty-drawBase: 0`).~~
- ~~Removed left-divider border from Raw contextual-help panel (Blocks contextual-help left divider kept as requested).~~
- ~~Removed left-divider border from Blocks contextual-help panel as well, matching Raw contextual-help borderless style.~~
- ~~Removed the remaining border on the Blocks-mode contextual-help/details splitter pane by setting the right pane container frame to `NoFrame`.~~
- ~~Fine-tuned Blocks splitter left pane (toolbox): applied left-border-only pane chrome and contextual-help-like inner padding on toolbox contents.~~
- ~~Updated Blocks mode chrome: block canvas now uses side-only borders (left/right only), and block-editor contextual help uses left-border-only separation. Updated user manual wording accordingly.~~
- ~~Aligned dark-mode surface tones for structured-authoring workspace: contextual-help/details panels now inherit the active source surface color, and the shared top command bar now mirrors the active text-tab source surface to avoid tone mismatch between toolbar/help and source area. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Removed the right-edge border from the left activity rail (`SidebarActivityRail`) so the rail no longer draws a vertical divider line on its outer seam. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Removed the residual seam line next to the activity rail by disabling the `QFrame` border on the main content splitter (`mainContentSplitter_`), eliminating the splitter-frame left edge line while preserving resize handles. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Removed the remaining global rail-right seam rule from application-wide chrome stylesheet (`src/main.cpp`): dropped `QFrame#SidebarActivityRail border-right`, so no global stylesheet reintroduces that divider after local sidebar styling. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Applied a visual consistency pass across workspace chrome: unified command-bar button styling (icon/text button shape, hover/pressed/disabled states, separator spacing) in main and detached map windows, increased tab-row breathing room and tab rhythm (`top inset`, tab padding, selected-weight), polished sidebar pane/tree/header surfaces for consistent panel contrast, and improved contextual-help text typography spacing for readability in dark/light modes. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Applied a visual consistency pass across workspace chrome: unified command-bar button styling (icon/text button shape, hover/pressed/disabled states, separator spacing) in main and detached map windows, polished sidebar pane/tree/header surfaces for consistent panel contrast, and improved contextual-help text typography spacing for readability in dark/light modes. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Rolled back tab-rhythm styling tweaks (extra top inset + custom tab padding/weight) after regression in native tab rendering; returned `mainEditorTabs` tab metrics to platform-default behavior while keeping other consistency updates. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added an explicit top border above the workspace icon toolbar (main window + detached map command bar) so the toolbar row is framed by consistent top/bottom separators. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Adjusted native main-tab vertical alignment to sit lower toward the toolbar separator by increasing `mainEditorTabs` tab-bar vertical offset (`tabBarTopInset`) and introducing a small tab-to-toolbar geometry overlap (`tabToToolbarOverlap`) so no visible gap remains, while keeping tab shapes native (no `QTabBar::tab` overrides). Verified with `cmake --build build --target TherionStudio`.~~
- ~~Increased `mainEditorTabs` tab-to-toolbar geometry overlap slightly (`tabToToolbarOverlap: 2 -> 3`) so the active tab crosses the toolbar separator line a bit more while keeping native tab rendering. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Reverted active-tab border blending override (`QTabBar::tab:selected { border-bottom-color: palette(base); }`) after it degraded native selected-tab rendering; restored stable native tab appearance with no selected-tab-specific style overrides. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Replaced the command-bar full-width top border with a tiny `WorkspaceTopSeparator` overlay that paints the toolbar top separator in two segments, leaving a gap under the active native tab. This hides the border segment under the active tab without styling `QTabBar::tab` or changing tab rendering. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Expanded the active-tab separator gap by 4px on each side so the custom toolbar top separator clears native tab borders/antialiasing and no line peeks through under the active tab edge. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Widened the active-tab separator overlay gap further (`activeTabSeparatorGapPadding: 4 -> 14`) so the toolbar top line clears the full native tab base/overhang and avoids visible stubs under the active tab. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Tuned TH2 Visual inspector spacing: reduced the outer padding around the inspector and tab contents, increased inner padding inside `Position`, and wrapped `Adjustments` controls in a padded framed group so the right panel uses less edge margin while controls have more breathing room. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Grouped TH2 Visual `Backgrounds` layer add/remove controls in the `Layers` header row as compact `+` / `-` buttons, leaving `Hide`, `Up`, and `Down` in the action row below the layer list. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Matched the TH2 Visual `Backgrounds` remove (`-`) control to the add (`+`) control by changing the embedded inspector remove button from `QPushButton` to `QToolButton`, preventing it from expanding across the header row. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Removed the explanatory text above the TH2 Visual `Objects` inspector tree so the object list starts directly under the `Objects`/`Backgrounds` tabs. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added per-row TH2 Visual `Inspector -> Objects` action icons: visibility toggles (`eye` / `eye-off`) hide/show source-linked map items in the current editor view, and delete (`trash-2`) routes through the existing confirmed source-block deletion path for points/lines/areas/scraps. Bundled the new Lucide icons in Qt resources. Updated specification/manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Connected TH2 Visual object-tree delete actions to document undo/redo by changing source-span deletion to an undoable `QTextCursor` edit and letting map-tab undo/redo fall back to the embedded text editor when the map command stack is empty. Reapplied fixed right-edge action columns after every object-tree rebuild so visibility/delete icons stay aligned to the inspector edge. Updated specification/manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added repeat-click deselection for TH2 Visual `Inspector -> Objects`: clicking the same source-linked object row again clears the tree/map/object-details selection while leaving visibility/delete icon clicks unaffected. Updated specification/manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Converted TH2 Visual `Inspector -> Backgrounds` layer list to the same right-edge row-action pattern as `Objects`: each layer row now has `eye` / `eye-off` visibility and `trash-2` removal icons, replacing the separate `-` and `Hide` controls while keeping `+`, `Up`, and `Down`. Updated specification/manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed TH2 Visual `Backgrounds` row action handling so clicking the visibility/delete icon columns no longer triggers a selection-refresh rebuild before the icon action reads its row index. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Removed the background-position nudge arrow buttons from TH2 Visual `Inspector -> Backgrounds` and added bottom grid controls (`Show grid`, `Columns`, `Rows`) wired into map-scene rendering without clearing the map undo stack. Updated specification/manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Changed TH2 Visual map grid controls from row/column counts to a square metric spacing control (`Spacing (m)`), and render grid lines from source map coordinates so the grid can be used as a visual scale. Updated specification/manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Corrected TH2 Visual metric grid scaling: `Spacing (m)` now converts through parsed scrap `-scale` metadata before rendering, so 10 m no longer means 10 raw TH2 coordinate units. Added focused scale conversion coverage in `MapGeometryFeatureParsingTest`. Updated specification/manual. Verified with `cmake --build build --target TherionStudio MapGeometryFeatureParsingTest` and `./build/MapGeometryFeatureParsingTest`.~~
- ~~Added initial release packaging rules: CMake now installs `TherionStudio`, keeps macOS Homebrew-style app installation without Qt bundling, and configures Windows-only Qt deployment plus NSIS CPack installer generation for Therion Studio + Qt runtime only. Documented the build/package workflow in `docs/BUILDING.md` and updated specification/manual. Verified with `cmake -S . -B build`, `cmake --build build --target TherionStudio`, `cmake --install build --prefix /tmp/therion-studio-install`, and `git diff --check`.~~

### Manual QA Runs

- Encoding checklist pass (`docs/ENCODING_QA_CHECKLIST.md`):
- macOS: Deferred
- Windows: Deferred
- Linux: Deferred

## Phase Plan

Legend:

- `MVP`: required for first cross-platform release.
- `Post-MVP`: enhancement after MVP acceptance criteria are met.

### ~~Phase 0 - Foundation and Build System (`MVP`)~~

- ~~Spec scope: Sections 1, 2, 5, 6.~~
- ~~Goal: app skeleton, build tooling, app startup/shutdown stability.~~
- ~~Status: Completed.~~
- ~~Verification:~~
- ~~`cmake --build build`~~
- ~~Manual launch sanity on desktop target.~~

### ~~Phase 1 - Project Browser, Tabs, Session Lifecycle (`MVP`)~~

- ~~Spec scope: 3.1, 3.7, 3.8.1, 3.10.~~
- ~~Goal: open project, open/close tabs, dirty-state prompts, session restore.~~
- ~~Status: Completed (including `Close` and `Close All Tabs`).~~
- ~~Verification:~~
- ~~`cmake --build build`~~
- ~~Manual workflows: open project, close/save/discard prompts, reopen session.~~

### ~~Phase 2 - Text Editor Core (`MVP`, with Phase 6 dependency)~~

- ~~Spec scope: 3.2, 3.8.2, 3.9 (find/replace-related commands).~~
- ~~Goal: syntax highlighting, inline find/replace, contextual help panel, status strip.~~
- ~~Status: Completed for UTF-8 documents; encoding-conversion sub-scope deferred to Phase 6.~~
- ~~Verification:~~
- ~~`cmake --build build`~~
- ~~Manual workflows: find/replace, help-panel collapse/expand, document status display.~~

### ~~Phase 3 - Structure/Inspector and Therion Runner (`MVP`)~~

- ~~Spec scope: 3.4, 3.5, 3.6, 3.8.5, 3.8.6, 3.8.7.~~
- ~~Goal: parser-backed structure tree, inspector editing/writeback, async Therion runner.~~
- ~~Status: Completed baseline; iterative refinement ongoing.~~
- ~~Verification:~~
- ~~`./build/TherionProjectStructureIndexTest`~~
- ~~`cmake --build build`~~
- ~~Manual workflows: selection sync, inspector rename/apply, Therion run/copy output.~~

### ~~Phase 4 - TH2 Source Editing Engine and Data Integrity (`MVP`)~~

- ~~Spec scope: 4, 4.1, 3.11, 3.11.1.~~
- ~~Goal: safe source rewrites for point/line/area geometry and draft insertion with robust token handling.~~
- ~~Status: Completed (`MVP` acceptance scope).~~
- ~~Implemented highlights:~~
- ~~`appendScrapBlock`, `appendDraftGeometry`, `rewritePointCoordinates`, `rewriteLineAreaVertex`.~~
- ~~Extensive token-selection hardening: quoted tokens, option-led continuation lines, mixed metadata, CRLF preservation, scientific notation.~~
- ~~Corpus-style positive/negative rewrite fixtures: targeted coordinate rewrites, malformed block failures, and no-mutation guarantees on failure paths.~~
- ~~Verification:~~
- ~~`./build/TherionDocumentEditorTest`~~
- ~~`./build/TherionProjectStructureIndexTest`~~
- ~~`./build/MapBackgroundPlacementTest`~~
- ~~`./build/TherionBackgroundMetadataTest`~~
- ~~`./build/TherionXviParserTest`~~
- ~~`cmake --build build --target TherionDocumentEditorTest TherionStudio`~~

### ~~Phase 5 - TH2 Map Workspace and Background Alignment (`MVP`)~~

- ~~Spec scope: 3.3, 3.8.3, 3.11.1.~~
- ~~Goal: split/text/map workspace, geometry rendering/editing, background-layer management, fit/pan/zoom semantics.~~
- ~~Status: Completed (`MVP` closeout complete).~~
- ~~Implemented highlights:~~
- ~~Embedded map workspace modes and map help panel.~~
- ~~Point and line/area vertex drag-to-source writeback.~~
- ~~Undo/redo command integration with merge semantics and obsolete-command handling for failures.~~
- ~~Raster and `.xvi` background parsing/placement/alignment; multi-layer controls and session restore.~~
- ~~Added explicit touch-friendly controls mode in the map toolbar, persisted via session settings, and wired into mode-aware wheel/touch-pan input policy.~~
- ~~Enabled `Open Dedicated Window` map workflow: detach active TH2 map session into a dedicated top-level map window and keep the same in-memory document session (no duplicate document state).~~
- ~~Opening the same TH2 document now focuses the existing detached map window/session instead of creating a duplicate map editor instance.~~
- ~~Simplified TH2 workspace to a single embedded split layout plus detachable map pane; removed `Text Only / Map Only / Split` switcher UX to avoid conflicting states when map is detached.~~
- ~~Fixed undo/redo re-entrancy crash path for map geometry edits by deferring `refreshMapScene()` while map undo commands are executing, then applying a single pending refresh after command completion.~~
- ~~Remaining checklist (Phase 5 closeout):~~
- ~~Add focused regression coverage for live preview callback sequencing during map drags (anchor/control), including no-self-update and no-stale-preview assertions.~~
- ~~Add focused regression coverage for preview-vs-commit parity on representative line-edit operations (anchor move, smooth control move, corner control move).~~
- ~~Add a lightweight GUI smoke test for map drag/undo/redo workflow (single document, deterministic coordinates) to catch scene-refresh and undo-stack regressions.~~
- ~~Re-run Phase 5 verification suite after checklist completion and document residual parity gaps (if any) as Post-MVP.~~
- ~~Verification:~~
- ~~`./build/MapBackgroundPlacementTest`~~
- ~~`./build/TherionBackgroundMetadataTest`~~
- ~~`./build/TherionXviParserTest`~~
- ~~`./build/TherionDocumentEditorTest`~~
- ~~`cmake --build build`~~

### Phase 6 - Encoding and File-Format Robustness (`MVP`)

- Spec scope: 3.2 (encoding conversion), 4 (encoding preservation).
- Goal: detect/load non-UTF-8 inputs, explicit UTF-8 conversion action, preserve original encoding until conversion.
- Status: In progress.
- Implemented highlights:
- ~~Encoding-aware `DocumentFile` read/write path with detection and codec-preserving save semantics.~~
- ~~Therion `encoding ...` directive-aware decode path with codec-name preservation for save.~~
- ~~Text editor status row now exposes explicit `Convert to UTF-8` action for non-UTF documents.~~
- ~~Text editor conversion flow now requires confirmation and surfaces explicit encoding-state notes for save behavior.~~
- ~~Added `DocumentFileEncodingTest` regression coverage for UTF-8 detection plus Latin1, Windows-1250, `cp1250` alias, and Windows-1252 byte-preserving save round-trips.~~
- ~~Added inspector-fallback encoding-preservation regression coverage to `DocumentFileEncodingTest`: non-UTF (`cp1250`) read-detect-rewrite-write path keeps source encoding while applying structure rename and line-option rewrite.~~
- ~~Added complementary inspector-fallback coverage for `windows-1252` so the same read-detect-rewrite-write encoding-preservation path is validated across two legacy single-byte codec families.~~
- ~~Added negative `DocumentFileEncodingTest` coverage for unknown `encoding ...` directive tokens, verifying UTF-8 fallback decode and byte-preserving save behavior.~~
- ~~Expanded `DocumentFileEncodingTest` non-UTF fixture coverage with `cp1252` alias resolution, `latin2` directive decoding (`iso-8859-2`), and unknown-directive Latin1 fallback byte-preserving round-trip checks.~~
- Planned verification:
- ~~New unit tests for encoding detection and conversion paths.~~
- Manual workflows with sample non-UTF-8 Therion files (tracked in `docs/ENCODING_QA_CHECKLIST.md`).

### Phase 7 - UX/Accessibility/Platform Conventions (`MVP` baseline)

- Spec scope: 3.9, 3.13.1, 5.
- Goal: keyboard command parity, panel ergonomics, platform-native behavior checks.
- Status: Partially completed.
- Implemented highlights:
- ~~Map command shortcut, close-all-tabs shortcut, collapsible sidebar/console/help panes.~~
- ~~Activity-rail sidebar and console integration.~~
- Planned verification:
- Manual shortcut matrix pass on macOS/Windows/Linux.

### Phase 8 - Release Readiness and Packaging (`MVP`)

- Spec scope: 5.1, 5.2.
- Goal: packaging/signing/distribution readiness and performance verification pass.
- Status: Not started.
- Planned verification:
- Release artifact checks per platform.
- Performance smoke pass on representative project sizes.

### Phase 9 - Map Editor Parity Polish (`Post-MVP`)

- Spec scope: 3.3, 3.8.3, 3.11.1.
- Goal: close remaining map-editor interaction/UX parity gaps after MVP completion.
- Status: Not started.
- Planned focus:
- Cross-platform interaction parity polish beyond MVP baseline.
- Expanded GUI automation for map-edit workflows and regressions.
- Residual map command-surface and ergonomics refinements identified during manual QA.
- Planned verification:
- `MapEditorDragUndoRedoSmokeTest`
- `MapEditorDetachedPaneTest`
- Map geometry/background/parser regression suite
- Manual parity pass on macOS, Windows, Linux

### Phase 10 - Interactive Map Drawing and Insertion (`Post-MVP`)

- Spec scope: 3.3, 3.8.3, 3.11.1.
- Goal: replace draft-card-only insertion flows with direct canvas authoring workflows for point/line/area creation.
- Status: In progress.
- Implemented highlights:
- ~~Point mode click-to-place insertion with immediate source writeback.~~
- ~~Line/area click-by-click vertex capture with `Enter`/`Complete Draft` commit.~~
- ~~Line/area draft session controls: `Backspace`/`Delete` removes last vertex, `Esc` cancels active draft.~~
- ~~Map object details now expose `Edit Object Settings...` in Visual mode and route `scrap`/`point`/`line`/`area` edits through the same catalog-driven configuration workflow used by structured block selection (with cursor-line fallback when no geometry item is selected).~~
- ~~Map object details now provide inline orientation controls (enable + `0..359.999` degrees) for point symbols and selected line anchor vertices, with source-synchronized `-orientation` rewrite/remove behavior and catalog-driven type/subtype applicability gating.~~
- Planned focus:
- ~~Point mode click-to-place insertion in map coordinates with immediate source writeback.~~
- ~~Line/area click-by-click draft construction in canvas with explicit finish/cancel behavior and preview feedback.~~
- ~~Freehand line capture as sampled geometry input; keep smart-trace as explicit staged capability if tracing heuristics are not yet implemented.~~
- Mode-aware undo/redo semantics for drawing sessions (single completed draw gesture = one undo step).
- Planned verification:
- `MapEditorDragUndoRedoSmokeTest`
- New focused GUI smoke tests for point insertion and line/area draft completion paths
- Manual authoring pass on macOS, Windows, Linux (mouse, trackpad, stylus where available)

### Phase 11 - Structured Text Authoring Canvas (`Post-MVP`)

- Spec scope: 3.2.
- Goal: provide a more user-friendly structured authoring surface while preserving raw Therion text as canonical.
- Status: In progress.
- Implemented highlights:
- ~~Reverted earlier tree-table structured editor slice and replaced it with a new experimental `Blocks` mode in `TextEditorTab`.~~
- ~~Added `Raw`/`Blocks` mode toggle with `.th`-only availability gating and automatic fallback to `Raw` for caret-precise commands.~~
- ~~Implemented drag-and-drop block toolbox + canvas cards PoC for `survey`, `centerline`, `data`, `map`, `scrap`, `team`, and `explo-date`, with source-synchronized insertion templates.~~
- ~~Added first-pass block `Config` actions: rename for survey/map/scrap and value editing for team/explo-date, applied through source rewrite paths.~~
- ~~Expanded block configuration for centerline/data authoring: centerline `Config` now supports quick insertion of `team`, `explo-date`, and starter `data` definitions; `data` `Config` now provides a structured dialog for editing header columns plus multi-row measurement content in one source-synchronized commit.~~
- ~~Upgraded `data` block structured editor to a schema-driven table: table columns are derived from `data ...` field definition, measurement rows are edited in-table with add/remove actions, and non-tabular rows (for example `extend ...`) are preserved in place.~~
- ~~Improved `data` table ergonomics: visible table width/columns now auto-size from active `data ...` fields, and pressing Enter in the last column creates/advances to a new row at first column for faster entry flow.~~
- ~~Added generic editable non-tabular directive rows in `data` block dialog: users can add/remove/edit rows such as `extend ...` without raw-mode switch and without command-specific hardcoded buttons.~~
- ~~Added drag-based block reorder in `Blocks` mode canvas: dragging a card now reorders source lines, supports whole-span container moves (including nested lines), and supports drop-into-compatible-container behavior.~~
- ~~Implemented multi-parameter option editing in Block Details for structured-option commands: catalog option metadata now drives fixed-arity parameter forms (`Selected Option Parameters`), exact-arity validation, and safe Therion token serialization for multi-value options such as `survey -person-rename \"old\" \"new\"`.~~
- ~~Fixed structured-option value parsing for single-value options with spaces (for example `-title`): Block Details now treats the full value cell as one logical value for `EXACTLY_ONE`/fixed-1 options, while still tokenizing true multi-value options such as `-person-rename`. Also clarified action intent by renaming `Add Option` to `Add New Option`.~~
- ~~Simplified option-section label wording in Block Details and legacy configure dialog from `Options (key/value pairs)` to `Options` to reduce UI noise.~~
- ~~Moved option row actions into the `Options` header as compact `+` / `-` controls in Block Details and removed them from the footer action row; `-` is now selection-aware (disabled when no option row is selected).~~
- ~~Fixed structured-options UX bug for multi-parameter options (`-person-rename` etc.): fixed-arity `>1` option value cells in the options table are now read-only and must be edited via `Selected Option Parameters`; validation errors now focus the offending option row automatically.~~
- ~~Fixed false arity errors on `Apply` after editing `Selected Option Parameters`: structured-options loader now preserves multi-value token boundaries when populating options table (re-serializes fixed-arity `>1` values with Therion-safe quoting), so revalidation no longer mis-splits names containing spaces.~~
- ~~Fixed `Blocks`-mode crash when selecting `survey` (and other structured-option blocks): `refreshBlockDetailsOptionArgumentEditors()` now blocks options-table signals while toggling per-row editability/tooltips, preventing recursive `itemChanged -> refresh -> setFlags` stack overflows.~~
- ~~Updated `QtReimplementationSpecification.md` structured-block requirements to explicitly mandate catalog-driven fixed multi-value option arity handling in Block Details (one field per required value label), exact-arity validation before apply, and token-boundary-preserving serialization for spaced values.~~
- ~~Made structured-options primary field naming data-driven: when a block uses structured options but is not in explicit ID mode, the Block Details primary label now resolves from the command catalog’s first argument signature (for example `source` now shows `File Name` instead of hardcoded `Primary Value`).~~
- ~~Fixed contextual-help dark-mode live adaptation in text editing surfaces: `TextEditorTab` help browsers now sync foreground/link palette roles (not only background), apply palette-driven document CSS (`body`/`a`/`code`) on appearance change, and refresh help content immediately on theme switch. Also wired `QStyleHints::colorSchemeChanged` in `TextEditorTab` to force realtime updates when platform color scheme toggles without focus/cursor movement. Verified with `cmake --build build`.~~
- ~~Fixed remaining contextual-help dark-mode lag caused by stale overridden palette reuse: panel/help surface sync now resolves colors from the current application palette (`QApplication::palette(...)`) on every appearance update in both `TextEditorTab` and `MapEditorTab`, preventing white help surfaces after runtime theme toggles. Verified with `cmake --build build`.~~
- ~~Hardened runtime theme-change propagation for help panels without deprecated APIs: installed app-level event filtering in `TextEditorTab` and `MapEditorTab` (`qApp->installEventFilter(this)`) and now react to `ApplicationPaletteChange` / `PaletteChange` / `StyleChange` events from the application object directly. This ensures contextual help repaints even when child widgets do not receive local palette events during mode/theme transitions. Verified with `cmake --build build`.~~
- ~~Fixed contextual-help palette source mismatch during macOS light/dark switching: help panels now derive their palette from the active editor/map widget palette instead of the application palette, and `QTextBrowser` plus viewport receive explicit background/text stylesheets so stale document or viewport colors cannot leave the help panel white in dark mode or dark in light mode. Verified with `cmake --build build`.~~
- ~~Added missing Phase 11 spec alignment for implemented Blocks UX behavior: toolbox scope filter requirements (`Auto` default + selected-block-derived context + persistent manual scope), hidden editable Block Details when no canvas block is selected, and command-level contextual help focus retention while editing options.~~
- ~~Promoted Blocks terminology from PoC/experimental to standard structured block-canvas wording in active docs: updated spec section heading/requirements, user manual mode labels, and current worklog Phase 11 focus wording.~~
- ~~Improved toolbox-to-canvas insertion UX for structured blocks: dropping outside block bounds now supports explicit document-edge insertion (above first = document start, below last = top-level append at end), and incompatible nested drops now auto-promote to nearest valid ancestor/root scope instead of hard-failing immediately. Also updated Blocks mode button tooltip to remove obsolete “Experimental” wording. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Added transient end-pair drag hints in Blocks canvas: while dragging from toolbox or reordering canvas cards, container closing directives (for example `endsurvey`, `endcenterline`) are rendered near container bottoms and cleared on drag end, improving drop-boundary readability without permanent end-cards. Updated spec/manual and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed transient end-pair hint placement: end markers now anchor to the true visual bottom of each container subtree (container + descendants), not directly below opening cards, so `endsurvey`/`endcenterline` indicate actual closure position during drag operations. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Added minimum vertical spacing for transient end-pair hints: drag-time end markers now apply collision-aware Y-offsets (with horizontal-overlap checks) so stacked `end...` labels remain visually separated and readable in dense nested blocks. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Corrected end-pair spacing direction: overlapping transient `end...` hints now stack upward (instead of downward) to preserve natural nested closing order and avoid pushing labels below append-drop guides. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Corrected end-pair anchor baseline: transient `end...` hints are now anchored above container closure boundaries (not below), then collision-stacked upward, so drop-after-end zones remain unobstructed and visually coherent during drag. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed end-marker drop targeting for new block insertion: toolbox drag/drop now treats transient `end...` labels as explicit insertion anchors, so dropping on `endcenterline` inserts after the centerline block and dropping on `endsurvey` inserts after the survey block at parent/root scope. Also aligned preview line rendering for end-marker hovers. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- Planned focus:
- Expand configurable block coverage (including additional centerline command families and richer command parameters).
- Improve insertion anchoring rules and validation feedback for complex nested source structures.
- Add focused tests for structured-mode source insertion/rewrite safety.
- Planned verification:
- `cmake --build build --target TherionStudio`
- Manual `.th` workflow pass: drag-drop insert, configure, save/reload, and raw-mode round-trip checks

## Post-MVP Backlog (Phase-Linked)

- Phase 3/7: broader GUI automation for structure/inspector/runner/map workflows.
- Phase 4: expand rewrite corpus/regression coverage beyond MVP fixture set for higher corpus-level confidence.
- Phase 8: extended performance benchmarking and packaging convenience improvements beyond baseline release criteria.
- Nice-to-have: extend current `input` relative-path autocomplete semantics (explicit `./` / `../` activation and current-file-relative suggestions) to other path-taking Therion commands/options (for example `-sketch`) for cross-command UX consistency.
- Nice-to-have: completion ranking polish for complex contexts (keep highest-confidence context-valid tokens first and further reduce low-signal fallback entries).

## Test Inventory (Current)

Automated tests currently in-tree and used as regression baseline:

- `TherionDocumentEditorTest`
- `TherionProjectStructureIndexTest`
- `MapBackgroundPlacementTest`
- `TherionBackgroundMetadataTest`
- `TherionXviParserTest`
- `DocumentFileEncodingTest`
- `MapEditorInputPolicyTest`
- `MapEditorDetachedPaneTest`
- `MapEditorDragUndoRedoSmokeTest`
- `SessionStoreTest`
- `TextEditorCaretInteractionTest`
- `TextEditorCompletionHighlightTest`

## Recent Completed (Latest Slice)

### 2026-05-17

- ~~Unified TH2 `Visual`-mode right-side editing surface into an Inspector tab set inside `MapEditorTab`: finalized two-tab structure (`Objects`, `Backgrounds`), with `Objects` combining source-linked TH2 object tree (grouped by scrap) and selection details/settings editing in one panel. Wired object-tree selection sync to current source line and row-activation navigation back to source, and wired background controls to existing map-layer APIs/signals. Simplified the left sidebar `Map` page to an informational handoff message so map-object/background editing is no longer duplicated there. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio -j4`.~~
- ~~Removed the nonessential `Zoom 100%` map toolbar action, moved current map zoom into the main status bar before the `Select`/`Insert` mode badge, and removed active document path display from the status bar. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed accidental wrapping of the map toolbar detach icon by making the wrapping layout use fixed item sizes and layout-aware separator width instead of stylesheet-only separator margins. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Changed the map editor toolbar from an in-pane layout row to a compact floating overlay centered at the top of the map canvas, with a rounded palette-aware container and resize/theme repositioning. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Reordered the floating map toolbar into separated groups for zoom/view controls, undo/redo, select/complete draft, insertion/drawing tools, and touch/detach controls. Added a `Zoom 100%` action that resets the view scale while preserving the current center. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Added wrapping behavior to the floating map toolbar so controls flow onto additional rows when the map canvas is too narrow, preserving the requested group order and separators. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with warning-free `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Changed the map toolbar `Insert Scrap` action to use the bundled Lucide `puzzle` icon and registered `resources/icons/lucide/puzzle.svg` in the Qt resource list. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Corrected the floating map toolbar anchoring so it is parented to the `QGraphicsView` and positioned over the viewport rectangle, keeping it independent from scene panning/scrolling. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Bound the floating map toolbar chrome, labels, buttons, and Lucide icon recoloring to the application palette so it follows live light/dark mode changes instead of inheriting stale map-view colors. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Removed the persistent raised border around map toolbar icons by switching the in-canvas `QToolButton`s to auto-raised flat toolbar styling while preserving fixed icon metrics, tooltips, accessibility labels, and automation object names. Updated `QtReimplementationSpecification.md`, `docs/USER_MANUAL.md`, and verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Tightened the in-canvas map toolbar further: removed the `QScrollArea` wrapper that created an extra scrollbar/gap row, forced fixed 24px toolbar buttons with 15px Lucide icons, narrowed spacing/margins, and reduced map-pane outer margins so the toolbar is glued to the canvas chrome. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Moved the map editor icon toolbar into the graphical map pane so it is attached to the top of the canvas area instead of spanning the full tab above every pane. Tightened toolbar margins, spacing, and icon/button metrics, and hid the expanding status label from the visible strip to keep it compact. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Converted the map editor top toolbar from text-heavy buttons to compact Lucide icon buttons with tooltip/accessibility labels and stable automation object names. Bundled selected Lucide SVGs plus license notice, added Qt SVG rendering so icons recolor from the active palette, and updated map toolbar tests to find controls by object name. Updated `QtReimplementationSpecification.md`, `docs/USER_MANUAL.md`, and `docs/THIRD_PARTY_NOTICES.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Aligned embedded map canvas padding with the surrounding editor/help pane rhythm: the graphical map pane now applies 12/8-style outer margins instead of sitting flush against the workspace edges. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Removed the dedicated bottom `Map Help` pane from the embedded map editor so the map column is reserved for graphical editing; source contextual help remains available in the right inspector through the embedded text editor. Added a minimum/default map pane width and hardened the drag smoke test target for the new layout. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Reordered the embedded TH2 map workspace panes to match the requested workflow: graphical map editor first, synchronized source text editor second, and the source contextual help inspector on the right through the embedded `TextEditorTab`. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Enabled Blocks mode for `.thconfig` sources in addition to `.th`: updated file-type gating (`.th`, `.thconfig`, and `thconfig` filename), mode tooltip, and unsupported-mode messages so structured block canvas can open config files as requested. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest -j4` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Moved Processing-data top-level command contexts (`select`, `export`, `source`, `unselect`, `layout`, `maps`, etc.) from `therion_command_catalog.overrides.json` into the catalog generator logic (`scripts/generate_therion_catalog.py`) so `context: none` is parser-generated, not override-patched. Trimmed overrides back to alias-only patches and regenerated `resources/therion_command_catalog.json`. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/check_therion_catalog.sh`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed `.thconfig` Blocks parsing for config directives after `source`: updated catalog generation so contextless top-level config commands (including `select`, `export`, `unselect`, `source`) default to `none` context in `resources/therion_command_catalog.json`, and fixed inline-vs-block `source` handling in block-canvas scope parsing so `source file.th` stays leaf while `source ... endsource` opens a container. Added regression coverage in `TextEditorCaretInteractionTest` for `.thconfig` rendering of `select` and `export`, and in `tests/test_generate_therion_catalog.py` for generated contexts. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `cmake --build build --target TextEditorCaretInteractionTest -j4`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed Map editor contextual-help dark/light adaptation: `MapEditorTab` now reapplies help panel and `QTextBrowser` palette syncing (background + text/link roles) on runtime appearance changes, including viewport/document styling refresh, so help text remains legible after theme switch. Verified with `cmake --build build --target TherionStudio MapEditorDetachedPaneTest MapEditorDragUndoRedoSmokeTest -j4`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Removed extra inner inset in Blocks contextual-help panel: eliminated double padding (`right pane` padding + `inner help` padding) by setting help-panel inner margins to zero, so Raw and Blocks contextual-help panes now have matching effective spacing. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest -j4` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Aligned Raw editor layout with Blocks layout: switched raw contextual help from bottom strip to a bounded right-side inspector column, added matching 12/8 wrapper spacing around raw editor/help workspace, and kept help splitter behavior/resizing in the new horizontal arrangement. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`; verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest -j4` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Unified Blocks right-column `Contextual Help` background surface in dark mode: the whole pane (container + edit/help subpanels + text browsers) now syncs to one base-tone palette, eliminating mismatched dark shades between panel chrome and help content area. Reapplied on runtime palette/style changes. Updated `docs/USER_MANUAL.md` and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest -j4` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed dark-mode contextual-help background mismatch by syncing `QTextBrowser` base/viewport palette to parent panel surfaces (Raw help pane + Blocks help pane), and reapplying this on runtime appearance changes. This removes darker/lighter patch artifacts inside help content boxes. Updated `docs/USER_MANUAL.md` and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Unified Blocks right-pane frame model for consistent borders/padding: promoted the right pane container to a framed surface and removed nested frame styling from the inner Contextual Help panel, avoiding double-border appearance while preserving consistent 12/8 internal spacing. Updated `docs/USER_MANUAL.md` and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Constrained Blocks right-side details/help pane width to reduce oversized empty Contextual Help areas on wide windows: added bounded min/max width for the side pane and tightened default splitter sizes so surplus width flows into the canvas. Updated `docs/USER_MANUAL.md` and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Normalized TextEditorTab layout rhythm to reduce inconsistent borders/margins in window chrome: unified key panel paddings/spacings (search bar, mode row, Blocks panel, toolbox column, Block Details, and both contextual-help panes) to a consistent 12/8 scale for cleaner edge alignment with sidebar pages. Updated `docs/USER_MANUAL.md` and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Made Blocks canvas react live to runtime appearance changes: `TextEditorTab` now handles palette/style change events, updates canvas background brush immediately, and rebuilds block-canvas visuals while Blocks mode is active so boundary guides and block visuals refresh without restart/reload. Updated spec/user manual and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed `Readings Order` chip input residue after completion acceptance: when a suggestion is accepted, input clearing is now deferred to the next event-loop tick so Qt completer post-activation text insertion cannot re-populate the line edit. This keeps the tag input empty after chip creation (including repeated-token accepts). Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Stabilized Block Details `Data` header `Readings Order` tag editor: prevented duplicate chip insertion when accepting completion with `Enter`/`Tab` (popup-accept path no longer double-commits), enforced case-insensitive token uniqueness, and removed misleading sample-like `Style` placeholder text (`required` now shown). Updated `TextEditorCaretInteractionTest` to validate tag/chip behavior instead of legacy secondary-line-edit visibility. Updated spec/user manual and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Changed Blocks `data` insertion defaults to empty parameters: inserting `Data` now creates a bare `data` line (no auto-filled `style`/`readings` tokens and no body row), leaving header parameters to be set explicitly in Block Details. Updated spec/user manual wording and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Applied a general no-example-values insertion rule in Blocks toolbox: new command templates no longer auto-populate sample argument tokens (for example `new-...`, `./path/file.th`, `value1`) and comment insertion now uses a bare `#` skeleton. Updated spec/user manual wording and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed `Data` header `Readings Order` token-completion overwrite bug in Block Details: selecting a suggested token now replaces only the active token using a pre-activation text/cursor snapshot, instead of letting default line-edit completion erase previously entered tokens. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Restored missing `Add Comment Row` action in `Configure Data Block` (`Edit Data Rows...`) dialog toolbar and wired it to append a `Comment`-typed row with focus in the `Comment` column. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Hardened `Readings Order` token-completion replacement timing in Block Details: moved completion snapshot refresh to queued popup-refresh path (instead of immediate `textChanged`/cursor handlers) so activation-time default completer commits cannot overwrite pre-commit token context. This preserves multi-token prefixes like `from to len` when accepting `length`/`backlength`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Reworked `Data` header `Readings Order` input to tag/chip semantics in Block Details: replaced fragile free-text token completion with a token-tag editor (add token via Enter/Space/Tab/comma or completion activation, remove token via chip `✕`, backspace pops last chip). Synced chips to existing apply pipeline and kept non-`data` secondary-field editing unchanged via stacked field switch. Updated spec/user manual and verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted Blocks `data` insertion template to avoid implicit measurement-row creation: dropping/inserting a new `data` block now adds only the `data <style> <readings...>` header line and no auto-generated numeric row (`1 2 0 ...`). Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed large accidental parent->first-child gaps in Blocks canvas after closure-collision shifts: after any line-based downward shift for closure-spacing constraints, immediate parent/child rows are now compacted back to the normal card gap (`10px`) while preserving nested order, preventing oversized holes between container headers (for example `survey`) and first child cards (for example `centerline`). Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed residual end-cap/card edge overlap after drag compaction: added a final boundary re-clamp pass that recomputes each closure marker’s min/max corridor from current scene geometry and caps end-lines against the next block gap, preventing short closure stubs from touching following cards. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Narrowed post-shift gap compaction to parent->first-child only: stopped compressing all sibling gaps after closure-spacing shifts, so required room for intermediate closure lines (for example between `data` and following sibling after `endcenterline`) is preserved while still eliminating oversized parent header gaps. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted connector alignment overlap: vertical container connectors are now inset a few pixels to the right (`+2px`) and start exactly at opening-card bottom edge so they visually overlap start/end boundaries as requested. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed remaining nested closure-line collapse case (`endcenterline` vs outer `endsurvey`) by adding overflow recovery in boundary spacing pass: when min-gap constraints cannot be satisfied in-place, subsequent blocks are shifted down and later closure corridors are translated, preventing equal-Y collapse. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed closure-gap enforcement when there is insufficient native space: container closure layout is now solved in closing-line order and can shift subsequent blocks downward to create required space, so closure markers keep equal visual gap above and below instead of collapsing onto neighboring elements. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Applied stroke-aware closure spacing compensation: closure-line placement now adds half line width to above/below gap constraints so visible spacing from closure marker to neighboring blocks matches the normal card-to-card gap. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Aligned closure-line vertical spacing with card spacing rhythm: closure markers now use the same 10px gap above subtree content and before the next block, matching element-to-element spacing expectations. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Refined block-boundary connector geometry to direct vertical spans: removed start-side horizontal hook and now draw the container connector from just below the opening block down to closure, improving start->end readability for nested scopes. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed nested closure-marker overlap under tight spacing: closure Y layout now tracks both minimum and maximum allowed positions per container and applies bounded back-propagation, preventing `endcenterline` / `endsurvey` collapse to the same line when both are constrained by the next block gap. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted closure-marker geometry to match opening-card width: thick end-cap lines now span the same left/right width as their opening block cards (instead of short stubs), with a thin connector bridge from gutter trunk to the marker. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Updated structured-block boundary styling so only closure markers are emphasized: end-cap lines are now thicker and color-matched to their opening block family (`survey`, `centerline`, etc.), while connector rails remain thin. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted container boundary-guide closure layout to avoid stacked nested closures at identical Y positions: when nested containers end at the same rendered subtree bottom, closure markers are now vertically separated by a minimum gap so `endcenterline` and `endsurvey` remain visually distinct. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~

### 2026-05-16

- ~~Reworked structured-block end-pair visualization to persistent container boundary guides: each container now renders a gutter-side vertical connector from header toward closure plus a thicker closure marker line. Drag/drop boundary targeting now resolves from these closure markers (for `after block`) while keeping container-body drops for `inside block`. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Rolled back unintended Blocks canvas nesting-rail visuals and refocused end-pair behavior: removed extra gutter tree rails, kept transient `end...` hints only, and stopped hint layout from being pushed below cards by block-collision adjustment. End-hint labels now stay anchored to closure span ordering instead of drifting to canvas bottom during drag preview. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Further simplified Blocks nesting guides after visual review: removed per-child horizontal ticks and kept only low-opacity vertical gutter rails per container scope. This reduces ladder-like noise while preserving depth cues. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Reduced drag-hint overlap clutter in Blocks canvas: transient `end...` labels are now laid out in a dedicated left-gutter hint column (instead of near block interiors) and receive additional collision-avoidance against existing card rectangles. This prevents stacked `endcenterline`/`endsurvey` labels from landing on top of dragged or nearby cards in dense nested layouts. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted nesting-rail trunk start offsets: vertical rails now extend from just below the parent card down through child rows (instead of starting exactly at first-child center), restoring the missing top guide segments for parent->first-child relationships while keeping the cleaner gutter-only guide style. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Refined Blocks nesting-guide visuals: replaced long parent-to-child horizontal connectors (which crossed under cards and looked noisy) with cleaner gutter-only rails (vertical trunk + short child ticks). This preserves depth readability while reducing visual artifacts around nested `centerline`/leaf rows. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks nesting readability by rendering subtle tree guides in the canvas: each parent block now draws a vertical trunk plus short horizontal branch connectors to direct children (behind cards), making nested scope depth explicit even when transient `end...` hints overlap nearby content during drag preview. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed reordered-block boundary-zone regression: move-preview and move-drop outer-boundary detection now keeps full-scene vertical bounds (including the dragged block’s original position) instead of excluding source subtree lines. This restores visible/usable in-between drop zones when moving a block back into a parent container after previously moving it out. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Stabilized canvas-reorder preview lifecycle: for moved card releases, end-hint/drop-preview state is now kept alive through destination resolution and then always cleared on `handleBlockMoveRequest` exit via `qScopeGuard`. Non-move releases still clear immediately. This restores reliable drop-zone visibility while preserving explicit `end...` hint targeting at drop time. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved canvas-card drag precision by switching move-preview/drop anchor from dragged-card center to live cursor scene position (`QGraphicsSceneMouseEvent::scenePos`). This restores intuitive drop-zone highlighting near `end...` hints and boundary zones during reorder operations, especially when dragging larger cards where center-based anchoring missed intended targets. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed container-reorder target filtering: when dragging a container block, destination resolution now excludes the entire dragged subtree from scene-boundary detection and nearest-target matching (instead of excluding only the dragged root line). This prevents false `Cannot move a block inside itself.` when dropping near former child rows and allows placing moved containers after enclosing `end...` boundaries as intended. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed macOS drag-preview crash regression in Blocks canvas: `updateBlockMovePreview` no longer iterates a pre-refresh `QGraphicsScene::items()` snapshot after end-hint refresh clears/recreates hint items. Source-subtree exclusion now resolves from a pre-refresh list, then all preview geometry/hit logic uses a fresh post-refresh scene-item snapshot, preventing stale `QGraphicsItem*` dynamic-cast access during `dragMoveEvent`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed drag-time closure-hint drift for canvas reorder: transient `end...` hints now exclude the currently dragged block subtree from hint-layout span calculations, so parent closure hints (for example outer `endsurvey`) no longer follow the dragged card during move preview. This restores stable drop-after-parent targeting while dragging nested blocks out of a container. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed Blocks canvas reorder destination resolution to match drag hints: moving existing cards now honors explicit `end...` drop hints and outer-canvas drop zones (above first / below last), instead of relying only on nearest-card targeting. This enables dragging nested blocks (for example nested `survey`) out to parent/root scope by dropping on `endsurvey` or below all cards. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Stabilized Blocks transient end-hint anchoring: `end...` hint Y positions are now computed from parsed source container spans (`open`..matching `end`) projected onto visible canvas card lines, instead of relying only on scene parent-child geometry. This fixes cases where `endcenterline` could visually overlap the last `data` card and keeps drop-after-end targeting aligned with rendered hint position. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed Blocks end-pair drop-boundary UX for container closes (`endcenterline`, `endsurvey`, and other catalog block pairs): end hints are now rendered below container subtrees (instead of overlapping inside cards), nested close ordering is preserved when boundaries coincide, and drop targeting resolves end-hints using expanded proximity hit-zones so toolbox/card drops can reliably insert after the intended container boundary. Verified with `cmake --build build --target TextEditorCaretInteractionTest TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Hardcode-reduction continuation: removed `data`-scope fallback literals (`centerline`) from Blocks parsing/config/delete/move paths and replaced them with catalog + line-context scope resolution (`resolveScopeForCommandAtLine`). Also generalized `inline_commands` catalog ingestion to all commands/contexts (removed centerline-specific branch). Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Blocks hardcode-reduction stabilization: removed stale duplicated legacy `Configure Data Block` implementation accidentally left in `TextEditorTab::handleBlockConfigureRequest` after metadata-driven refactor, leaving a single active data-configuration path and restoring clean compile/runtime behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Hardcode-reduction continuation: replaced `handleCanvasDrop` command-template switch (previously hardcoded for `survey`/`centerline`/`data`/`map`/`scrap`/`team` variants) with catalog-driven insertion defaults. New insertion path derives required argument placeholders from command metadata (`helpEntries_` + parsed argument signatures + `commandArgumentValueTokens_`) and uses generic open/close handling for container commands via block-pair metadata. Kept a minimal `data` bootstrap row, now generated from parsed readings schema/defaults rather than fixed token list. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Hardcode-reduction continuation: `TextEditorTab` block open/close/container directive checks now load from catalog-derived `block_pairs` metadata at runtime (`resources/therion_command_catalog.json`) via `loadHelpMetadataFromCommandCatalog`, replacing fixed `survey/centerline/map/scrap/...` mappings in helper functions. Added safe default map fallback for startup/error cases and generalized completion context token normalization to accept catalog contexts by pattern instead of a static whitelist. Verified with `cmake --build build --target TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Parser-first catalog schema enrichment: extended `scripts/generate_therion_catalog.py` to emit normalized command `directive`, per-command `block` role metadata, and root-level derived `block_pairs` (open/close directives), then regenerated `resources/therion_command_catalog.json`. Updated `docs/THERION_COMMAND_CATALOG_PIPELINE.md` with explicit schema-extension policy (parser update -> regenerate -> consumer update) to prevent generated JSON hand-edits or UI hardcoded fallbacks. Verified with `scripts/refresh_therion_catalog.sh --skip-update` and `scripts/check_therion_catalog.sh`.~~
- ~~Safe hardcode-reduction slice: toolbox scope selector entries (`Inside ...`) are now catalog-context driven at runtime from `contextCommandTokens_` instead of hardcoded static list; `Auto/All/Top-level` remain fixed. Scope selection is preserved across metadata refresh. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Safe hardcode-reduction slice: removed the local hardcoded `isLikelyCenterlineCommand` list and replaced those decision points with catalog-driven scope checks (`isCommandDirectiveInScope(..., \"centerline\")`) in data-span detection, schema-mismatch detection, and delete/configure flows. Behavior preserved while making command recognition data-driven. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed leaf-command reordering coverage in Blocks mode: move/drop parsing now registers any catalog-recognized in-scope command directive (not only previously hardcoded `team`/`explo-*` subset), enabling reorder for commands like `date` and aligning move target resolution with canvas rendering. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Unified contextual-help panel spacing between Raw and Blocks modes: Blocks right-column help now uses the same framed `Contextual Help` panel pattern (`StyledPanel`, internal header row, no-frame browser, matching 8px inner margins) as Raw help, fixing header/padding inconsistency. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Improved toolbox drag/drop precision in Blocks mode: dragging from left command list now shows live dashed placement guide on canvas, and drop resolution now uses boundary-aware semantics (before/after near edges, inside compatible container in middle zone) instead of parent-only append behavior. Implemented by parsing current canvas block entries with parent/span metadata and resolving insertion target from nearest block-edge distance. Verified with `cmake --build build --target TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted container edge-drop semantics for block reordering: when a child-compatible block is dropped near a container card bottom edge, insertion now targets the start of that container body (before first child) rather than after the whole container span. This fixes comment placement between `survey` header and first `centerline` child. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Refined block drag drop-zone semantics for precise placement: nearest-target matching now uses card-edge distance (not center distance), and container child insertion is only applied when dropped in card middle zone; near top/bottom edges now force before/after placement. This fixes comment-card reordering into gaps such as between `survey` and `centerline`. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed toolbox-to-canvas insertion regression in Blocks mode: drop context no longer falls back to stale focused/selected card when not dropping directly on a card; it now resolves to nearest block by drop Y-position and promotes leaf targets to their container context (so child commands like `centerline` can still insert under `survey` when dropped near leaf rows). Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed Blocks-mode full-line comment card visibility and move handling: canvas rebuild/move parsing now scans source line-by-line (`parseLine`) instead of token-only `parseText`, so `# ...` comment lines are preserved as first-class `comment` blocks. Added live drag insertion guide line during card move to improve precise between-block placement feedback. Verified with `cmake --build build --target TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Stabilized multi-token `Readings Order` completion timing in Block Details: token-popup refresh is now scheduled post-event-loop (`QTimer::singleShot(0, ...)`) and guarded against completer replacement, preventing Qt internal full-line prefix updates from suppressing suggestions after the first token. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Added first-class `Comment` block insertion in Blocks mode: toolbox now exposes draggable `Comment` item, dropping it inserts full-line `# comment` in-context, full-line comments are rendered as canvas cards (`comment: ...`), and comment cards support delete/reorder flows. Updated `docs/USER_MANUAL.md` and `QtReimplementationSpecification.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed `Data` header `Readings Order` token completion in Block Details: suggestions now refresh per-token after whitespace (for example after `from `) and keep popup suggestions visible while typing subsequent tokens (for example `le` -> `length`, `left`). Verified with `cmake --build build --target TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Refined `Configure Data Block` mismatch/comment UX: empty body rows are no longer auto-classified as `Directive`, explicit full-line comment creation is now discoverable via `Type` pick list (`Data`/`Directive`/`Comment`) and `Add Comment Row`, and dialog now warns when existing rows no longer match active schema (`Schema no longer matches ...`). Updated `docs/USER_MANUAL.md` and `QtReimplementationSpecification.md`. Verified with `cmake --build build --target TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks `Configure Data Block` row-editing UX: added inline directive suggestions in `Directive` cells, enforced row-type edit locking (`data` rows lock `Directive`; `directive`/`comment` rows lock measurement columns), added `Add Comment Row` and comment-only row serialization, and enabled comment-only fallback for empty-directive rows with comment text. Also added catalog-backed completers for `Data` header `Style` and token-wise multi-value `Readings Order` fields in Block Details. Updated `docs/USER_MANUAL.md` and `QtReimplementationSpecification.md`. Verified with `cmake --build build --target TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks `Configure Data Block` UX for centerline data entry: removed duplicate editable `Columns` input from the rows dialog (schema now comes from the active `data <style> <readings order>` header in Block Details), added a trailing per-row `Comment` column for both measurement and directive rows with inline-comment round-trip serialization, and normalized generated data-field column headers to consistent humanized labels. Updated `docs/USER_MANUAL.md` and `QtReimplementationSpecification.md`. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added first-class inline comment support to Blocks `Block Details`: all editable blocks now show an always-visible optional `Comment` field, and line rewrites preserve/apply end-of-line comments using the existing comment marker style (`#`/`%`). Added card-level inline-comment badge rendering with hover tooltip on block cards. Extended `TextEditorCaretInteractionTest` to verify parsing/display and preservation of inline comment through Apply. Updated `docs/USER_MANUAL.md` and `QtReimplementationSpecification.md`.~~
- ~~Improved Blocks option-row editing UX in `Block Details`: `Add Option` now inserts an empty row (no forced default key), option/value cells use catalog-backed inline completion while keeping free-form text entry, and enum-backed option values are now validated on Apply (invalid values keep `Apply` disabled with inline status error). Added regression checks in `TextEditorCaretInteractionTest`. Updated `docs/USER_MANUAL.md` and `QtReimplementationSpecification.md`.~~
- ~~Adjusted Blocks contextual-help focus behavior per UX decision: `Block Details` now keeps parent command/parameter help visible even when options-table rows are focused/selected (no option-only takeover). Updated `TextEditorCaretInteractionTest` expectations and `docs/USER_MANUAL.md`.~~
- ~~Fixed sticky option-help focus bug in Blocks `Block Details`: command-level contextual help is now restored when selecting a block card, and option-specific help appears only while the options table is focused. Added regression checks in `TextEditorCaretInteractionTest` for focus transitions between block selection and option table rows. Updated `docs/USER_MANUAL.md`.~~
- ~~Fixed Blocks `data` Block Details parsing to use two positional fields (`Style` and `Readings Order`) instead of one merged `Columns` field. `Apply` now validates and serializes back as `data <style> <readings order>`, preserving existing data rows. Added regression assertions in `TextEditorCaretInteractionTest`. Updated `docs/USER_MANUAL.md` and `QtReimplementationSpecification.md`.~~
- ~~Changed Blocks toolbox default scope selector to `Auto (selected block)` (instead of `All`) so first-column filtering starts context-aware by default. Updated regression coverage by explicitly selecting `All` where broad command visibility is expected (`data` lookup) in `TextEditorCaretInteractionTest`. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted Blocks `Block Details` vertical rhythm: added extra spacing between the bottom action row (`Add Option` / `Remove Option` / `Apply`) and the `Contextual Help` header to improve visual separation of editing controls vs help content. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Refined Blocks `Block Details` action grouping for options editing: moved `Add Option` and `Remove Option` from above the table into the bottom stable action row under the options table, colocated with `Apply` (and optional `Legacy Configure...`) to reduce cursor travel and keep edit/commit controls in one place. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Reduced Blocks toolbox scope UI redundancy: removed the extra `Active scope: ...` label row beneath scope selector and kept scope context visible through selector choice plus command-list section headers; retained resolved-context hint as tooltip on scope selector in `Auto` mode. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted Blocks canvas hierarchy presentation to match encoding-root semantics: when `encoding` is pinned as fixed document root, all non-`encoding` cards are now rendered one additional visual indent level to make the root/content boundary explicit. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Enforced Blocks-mode `encoding` root invariant for `.th` documents: `encoding` is now managed as fixed document root (single instance, line 1, auto-inserted when missing from detected file encoding), excluded from toolbox insertion list, rendered as non-movable/non-deletable canvas card, and protected in delete/reorder handlers. Also wired UTF-8 conversion action to refresh the managed root directive value. Added regression coverage in `TextEditorCaretInteractionTest` for root insertion, uniqueness, toolbox exclusion, and non-editable details behavior. Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks toolbox UX with persistent context scoping: added scope selector (`Auto (selected block)`, `All`, `Top-level`, `Inside ...`), active-scope status label, and context-aware command list repopulation that can follow selected canvas block context in auto mode while preserving keyword filter behavior. Also hardened teardown safety by disconnecting all scene-selection callbacks and guarding auto-scope repopulation during destruction to avoid re-entrant updates. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Aligned Blocks `team`/`explo-team` editing with Therion token semantics for spaced person values: added explicit `explo-team` centerline leaf handling in Blocks compatibility/scan logic, split `team` details into `Person` + optional `Roles`, and updated line serialization to auto-quote person tokens when required (spaces/special chars) so output remains parser-valid (`team "..."`, `explo-team "..."`). Extended `TextEditorCaretInteractionTest` with quoted `team` and `explo-team` apply assertions. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Generalized Blocks simple-command argument mapping in `Block Details`: positional field labels are now derived from command argument metadata (`arguments`) and raw tokens are parsed into separate fields accordingly (for example `team` -> `person` + `roles`). Kept person-token quoting for `team`/`explo-team` serialization. Added regression assertion that `team` exposes separate fields in `TextEditorCaretInteractionTest`. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Hardened `team` argument field UX fallback in Blocks `Block Details`: even when argument metadata is incomplete, `team` now always renders separate `Person` and `Roles` fields (instead of collapsing to a single `Value` field), while preserving parser-safe serialization behavior. Re-verified with `cmake --build build --target TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Stabilized Blocks-details regression coverage around `team` argument UX: made block selection assert exact `Command: ...` token matching (avoids accidental `team` vs `explo-team` substring collisions), bound tests to named `blockDetailsHelpBrowser`, and kept caret-typing smoke edits isolated from `team` argument fixtures. Verified with `cmake --build build --target TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Normalized Blocks argument-field label formatting for consistency across commands: signature-derived labels now humanize to title case (for example `<date>` -> `Date`, `<person>` -> `Person`, `[<roles>]` -> `Roles`) with `_`/`-` converted to spaces. Re-verified with `cmake --build build --target TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Confirmed UX split: Block Details field labels are humanized for editing clarity while Contextual Help argument signatures remain raw Therion syntax. Rebuilt app and re-ran `TextEditorCaretInteractionTest` to verify no regression (`cmake --build build --target TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`).~~
- ~~Improved Blocks `Block Details` option-section visibility: hid `Options (key/value pairs)` label/actions/table for commands that do not support options (for example `team`, `date`, `explo-date`) and kept it visible for option-capable commands (or when option tokens already exist on that line). Added regression assertions in `TextEditorCaretInteractionTest` and updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks `Block Details` UX clarity: replaced line-number status (`Line N: ...`) with command-only status (`Command: ...`), renamed `Additional Positional Tokens` to `Extra Arguments (Advanced)`, and hid this advanced field unless unsupported positional tokens are actually present on the selected line. Updated `TextEditorCaretInteractionTest` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted Blocks-mode contextual-help content for parameter editing workflows: removed `Syntax` section from third-column help when working in Blocks mode (selected canvas blocks and legacy block-help fallback), while keeping Raw editor help unchanged. Added regression assertion in `TextEditorCaretInteractionTest` for selected-block help rendering. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted toolbox-preview help consistency in Blocks mode: restored `Summary:` prefix in third-column compact command preview while keeping the introductory instruction sentence removed and full syntax/arguments/options sections hidden. Updated `TextEditorCaretInteractionTest` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Further reduced toolbox-preview help noise in Blocks mode: removed the introductory instruction sentence and the `Summary:` label from left-column command preview in the third column; preview now shows only command title + description text. Updated `TextEditorCaretInteractionTest` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Refined toolbox-command contextual-help density: third-column preview for left-column command selection now renders compact help (`Summary` only) instead of full syntax/arguments/options sections; full help remains available when selecting actual canvas blocks. Updated `TextEditorCaretInteractionTest` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Refined Blocks third-column visibility semantics: when selecting toolbox commands for help preview, the editable `Block Details` section is now hidden; it becomes visible only when an actual canvas block is selected. Contextual help remains visible in both cases. Updated `TextEditorCaretInteractionTest` expectations and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks toolbox-to-details guidance: selecting a command item in the left commands column now switches the third-column `Block Details` pane into command-help preview mode (`Command: ...`) and renders contextual help before insertion (drag/drop remains unchanged). Added regression coverage in `TextEditorCaretInteractionTest`. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks `Block Details` action ergonomics: moved `Apply` and `Legacy Configure...` to a stable action row above `Contextual Help` so primary edit/commit actions remain fixed while help content varies in length and scroll position. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks layout ergonomics for wide parameter editing: replaced fixed three-column `QHBoxLayout` with a horizontal `QSplitter` (`Toolbox | Canvas | Block Details`) using non-collapsible panes and wider default details width, so users can resize the `Block Details` column when commands/options require more horizontal space. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved Blocks `Block Details` editing workflow: `Apply` now uses a single undoable in-document line edit (instead of full-text reset), `Apply` enablement is now change-aware/validation-aware, inline validation feedback is shown in details status, and details-mode coverage is broadened generically (container commands + option-bearing commands + simple value fallback), reducing legacy configure dependence for in-scope block cards. Added object-name hooks and expanded `TextEditorCaretInteractionTest` Blocks smoke checks for selection-by-kind and details apply/discard stability. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Extended Blocks `Block Details` pane coverage beyond container headers: `team`, `date`, and `explo-date` now edit directly as simple value fields, and `data` now supports direct header-column editing (`data ...`) with `Apply` while keeping `Edit Data Rows...` action for full row/body editing. Added focused regression coverage in `TextEditorCaretInteractionTest` for Blocks-mode details selection/apply workflow (`survey` id, `team` value, `data` header) plus a close/discard crash-guard path that destroys a selected dirty tab instance after Blocks interactions. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Simplified Blocks card actions: removed the per-card configure icon from canvas cards and kept delete-only icon action, since block parameter editing is now handled by selecting a card and using the persistent `Block Details` pane. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed Blocks-mode tab teardown/discard crash on macOS caused by details-pane updates during `QGraphicsScene` destruction: added explicit `TextEditorTab` teardown guard (`tearingDown_`), disconnected scene `selectionChanged` signal in destructor, blocked scene signals while clearing/rebuilding the block canvas, and avoided duplicate scene insertion for child `BlockCanvasItem`s (root items are now the only explicit `addItem` calls). This prevents re-entrant details-help `setHtml` updates while widgets/items are being destroyed. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Implemented persistent Blocks-mode `Block Details` pane (third column) synchronized with canvas selection: configure icon now selects/focuses block details instead of opening modal configure for supported kinds, and users can edit `survey`/`map`/`scrap`/`centerline` headers inline (`ID`, option key/value rows, preserved additional positional tokens) with explicit `Apply`. Field-level contextual help now follows currently focused parameter/option row in details pane. Unsupported block kinds expose `Legacy Configure...` fallback from details pane. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~

### 2026-05-15

- ~~Refined Blocks-mode `Configure` UX for `survey`/`map`/`scrap`/`centerline`: replaced coarse freeform parameter entry with structured `ID` + option key/value editing, added value-arity validation from catalog metadata, and made dialog contextual help focus on the actively edited parameter (`ID` or selected option row). `centerline` now supports optional header ID editing in the same flow. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Adjusted Blocks-mode `Configure` semantics to focus on object parameters: removed action-picker and command-insert flows from `Survey`/`Map`/`Scrap` and `Centerline` config, replacing them with line-parameter editors (`Name` + `Parameters` for `survey/map/scrap`, `Parameters` for `centerline`) plus inline contextual help. Nested command insertion remains toolbox drag/drop only. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Added inline contextual help to Blocks-mode generic `Configure Block` value dialog for leaf directives without option-specific editors. Help content is now rendered directly in the dialog from command-catalog metadata (summary/syntax/arguments/accepted values), with fallback to opening directive metadata for closing tokens. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Updated project-browser file creation defaults: `New .th`, `New .th2`, and `New thconfig` now initialize files with `encoding utf-8` as the first line. Added write-failure rollback/error handling for initialization failures. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed Files sidebar refresh behavior for project-browser file/folder operations: after `New .th/.th2/thconfig`, `New Folder`, `Duplicate`, `Rename`, `Delete`, `Rename Folder`, and `Delete Folder`, the tree now refreshes immediately and focuses the affected path via new `MainWindow::refreshProjectBrowserView(...)` helper (no restart required). Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCompletionHighlightTest TextEditorCaretInteractionTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Extended Blocks toolbox with live command filtering and broader catalog-driven command listing: added `Filter commands...` input above toolbox, switched list population from small hardcoded set to context-catalog-derived commands per section (`Top-level` / `Inside Survey` / `Inside Centerline`, including `all` scope), and added generic drop insertion fallback for newly visible leaf commands (for example `input`) using required-argument metadata placeholders. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Updated Blocks canvas card actions: replaced text `Config` button with icon-based action buttons (configure + delete) and added delete workflow that removes full logical block spans from source with confirmation (container blocks with matching end directive, `data` block including body rows, and single-line leaf directives). Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Extended `Blocks` canvas parsing from hardcoded leaf directives (`team`, `explo-date`) to catalog-context-driven leaf command rendering by scope (`none`/container context + `all`). This makes non-container directives such as `input` visible as block cards when valid in current scope. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Completed parser-level data-schema extraction for `centerline -> data`: `scripts/generate_therion_catalog.py` now emits argument-level `allowed_values` for `<style>` and `<readings order>` (with `<readings order>` marked as `N` arity), regenerated `resources/therion_command_catalog.json`, and wired `TextEditorTab` to consume argument-level metadata for data-column parsing (removed local hardcoded style set). Added regression assertions in `tests/test_generate_therion_catalog.py` for argument-level enums. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/check_therion_catalog.sh`, `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Continued command-catalog wiring for Blocks editor (slice 4): added catalog-driven `Insert Command` actions to `Survey`/`Map`/`Scrap` block configuration (in addition to rename), reusing shared command-template generation used by centerline inserts. Container inserts now derive command list from catalog context, generate placeholders from required-argument metadata, and auto-emit matching `end...` directive for block-opening commands where appropriate. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Continued command-catalog wiring for Blocks editor (slice 3): `Centerline -> Config` command-insert action list is now catalog-driven from `centerline` command contexts instead of fixed hardcoded options. Insertion templates now include: starter multi-line template for `data`, specialized defaults for `team`/`date`/`explo-date`, direct insertion for structural inline commands (`group`/`endgroup`/`break`), and generic placeholder/value templates for the remaining inline commands based on catalog argument/value metadata. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Continued command-catalog wiring for Blocks editor (slice 2): replaced hardcoded block nesting compatibility checks with catalog-context-based validation (`none/survey/centerline/...` + `all`) via `isCompatibleChildKindForBlocks`, with legacy fallback only when command-context metadata is missing. Also generalized block `Config` value editing for simple non-container directives without catalog options (previously hardcoded to `team`/`explo-date`). Updated user manual and verified with `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Started wiring command-catalog data into Blocks editor toolbox (slice 1): added catalog-derived `blockCommandContextsByKind_` map during metadata load and switched `populateBlockToolbox()` to render supported block kinds by context sections (`Top-level`, `Inside Survey`, `Inside Centerline`) based on `resources/therion_command_catalog.json` contexts. Kept minimal legacy fallback rows only when catalog metadata is unavailable. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TextEditorCaretInteractionTest TherionStudio`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Simplified contextual-help output by removing the `Related Keywords` section from default rendering (`TextEditorTab::renderHelpHtml`) to reduce help-panel noise for normal editing workflows. Kept metadata ingestion unchanged for completion/use-internals. Added GUI regression assertion in `TextEditorCompletionHighlightTest` to ensure help text does not include `Related Keywords`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Changed raw text-editor Tab behavior to spaces: pressing `Tab` now inserts four spaces (no literal `\t`), and editor tab-stop distance is configured to 4-space width for consistent visual alignment. Added `TextEditorCaretInteractionTest` regression checks for 4-space insertion and no-tab-character guarantee. Updated `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TextEditorCaretInteractionTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Improved end-pair robustness for help/highlighting paths: contextual help candidate resolution now maps closing directives to their opening command token (`end...` -> opening command) as explicit fallback, and GUI regressions now cover additional closing directives (`endcentreline`, `endline`, `endarea`, `endscrap`, `endmap`, `endsurvey`) for keyword highlighting plus `endcentreline` help resolution. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Changed `input` file autocomplete to explicit path-prefix activation: suggestions now start only when the current `input` argument token begins with `./` or `../` (no popup for bare `input ` or arbitrary text like `d`). Implemented activation gate in `TextEditorTab::buildCompletionSuggestionsForCursor`, kept `./` same-directory filtering and `../` parent-scope behavior, and updated `TextEditorCompletionHighlightTest` to cover hidden-popup-until-prefix plus `./` and `../` scenarios. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Fixed `input ./` popup-state regression and strict same-directory filtering: `TextEditorTab` now treats normalized-empty `./` prefix as a valid same-directory scope (not a literal starts-with filter that yields zero matches), explicitly hides stale completion popup when new suggestion set is empty, and applies final popup-level `./` filtering to prevent inherited `../...` entries from previous completion states. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Fixed `input` path completion for `./` prefixes: in file-path value context, completion now uses the full token prefix (including `/`) instead of fallback word prefix, normalizes `./` for candidate matching, and suppresses `../...` candidates when user explicitly types `./...` (same-directory intent). Added regression in `TextEditorCompletionHighlightTest` for nested-file `input ./` case to require same-directory candidates (`child.th2`) and reject parent-directory candidates (`../completion-highlight-test.th`). Verified with `cmake --build build --target TextEditorCompletionHighlightTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Added data-driven identifier syntax highlighting in `TherionSyntaxHighlighter`: positional ID arguments (for commands whose argument metadata contains `<...id...>` placeholders), ID-referencing option values (for example `-id`, `-projection`, `-layout` when option metadata declares ID placeholders), and optional closing-pair IDs (for example `endsurvey [<id>]`) now use dedicated `identifier` style from `resources/therion_syntax_palette.json`. Added GUI regression checks in `TextEditorCompletionHighlightTest` for `survey demo`, `line ... -id line-8`, and `endsurvey demo`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Finalized `input` suggestion format to plain current-file-relative paths (no forced `./` prefix): completion now returns forms like `data/hp.th` when target is below current file directory and `../...` only when needed. Also fixed relative-base computation to use absolute current-file directory when deriving candidate paths, preventing malformed deep `../../.../<project>/...` outputs. Verified with `cmake --build build --target TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `cmake --build build --target TherionStudio`.~~
- ~~Fixed malformed `input` suggestion paths and normalized same-tree results to `./...`: completion path derivation now canonicalizes project/base/candidate paths, computes project-contained relative candidates first, then re-relativizes to current file directory, preventing unstable cross-root `../../..../project/...` outputs. Suggestions now use `./...` for same-dir/subdir targets and `../...` only when needed. Updated `TextEditorCompletionHighlightTest` expectations accordingly and verified with `cmake --build build --target TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `cmake --build build --target TherionStudio`.~~
- ~~Adjusted `input` file-path completion to current-file-relative paths: candidate scan still walks project root recursively for `.th`/`.th2`, but emitted suggestions are now relative to the active file directory (fallback base: project root when current file path unavailable). Added regression coverage in `TextEditorCompletionHighlightTest` for nested-current-file behavior (`../completion-highlight-test.th`, `child.th2`). Verified with `cmake --build build --target TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `cmake --build build --target TherionStudio`.~~
- ~~Implemented project-file autocomplete for `input` command values: `TextEditorTab` now suggests `.th` and `.th2` files recursively from project root (including subdirectories) when cursor is in `input` positional file-argument context. If project root is unavailable, fallback root is current file directory. Added regression coverage in `TextEditorCompletionHighlightTest` with nested fixture (`subdir/child.th2`) and verified with `cmake --build build --target TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `cmake --build build --target TherionStudio`.~~
- ~~Fixed centerline `data` reading-token metadata completeness for highlighting/completion: parser now expands `[back]...` reading forms into both base and backward variants (for example `length` + `backlength`, `compass` + `backcompass`, `clino` + `backclino`) when extracting `data` allowed values from Therion-book prose. Regenerated catalog and verified presence of `length/compass/clino` plus backward variants in `resources/therion_command_catalog.json`; verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/refresh_therion_catalog.sh --skip-update`, `scripts/check_therion_catalog.sh`, `cmake --build build --target TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `cmake --build build --target TherionStudio`.~~
- ~~Fixed numeric highlighting for bracket-attached values in array-like argument lists (for example `-scale [-128 ... 0.0 m]`): `TherionSyntaxHighlighter::tokenLooksNumeric` now trims leading/trailing bracket and punctuation wrappers before numeric detection, so tokens like `[-128` and `0.0]` are styled consistently as numbers. Added GUI regression in `TextEditorCompletionHighlightTest` that compares formatting of bracket-attached `-128` against plain numeric `851` in the same `-scale` array line. Verified with `cmake --build build --target TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `cmake --build build --target TherionStudio`.~~
- ~~Fixed `endcenterline` alias highlighting gap: syntax highlighter now derives closing `end...` tokens from syntax metadata and expands them through command aliases (for example `centreline` alias `centerline` -> highlight `endcenterline` as keyword). Added regression coverage in `TextEditorCompletionHighlightTest` and verified with `cmake --build build --target TextEditorCompletionHighlightTest`, `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`, and `cmake --build build --target TherionStudio`.~~
- ~~Improved syntax-highlighting structure visibility and centerline inline value categorization: `TherionSyntaxHighlighter` now highlights closing directives (`end...`) as keywords by extracting closing tokens from command syntax metadata, and first-token keyword detection now uses keyword-token set (not only command-token set). Also added command-level allowed-value token styling for known command positional enums (for example centerline `data normal from to ...`, `infer ... on/off`) using catalog `allowed_values`. Added GUI regression asserting `endcenterline` is highlighted as structural keyword in `TextEditorCompletionHighlightTest`. Verified with `cmake --build build --target TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Fixed missing contextual-help resolution on centerline inline command lines: `TextEditorTab::helpCandidateTokens()` now includes resolved command fallback (`currentCompletionCommand()`) in addition to direct cursor token and parsed directive, so help lookup remains stable for inline centerline command contexts. Added GUI regression in `TextEditorCompletionHighlightTest` asserting help panel resolves `infer` entry inside `centerline` block. Verified with `cmake --build build --target TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Reviewed centerline inline-command metadata coverage and expanded parser extraction for autocomplete: `scripts/generate_therion_catalog.py` now emits standalone `centerline`-context command entries for centreline `\\comopt` commands that are not documented as top-level sections (for example `infer`, `mark`, `flags`, `walls`, `extend`, `station-names`, `team`, `date`, etc.), while avoiding collisions with existing top-level commands (`grade`, `cs`, `equate`). Added parser-side enum extraction refinements for inline descriptions (including `is one of:` and supported-flags clauses), keeping cross-reference command names out of enum suggestions. Updated generator regression (`test_centerline_inline_commands_have_command_entries`) and extended GUI regression (`TextEditorCompletionHighlightTest`) for `infer` `on/off` value suggestions. Regenerated catalog and verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/refresh_therion_catalog.sh --skip-update`, `scripts/check_therion_catalog.sh`, `cmake --build build --target TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Migrated `centerline data` metadata from overrides to parser-level extraction: `scripts/generate_therion_catalog.py` now derives `data` command metadata and allowed style/reading keywords directly from centreline `\\comopt` source text, and `resources/therion_command_catalog.overrides.json` no longer carries a `data` extra-command patch. Added explicit repo policy updates in `AGENTS.md` and `docs/THERION_COMMAND_CATALOG_PIPELINE.md` requiring parser/generator-first implementation for Therion metadata behavior changes, with overrides limited to short-term exceptions. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/check_therion_catalog.sh`, `cmake --build build --target TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Fixed missing `centerline data` value suggestions (`normal`, `from`, `to`, etc.) at metadata source level: added explicit catalog override entry for `data` command with `centerline` context and Therion-book-derived allowed value tokens in `resources/therion_command_catalog.overrides.json`, regenerated `resources/therion_command_catalog.json`, and extended generator regression (`test_centerline_data_command_values_are_available`) plus GUI completion regression (`TextEditorCompletionHighlightTest`) for `data` value suggestions and prefix filtering. Also refined completion behavior so commands with required positional arguments can still suggest catalog-defined value tokens while required positional input is incomplete. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/check_therion_catalog.sh`, `cmake --build build --target TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Enforced stricter data-driven Therion completion behavior and removed legacy UI magic fallbacks: `TextEditorTab` no longer injects hardcoded `line` option tokens (`-id/-close/-reverse`) and no longer derives centerline inline command suggestions from `option_key` when `inline_commands` is missing. Completion now relies on catalog metadata as the single source of truth for these paths. Added explicit repository guidance in `AGENTS.md` requiring metadata-source fixes first and only minimal, centralized, temporary compatibility fallbacks for Therion completion/highlighting/help behavior. Verified with `cmake --build build --target TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Corrected catalog source model for centerline command metadata: `scripts/generate_therion_catalog.py` now emits structured `inline_commands` for `centreline`/`centerline` from `\\comopt` blocks and keeps those entries separate from regular command `options`, preventing placeholder/prose leakage in downstream consumers. Updated `TextEditorTab` loader to consume `inline_commands` first (with backward-compatible fallback), regenerated `resources/therion_command_catalog.json`, extended generator regression (`test_centreline_inline_commands_are_structured`) and GUI regression (absence of `what/x/y/z/zero` in centerline completion), and updated pipeline docs. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/refresh_therion_catalog.sh --skip-update`, `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest`, and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest && QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Removed placeholder-noise centerline suggestions in command-position autocomplete (`what`, `x`, `y`, `z`, `zero`): centerline inline command suggestions are now derived from structured centerline `option_key` metadata (stripped `-` prefix) instead of raw syntax-text tokenization. Extended GUI regression in `TextEditorCompletionHighlightTest` to assert `data/date` are present and placeholder tokens are absent inside centerline command slot completion. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest && QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Fixed missing command-position autocomplete suggestions inside `centerline` blocks for inline centerline commands (for example `data`, `date`): `TextEditorTab` now derives additional centerline command tokens from the catalog `centreline` syntax rows and injects them into `centerline` scope completion candidates. Added GUI regression coverage in `TextEditorCompletionHighlightTest` to assert blank command slot inside `centerline` suggests both `data` and `date`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest && QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Added inline caret tooltip for invalid text-editor tokens: when cursor moves onto an invalid option/value token, `TextEditorTab` now surfaces a short validation tooltip near caret (with allowed values) in addition to the contextual help `Validation` panel. Tooltip emission is deduplicated by validation issue key to avoid repeated flicker on same token. Added GUI regression assertion in `TextEditorCompletionHighlightTest` that verifies invalid enum token produces editor tooltip text containing `Allowed: on, off, auto`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest && QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Added inline invalid-token explanation in text-editor contextual help: `TextEditorTab` now evaluates cursor token validity against command-option/value metadata and, for invalid option/value tokens, displays a dedicated `Validation` help panel with reason plus allowed values (including type-scoped `-subtype` domains). Added GUI regression assertion in `TextEditorCompletionHighlightTest` to verify invalid enum token (`line wall -close maybe`) drives contextual help content and lists allowed values (`on/off/auto`). Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest && QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Extended value-position autocomplete to honor catalog `value_arity` semantics in `TextEditorTab`: completion now resolves active value-context not only for immediate `-option <value>` slots but also for multi-value options (`value_arity = N`), keeps suggesting option-domain values across additional tokens (for example `line wall -context point ...`), suppresses already-used values in the same multi-value group, and avoids fallback to unrelated option tokens while still in strict value context. Added regression coverage in `TextEditorCompletionHighlightTest` for multi-value `-context` completion behavior. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest && QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Extended command-catalog-driven syntax validation from option names to option values in `TherionSyntaxHighlighter`: loader now captures per-command option `value_arity`, enum `allowed_values`, and symbol `subtype_by_type` maps (including aliases), and highlighter now marks invalid option values with wave-underline for enum domains and type-scoped subtype mismatches (for example `line wall -close maybe`, `line wall -subtype cave`). Added regression coverage in `TextEditorCompletionHighlightTest` for invalid/valid enum value cases and subtype mismatch validation. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest && QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Added focused GUI regression coverage for the latest autocomplete/highlighting UX rules in `TextEditorCompletionHighlightTest`: verifies required-argument gating (`survey` must collect `<id>` before option popup appears), verifies option suggestions appear once required positional input is present (`-title` expected), and verifies invalid-option syntax marking (unknown option token gets wave-underline formatting). Wired new target/resources in CMake and verified with `cmake --build build --target TextEditorCompletionHighlightTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCompletionHighlightTest`.~~
- ~~Improved raw-editor autocomplete and syntax-highlighting UX using command-catalog metadata: completion now enforces required positional-argument slots before offering options (for example `survey <id>` must be filled first), and manual completion shows a short tooltip when required arguments are still missing. Also upgraded syntax highlighting with command-aware option styling (`option`) and invalid-option marking (`invalidToken` with red wavy underline) by validating `-options` against per-command catalog `option_key` sets (including aliases). Updated `resources/therion_syntax_palette.json` and `docs/USER_MANUAL.md`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Implemented catalog-driven subtype value completion for symbol commands in raw editor: `TextEditorTab` now loads command-level `type_values` and `subtype_by_type` metadata from `resources/therion_command_catalog.json`, mirrors that metadata for command aliases, and resolves `-subtype` suggestions by current line type context (`line <type>`, `area <type>`, `point x y <type>`). When a specific type match is unavailable, completion falls back to the union of known concrete subtypes for that command (excluding wildcard/placeholder tokens), with global-token fallback kept disabled in value-context. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Completed focused catalog-parser audit for symbol commands and added structured type/subtype extraction for `point`/`line`/`area`: generator now emits `type_values` and `subtype_by_type` fields from thbook source sections (including `\\comopt` subtype tables), with cleanup to exclude option-name noise from type lists. This captures allowed combinations such as `line.wall -> {bedrock, presumed, ...}`, `line.survey -> {cave, surface}`, `point.station -> {temporary, painted, natural, fixed}`, and `area.u -> *` (arbitrary subtype). Added regression coverage `test_symbol_type_subtype_matrix_presence`, reverified `test_line_options_include_subtype`, regenerated `resources/therion_command_catalog.json`, and verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/check_therion_catalog.sh`, and `cmake --build build --target TherionStudio`.~~
- ~~Expanded catalog option extraction to include `\\comopt` blocks in addition to `\\options`, with signature-level dedupe in generator output. This restores missing line-command options (including `-subtype`, `-close`, `-reverse`, etc.) in `resources/therion_command_catalog.json` and reduces need for UI-side fallback patches. Added regression coverage `test_line_options_include_subtype`, regenerated catalog via `scripts/refresh_therion_catalog.sh --skip-update`, and verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/check_therion_catalog.sh`, and `cmake --build build --target TherionStudio`.~~
- ~~Added focused fallback option tokens for `line` command completion in raw editor (`-id`, `-close`, `-reverse`) so `line ... -` continues to provide useful suggestions even when catalog metadata only exposes a subset of line options. This fallback remains subject to used-option filtering and does not re-enable global cross-command fallback. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed option-context global-fallback leakage in text-editor autocomplete: when cursor is in `-option` position for a resolved command, empty per-command option candidate sets no longer fall back to global completion tokens. This prevents unrelated cross-command options (for example `-layout`, `-map-level`) from appearing after used-option filtering on map geometry lines such as `line ... -id ... -`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Tightened block-scope command completion behavior to strict mode inside known scopes: at command-position within nested scopes (for example `centerline`, `scrap`, `survey`), completion now keeps only scope-matching + `all` context commands and no longer falls back to global command tokens when scoped set is empty. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed whitespace-backspace autocomplete noise in raw text editor: auto-popup on `Backspace/Delete` now triggers only when caret is adjacent to a completion-token character (`[A-Za-z0-9_-]`), so deleting runs of spaces no longer reopens option suggestions unexpectedly. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed command-help value-domain source pollution at catalog-generation level: `scripts/generate_therion_catalog.py` no longer derives command-level `allowed_values` by unioning option enums, so identifier-first commands (for example `survey <id>`) no longer inherit option-only values (`on/off`) in contextual help. Updated generator regression expectation (`test_survey_allowed_values_are_not_polluted`), regenerated `resources/therion_command_catalog.json` via `scripts/refresh_therion_catalog.sh --skip-update`, and verified with `python3 -m unittest tests/test_generate_therion_catalog.py`, `scripts/check_therion_catalog.sh`, and `cmake --build build --target TherionStudio`.~~
- ~~Corrected contextual-help `Accepted Values` semantics for command tokens: option-level allowed values are no longer merged into the parent command help entry, so command docs such as `survey` no longer incorrectly display option-only values like `on/off` under `<id>` argument context. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Refined option-context autocomplete relevance: when suggesting `-options` for a command line, `TextEditorTab` now suppresses options already present on that same line (except the option token currently being edited), so duplicate suggestions like `-title` are not repeatedly offered after they are already set. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Narrowed value-context autocomplete to option-specific allowed values: `TextEditorTab` now stores per-command per-option value domains from catalog metadata and uses them when cursor is in an option-value slot, instead of falling back to broad command-wide value tokens. This removes noisy unrelated suggestions (for example generic `on/off`) while editing free-form option values such as `survey ... -person-rename ""`. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed autocomplete popup blink/disappear regression in raw text editor by removing custom popup hide-on-`FocusOut` handling from `TextEditorTab` and keeping popup dismissal on explicit interaction paths (`Esc`/confirm/mouse). Verified with `cmake --build build --target TextEditorCaretInteractionTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed raw text-editor click-to-place-caret reliability by explicitly focusing the editor viewport and setting cursor position on left-button press/double-click before default handling, while still dismissing active completion popup state. Added `TextEditorCaretInteractionTest` GUI smoke coverage that clicks into the editor and verifies typed text insertion at the clicked caret position. Verified with `cmake --build build --target TherionStudio TextEditorCaretInteractionTest` and `QT_QPA_PLATFORM=offscreen ./build/TextEditorCaretInteractionTest`.~~
- ~~Fixed raw text-editor caret placement regression after autocomplete popup interactions: editor mouse press/double-click/focus-out events now explicitly dismiss completion popup before default cursor handling, preventing popup-state interference with click-to-place cursor behavior in raw mode. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Improved TeX-to-plain-text normalization in Therion catalog generator summaries/descriptions: strip TeX comment fragments (`% ...`), normalize non-breaking spaces (`~`), convert `\[`...`]` note wrappers into plain parenthetical text, and normalize dash runs (`---`, `--`) for readable UI help text. Added regression coverage (`test_survey_summary_tex_artifacts_are_stripped`) and regenerated `resources/therion_command_catalog.json`. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py`.~~
- ~~Fixed catalog-driven autocomplete pollution and weak option-prefix filtering in `TextEditorTab`: stopped deriving option-like completion tokens from prose fragments (for example `station-name`, `cave-list`), normalized multi-form `option_key` parsing into strict `-option` tokens, and hardened cursor token prefix handling so partial option input (for example `survey ... -ti`) filters correctly to matching options such as `-title` instead of showing unrelated fallback entries. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed incomplete option extraction in Therion command catalog generator: `extract_block` now preserves block line structure (instead of whitespace-flattening), allowing full multi-item `\\options` lists to parse correctly (for example `survey` now includes `-namespace`, `-declination`, `-person-rename`, `-title`, `-entrance`). Updated text-editor catalog loader to register per-option `option_key` directly for completion suggestions (so options without explicit `-` in signature text still autocomplete as `-...`). Added regression coverage in `tests/test_generate_therion_catalog.py` for survey option-key set. Regenerated catalog and verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Added block-command completion end-pair insertion in text editor: confirming completion for block-opening directives on an otherwise empty command line now auto-inserts matching closing directive line (for example `survey` -> `endsurvey`, `map` -> `endmap`, `line` -> `endline`) and keeps caret on opening line for immediate argument typing. Includes duplicate-guard when immediate next line already matches closing directive. Updated user manual and verified with `cmake --build build --target TherionStudio` and `python3 -m unittest tests/test_generate_therion_catalog.py`.~~
- ~~Hardened completion popup key handling across focus targets: installed `TextEditorTab` event filter on editor, editor viewport, and completer popup view so `Enter`/`Tab` confirmation works even when popup has focus. Confirmation now inserts selected completion token and closes popup instead of inserting a newline. Verified with `cmake --build build --target TherionStudio` and `python3 -m unittest tests/test_generate_therion_catalog.py`.~~
- ~~Fixed text-editor completion confirmation key handling: when completion popup is visible, `Enter`/`Tab` now explicitly insert the currently selected completion token and consume the key event (preventing accidental newline insertion), and `Esc` closes the popup consistently. Updated user manual and verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Adjusted manual text-editor completion shortcut to avoid macOS Spotlight conflict: removed `Cmd+Space` trigger and kept `Ctrl+Space` as the explicit manual completion shortcut (auto-popup while typing remains enabled). Updated user manual and verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Enabled as-you-type text-editor autocomplete popup (no shortcut required): command/option/value suggestions now auto-open while typing in raw editor mode, with `Ctrl/Cmd+Space` preserved as manual trigger and popup auto-hide on whitespace/enter/escape navigation keys. Updated user manual wording and verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Renamed syntax-palette supplemental keyword field from `keywords` to `additionalKeywords` (no backward-compatibility fallback) and reset the default list to empty in `resources/therion_syntax_palette.json`. Updated `TherionSyntaxHighlighter` to read only `additionalKeywords`. Verified with `cmake --build build --target TherionStudio` and `python3 -m unittest tests/test_generate_therion_catalog.py`.~~
- ~~Removed legacy contextual-help metadata resource `resources/therion_help_metadata.json` and switched text-editor help/completion metadata loading to catalog-only (`resources/therion_command_catalog.json`). Removed legacy fallback code path from `TextEditorTab`, removed resource packaging entry from CMake, updated user manual wording, and verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Enriched generated catalog option schema with structured shape hints: each option now includes `option_key`, `value_arity` (`0`/`1`/`N`), and `value_domain` (`enum`, `number`, `path`, `string`, `keyword`, etc.) inferred heuristically from signature/value metadata. Added regression coverage in `tests/test_generate_therion_catalog.py` for option-shape field presence and known examples (`export -d` no-value flag, `select -recursive` enum value). Regenerated `resources/therion_command_catalog.json` and updated pipeline documentation. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py` and `scripts/check_therion_catalog.sh`.~~
- ~~Normalized generated command `contexts` to strict enums in catalog generator (`all`, `none`, `survey`, `scrap`, `centerline`, `map`, `surface`, `layout`, `lookup`) and removed prose/leak fragments from context extraction (for example `encoding` now resolves to `none`; `import` resolves to `all` + `survey`). Added regression coverage in `tests/test_generate_therion_catalog.py` to enforce allowed context enums and known context cases. Regenerated `resources/therion_command_catalog.json` and verified with `python3 -m unittest tests/test_generate_therion_catalog.py` and `scripts/check_therion_catalog.sh`.~~
- ~~Added a lightweight completion-debug hint for scope verification: when `Ctrl/Cmd+Space` opens completion popup, text editor now shows a short tooltip with detected completion scope (`top-level`, `survey`, `scrap`, `centerline`, etc.). Updated user manual and verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Extended catalog-backed completion with nested block-scope filtering for command-position suggestions: completion now derives active scope stack from directives above the cursor and prefers context-matching commands (`none`/top-level, `survey`, `scrap`, etc.) from catalog metadata before falling back to global command tokens. Verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Improved text-editor catalog-backed completion relevance: `Ctrl/Cmd+Space` completion now applies cursor-context filtering (`command` slot -> command keywords, option slot -> command options, option-value slot -> allowed values + options fallback) with global token fallback when command context is unavailable. Updated user manual and verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Wired generated Therion command catalog into text-editor runtime help/completion paths: `TextEditorTab` now loads `resources/therion_command_catalog.json` first (with merge fallback to `therion_help_metadata.json`), populates contextual help entries, registers completion tokens, and exposes a completion popup on `Ctrl/Cmd+Space`. Also added catalog-derived command keyword ingestion in `TherionSyntaxHighlighter` and bundled catalog resources in CMake. Updated user manual. Verified with `cmake --build build --target TherionStudio`, `python3 -m unittest tests/test_generate_therion_catalog.py`, and `scripts/check_therion_catalog.sh`.~~
- ~~Hardened Therion command-catalog section parsing and added generator regression tests: fixed `\\subsubchapter` heading extraction in `scripts/generate_therion_catalog.py` to prevent over-captured command names, regenerated `resources/therion_command_catalog.json` (32 clean commands), and added `tests/test_generate_therion_catalog.py` covering core command presence, command-name validity, override alias mapping, context examples, and metadata source-file recording. Verified with `python3 -m unittest tests/test_generate_therion_catalog.py` and `scripts/check_therion_catalog.sh`.~~
- ~~Added Therion command-catalog regeneration pipeline assets for long-term thbook sync: `scripts/update_thbook_snapshot.sh`, `scripts/refresh_therion_catalog.sh`, `scripts/check_therion_catalog.sh`, `scripts/generate_therion_catalog.py`, `docs/therion-source.lock`, and `docs/THERION_COMMAND_CATALOG_PIPELINE.md`; generated `resources/therion_command_catalog.json` with override support via `resources/therion_command_catalog.overrides.json`. Also made refresh script lock-aware (repo/ref defaults from lock) and hardened snapshot hash generation for both `shasum` and `sha256sum` environments.~~
- ~~Refined `Configure Data Block` width fitting pass: column width estimation now uses header section hints plus content widths, and a deferred post-show sizing pass (`QTimer::singleShot(0, ...)`) reapplies dialog/table expansion so wide column sets become visible when host/screen space allows. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Improved `Blocks` mode `Configure Data Block` sizing: dialog now auto-expands horizontally (bounded by host window/screen space) based on computed table column width so all columns are visible when practical. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Reworked `Blocks` mode `data` dialog row model to allow mixed ordering of directive and measurement rows in one table: added combined row table with `Type` + data columns + `Directive` column and row-level add/remove actions (`Add Data Row`, `Add Directive Row`). Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Adjusted `Blocks` mode `data` table width behavior to fill dialog width: column sizing now starts from content-based widths and expands to consume available viewport space, including live recompute on table resize. Updated user manual wording. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added generic directive-row editing to `Blocks` mode `data` dialog: non-tabular rows (for example `extend right`) are now editable via `Add Directive` / `Remove Directive` list instead of read-only preview. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Improved `Blocks` mode `data` table UX: dynamic column-based table sizing and enter-key row continuation (Enter on last column creates/advances to next row, focus to first column). Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed `Blocks` mode canvas stale-refresh issue after source rewrites triggered by block actions/reorder: `replaceTextForCommand` now rebuilds the block canvas immediately, so no manual Raw->Blocks toggle is required. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Hardened `Blocks` canvas drag-reorder gesture detection: reorder trigger now follows actual card-position delta on mouse release (instead of only internal drag-flag transition), fixing cases where visual drag occurred but source reorder was skipped. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed `Blocks` canvas drag-reorder no-op regression: drop-target resolution now ignores the dragged source card itself and falls back to nearest valid sibling target, so drag-and-drop reorder reliably applies source block moves. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added drag reorder for experimental `Blocks` mode canvas: dragging existing cards now reorders underlying source blocks (whole-span moves for container blocks, including nested lines), with compatibility checks for parent/container drops. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Implemented schema-driven `data` block table editing in experimental `Blocks` mode: measurement table columns are generated from `data ...` column definition, row add/remove actions are available, and non-tabular rows are preserved in source order. Updated specification and user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Expanded the experimental text-editor `Blocks` mode with practical centerline/data editing flows: centerline cards now provide quick insert actions (`team`, `explo-date`, starter `data`), and data cards now open a structured editor dialog for `data ...` columns plus multi-line body rows with source-synchronized rewrite. Updated specification and user manual wording. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Delivered new experimental text-editor `Blocks` mode PoC for `.th` files after reverting the earlier structured-table approach: added `Raw`/`Blocks` mode switch, drag-and-drop toolbox/canvas insertion flow (`survey`, `centerline`, `data`, `map`, `scrap`, `team`, `explo-date`), and first-pass block configuration actions synchronized back to raw source. Updated specification and user manual for the new workflow scope. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Aligned Structure sidebar column header naming with Files pane for consistency: Structure tree now uses `Name` as the visible column header label (matching Files) instead of `Structure`. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Removed the Structure inspector panel from the Structure sidebar and kept only the Structure tree. Simplified sidebar construction by dropping the Structure-page vertical splitter/inspector wiring, removed inspector selection sync hooks, and retired the dedicated `MainWindowInspector.cpp` translation unit from the build. Updated specification + user manual wording to reflect Structure-sidebar-only behavior. Verified with `cmake --build build --target TherionStudio`.~~

### 2026-05-14

- ~~Further simplified the Structure Inspector UI: removed duplicated `Type/Name` rows, disabled rename/apply workflow (removed `Open Source`/`Apply` buttons), kept compact `Type: Name` summary, switched `Location` to absolute path with wrapping support, and limited `Line State` visibility to line entries only. Updated specification + user manual. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Streamlined the Structure Inspector to remove duplicate metadata: kept a compact summary (`Type: Name`) and minimal rows (`Type`, editable `Name`, `Location`), while preserving `Open Source`, `Apply`, and line state toggles. Updated user manual guidance. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed stale Structure rows after unsaved edits (for example deleting `new-scrap` in an open tab): project structure scan now accepts in-memory file-content overrides from open text/map tabs (including detached map tabs), and structure sidebar rebuild is triggered on document text changes. Structure now reflects unsaved hierarchy edits immediately. Updated user manual/spec notes. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Improved Structure-tree parenting for simplified `Survey -> Map -> Scrap` view: scraps now attach under the map node that references them in `map ... endmap` blocks when the scrap reference resolves uniquely (fixes cases like `clopy01` appearing at survey level instead of under `1302.m`). Kept iconized rows and no synthetic top rows. Updated user manual/spec notes. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Added icons to simplified Structure sidebar rows: `Survey`, `Map`, and `Scrap` items now render category icons for faster scanning while keeping the stripped hierarchy (no project-root/summary rows). Updated user manual/spec notes. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Simplified Structure sidebar tree content: removed synthetic top rows (project-root and summary row) and limited hierarchy nodes to `Surveys`, `Maps`, and `Scraps` only. Non-matching categories are excluded from this tree while preserving nesting under the nearest visible ancestor. Updated user manual/spec requirements. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Improved dark-mode text-editor gutter readability: inactive line numbers now use a text-based color with tuned alpha in dark palettes instead of `QPalette::Mid`, which was too low-contrast. Highlighted line number styling remains unchanged. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Simplified text-row highlight visuals in the editor by removing the extra left accent strip from active-line painting; highlighted rows now use only the full-width background band to avoid cursor-like artifacts when map/text sync selects lines. Verified with `cmake --build build --target TherionStudio`.~~
- ~~Fixed edge-insertion/background drift when drawing with hidden backgrounds: map rendering and scene-to-source insertion mapping now use stable source bounds from `##XTHERION## xth_me_area_adjust` when available (fallback: geometry bounds), preventing remap jumps when new line vertices extend geometry near canvas borders. Wired the same bounds into background metadata reprojection so hidden-insert-unhide keeps alignment. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed background-alignment drift after map-geometry insertion when background layers were hidden: map scene refresh now reapplies metadata-based background projection after geometry re-render, so `xth_me_image_insert`-anchored raster and `.xvi` layers stay aligned with updated geometry mapping before layers are shown again. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Aligned map-draft identifier policy with Therion-style constraints: auto-created scraps now use `scrap-X` naming, generated area-border lines now use `line-X` naming unique within the owning scrap, and generated `area` blocks no longer force an `-id` (border-line `-id` remains mandatory because it is referenced by the area block). Updated `TherionDocumentEditorTest` coverage for default scrap naming and per-scrap `line-X` uniqueness. Verified with `cmake --build build --target TherionDocumentEditorTest`, `./build/TherionDocumentEditorTest`, `cmake --build build --target MapEditorDragUndoRedoSmokeTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed transient Bezier draft-preview glitch on anchor release (line/area insert): capture now clears anchor-press/drag/hover state before preview rebuild, preventing a brief stale “uncommitted candidate” handle/point frame from appearing after mouse release. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Updated map-mode status UX in main status bar from plain text to a color badge for faster mode recognition: `Insert` now appears in a red box and `Select` in a green box for active map-editor tabs. Added explicit mode-state query wiring from `MapEditorTab` into `MainWindow::refreshDocumentStatusWidgets` and kept tooltip text with full mode description. Updated user manual/spec requirements. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Improved Bezier area closure fidelity: when interactive area drafting has resolvable closing tangents, the final closing segment (last anchor back to first anchor) is now emitted as an explicit cubic row in the generated closed border line instead of relying on implicit straight `-close` closure. Area draft preview now renders this curved close path as well. Added `MapEditorDragUndoRedoSmokeTest` assertions for explicit cubic closing-row emission and endpoint closure to the first anchor. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `./build/TherionDocumentEditorTest`.~~
- ~~Extended interactive area drafting to support bezier curve authoring with the same click/drag control-point workflow as line drafting (including control-handle drag + mirrored opposite handle on smooth vertices). Area commit/cancel paths now serialize drafted coordinate rows into the generated closed border line and keep area reference-block output unchanged. Added `MapEditorDragUndoRedoSmokeTest` coverage asserting area click+drag produces bezier coordinate rows in the referenced border line. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `./build/TherionDocumentEditorTest`.~~
- ~~Reworked interactive bezier line-draft seeding to improve curve-authoring ergonomics: click+drag now treats the drag position as a curve pull point and derives cubic controls from that pull point (quadratic-to-cubic elevation), replacing prior midpoint-coupled handle seeding that forced awkward endpoint-parallel behavior. Updated `MapEditorDragUndoRedoSmokeTest` control-hit calculations to the new seeding model. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed interactive line-drafting tangent consistency for consecutive curved anchor placement: when click+drag creates a new curved segment from a draft vertex that already has the opposite control handle, the existing opposite handle is now auto-mirrored immediately (preview + captured draft data) so both controls stay collinear around that vertex. Added `MapEditorDragUndoRedoSmokeTest` regression coverage that commits a 3-anchor consecutive click+drag draft and asserts middle incoming/outgoing controls remain collinear/opposed in serialized TH2 rows. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest TherionStudio` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Changed area insertion serialization to Therion border-reference form: committing an interactive area now writes a closed `line border -id ... -close on` geometry block and an `area ...` block that references that line id (instead of inline area coordinates). Added parser/renderer support for area blocks that reference existing line ids so area fill remains visible, while referenced-border areas are treated as non-directly-editable area vertices in map view. Updated `TherionDocumentEditorTest` and `MapEditorDragUndoRedoSmokeTest` regressions plus user manual/spec behavior notes. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `./build/TherionDocumentEditorTest`.~~
- ~~Refined interactive bezier draft editing in Line mode: hovering draft control points now shows hand cursor feedback (`OpenHand` hover, `ClosedHand` drag), and dragging one control on a draft vertex with both handles now mirror-adapts the opposite handle (smooth tangent behavior parity with regular line editing). Expanded `MapEditorDragUndoRedoSmokeTest` with a 3-anchor bezier draft case that drags one middle-vertex control and verifies mirrored opposite-control serialization geometry. Updated specification and user manual behavior notes. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest TherionDocumentEditorTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `./build/TherionDocumentEditorTest`.~~
- ~~Made bezier control points usable during interactive line insertion: line-draft preview now renders control handles/connectors for curved segments, supports direct handle drag adjustment before commit, and preserves adjusted control coordinates in serialized TH2 line rows. Added smoke coverage in `MapEditorDragUndoRedoSmokeTest` that drags a draft bezier control before commit and verifies serialized control-point coordinates change. Updated specification and user manual behavior notes. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `./build/TherionDocumentEditorTest`.~~
- ~~Implemented interactive bezier line insertion semantics in Line mode: click-only anchor placement creates straight segments, while click-and-drag on new anchor placement creates curved segments with control points and serializes bezier coordinate rows. Added `appendDraftLineGeometry` insertion path for explicit line coordinate rows, wired map-line commit/cancel to use it, and expanded `MapEditorDragUndoRedoSmokeTest` with click+drag bezier regression assertions. Updated specification and user manual behavior text. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest MapEditorDragUndoRedoSmokeTest`, `./build/TherionDocumentEditorTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed interactive-drawing commit misplacement/shape-collapse when drafting outside the fitted existing-geometry strip: preview-to-source mapping no longer clamps to fitted bounds and now uses shared inverse map transform directly. This preserves relative drafted shape when committing with `Enter`/`Esc`/`Complete Draft`. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest MapEditorDragUndoRedoSmokeTest`, `./build/TherionDocumentEditorTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed drafted-line shape corruption after multi-click insert: line commits are now serialized as per-anchor coordinate rows (one vertex row per point) instead of a single packed coordinate row that the parser interpreted as cubic control geometry. This preserves drafted polyline shape and placement. Updated `TherionDocumentEditorTest` expected serialization and kept map smoke regression on total committed coordinate tokens across the inserted line block. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest MapEditorDragUndoRedoSmokeTest`, `./build/TherionDocumentEditorTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed line/area interactive-drawing vertex-loss regression: `appendDraftGeometry` no longer truncates to first 2 (line) / first 3 (area) vertices and now writes all captured vertices on `Enter`/`Esc`/`Complete Draft` commit. Added regression coverage in `TherionDocumentEditorTest` (multi-vertex line + area append) and `MapEditorDragUndoRedoSmokeTest` (4-click line draft commit preserves >=8 numeric coordinate tokens). Updated user manual note. Verified with `cmake --build build --target TherionStudio TherionDocumentEditorTest MapEditorDragUndoRedoSmokeTest`, `./build/TherionDocumentEditorTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Added status-bar map mode indicator for active map-editor tabs: main status area now shows `Map mode: Select` or `Map mode: Insert` and updates live on mode transitions. Implemented via `MapEditorTab::statusModeText()` + `modeStatusChanged` signal wiring into `MainWindow::refreshDocumentStatusWidgets`. Updated user manual and specification requirements. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Hardened Enter/Esc interactive-drawing key handling against focus issues: moved drawing hotkeys to map-tab `QShortcut`s with `WidgetWithChildrenShortcut` context and mode-aware enablement (`Esc` active in insert modes, `Enter` active in line/area modes). Removed duplicate fragile viewport-only Enter/Esc handling path. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Adjusted line/area Enter-commit mode persistence: pressing `Enter` now commits the current valid line/area draft and explicitly keeps the same insert mode active, so users can insert the next line/area without reselecting the tool. Added smoke coverage that commits by Enter and then inserts another line without re-entering Line mode. Updated user manual wording. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Adjusted interactive drawing `Esc` semantics for `Line`/`Area`: pressing `Esc` now exits insert mode to `Select` while preserving captured work by committing the current valid draft (line: >=2 vertices, area: >=3 vertices). Incomplete line/area drafts are canceled. Added smoke coverage for both line and area `Esc`-commit flows in `MapEditorDragUndoRedoSmokeTest`. Updated user manual behavior text. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed interactive drawing cancel behavior for `Esc`: pressing `Esc` now exits active insert mode (`Point`, `Line`, `Freehand`, `Area`) back to `Select`; for `Line`/`Area` it cancels the current draft session. Added a map-editor tab-level `Esc` shortcut (`WidgetWithChildrenShortcut`) so cancel works even when focus is in the text pane. Expanded `MapEditorDragUndoRedoSmokeTest` coverage for point and line Esc-cancel flows. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Implemented direct interactive freehand drawing: `Freehand` mode now captures sampled drag strokes in the map canvas and inserts a line on mouse release (single gesture, no draft-card handoff). Updated user manual workflow text and expanded `MapEditorDragUndoRedoSmokeTest` coverage for freehand insertion. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Started Phase 10 interactive drawing workflow with direct canvas authoring for core modes: `Point` now inserts on click, `Line`/`Area` now capture click-by-click vertices and commit via `Enter` or `Complete Draft`, with `Backspace`/`Delete` last-vertex removal and `Esc` cancel for active drafts. Added smoke coverage in `MapEditorDragUndoRedoSmokeTest` for point click insertion and line draft commit. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Implemented scrap-block text-to-map selection sync: placing text cursor on `scrap` or `endscrap` now selects/highlights all map objects inside that scrap block (line/area/point/station object starts). Added smoke coverage for both directives in `MapEditorDragUndoRedoSmokeTest`. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Mapped text-cursor selection on TH2 block-end directives to owning geometry blocks: placing cursor on `endline` now selects the parent `line` object, and placing cursor on `endarea` selects the parent `area` object (matching block-start behavior). Added smoke coverage in `MapEditorDragUndoRedoSmokeTest` for both end-directive mappings. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed remaining `.th2` active-line highlight drift in map-editor text panes by switching highlighted-row painting from `cursorRect(blockCursor)` to block-layout geometry (`firstVisibleBlock` + `blockBoundingGeometry`/`blockBoundingRect` with `contentOffset()`), matching gutter row positioning and removing per-document-type offset behavior. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Fixed text-editor active-line highlight row offset regression after line-number gutter work: active-line paint now uses the highlighted block’s `cursorRect().top()` directly (no extra line-height shift), so the highlighted band aligns with the actual cursor/source row. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Added text-editor line numbers: `QPlainTextEdit` now renders a synchronized left gutter with 1-based line numbers and active-line emphasis in the custom editor widget. Updated user manual + specification text-editor requirements. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`.~~
- ~~Confirmed non-hardcoded line-option cursor sync: any non-coordinate row inside a `line ... endline` block now maps to the current vertex (not keyword-specific), and smoke coverage now includes an arbitrary option row (`altitude 12`) in addition to `smooth off` / `subtype presumed`. Verified with `cmake --build build --target MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`.~~
- ~~Fixed text-cursor vertex sync for line option rows beyond `smooth`: option-only rows like `subtype presumed` now resolve to the current line vertex, so cursor placement on those rows highlights the corresponding map vertex. Added `MapEditorDragUndoRedoSmokeTest` coverage for `smooth off` and `subtype presumed` row mapping plus shifted blank-line point-sync regression checks. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`.~~
- ~~Fixed map click-to-text line targeting for dense geometry: map-selection sync now uses the last left-click scene position (instead of only global-cursor fallback), resolves the clicked selected item deterministically, and ignores decorative overlays by making grid/labels/connectors/detail strokes mouse-transparent. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest`, `QT_QPA_PLATFORM=offscreen ./build/MapEditorDragUndoRedoSmokeTest`, and `QT_QPA_PLATFORM=offscreen ./build/MapEditorDetachedPaneTest`.~~
- ~~Hardened map click-intent filtering for text sync: pending click-target capture and cursor-hit fallback now ignore non-interactive/non-selectable map scene items (including mouse-transparent overlays), preventing overlay z-order from biasing source-line navigation to adjacent lines. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest` plus offscreen smoke runs for `MapEditorDragUndoRedoSmokeTest` and `MapEditorDetachedPaneTest`.~~
- ~~Fixed map-driven source-line visual offset by changing text-editor line navigation to place the cursor at the start of the resolved line instead of selecting through line end; the existing full-width current-line highlight now marks the navigated line directly. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Fixed station/point map-to-text navigation landing on blank separator lines: point selection now resolves the current text row from the selected point coordinates before falling back to scene item line metadata. Added a smoke regression that inserts a blank line above a point and deliberately skews the item line role to prove selection still lands on the coordinate row. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Separated the text editor's visible current-line highlight from transient cursor normalization: map-driven `goToLine` / `goToLineColumn` calls now set an explicit highlighted source line, so the blue active-line band stays on the resolved row instead of a neighboring blank separator. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Changed text-editor source-row highlighting from full-width block background to text-range background so the visible highlight sits under the actual command text instead of the inter-line gap. Point source-row resolution now picks the nearest point coordinate within a small tolerance to account for preview/source round-trip precision. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Restored reliable current-line highlight visibility by combining a full-width active-line band with a stronger text-range overlay on the same source row. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Removed the full-width text-editor active-line band after it painted in the visual gap above the cursor row on some line layouts; current-line highlighting now uses a stronger text-token background anchored to the actual block text. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Replaced fragile `QTextEdit::ExtraSelection` active-line rendering with a small `QPlainTextEdit` subclass that paints the highlighted row from actual block geometry, including a left accent strip. This restores visible current-line highlighting while avoiding the previous one-line visual offset. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Corrected custom text-editor active-line painting to use `cursorRect()` for the highlighted block instead of `blockBoundingGeometry()`, fixing the consistent one-line-up visual offset while preserving the visible row band and accent strip. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Applied a direct row-offset correction to custom text-editor active-line painting after repeated manual screenshots showed the painted rectangle was consistently one rendered line above the caret/source row. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Adjusted text-cursor-to-vertex fallback on TH2 line rows: when cursor is outside explicit coordinate tokens on a line row, source-vertex resolution now prefers the row anchor pair (last coordinate pair) instead of nearest-first control pairs. This keeps text-line clicks aligned with expected anchor vertex selection on cubic rows. Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Adjusted map hit-target priority for sync/navigation selection: anchor/area vertices and points are now preferred over line-control handles when multiple interactive items overlap the click position. This prevents control-handle captures that were driving text navigation to adjacent control rows (per repeated “one line above” vertex-selection screenshots). Verified with `cmake --build build --target TherionStudio MapEditorDragUndoRedoSmokeTest MapEditorDetachedPaneTest` plus offscreen smoke runs for both map-editor tests.~~
- ~~Hardened map-click source-line reveal targeting: map-to-text sync now first resolves the selected item under the current cursor hit-test (when available) before fallback selection iteration, reducing wrong-line highlights caused by ambiguous selected-item ordering.~~
- ~~Fixed map-selection source-line reveal instability: map-to-text sync now resolves the navigation target from the primary visible selected map item, preventing stale hidden selections from overriding the highlighted text line when clicking line/point objects.~~
- ~~Extended text-to-map vertex sync for line option rows: placing the text cursor on vertex-related options (for example `smooth off`) now selects the corresponding current line vertex in map view.~~
- ~~Updated repository instructions (`AGENTS.md`) with an explicit “living specification” rule: keep `QtReimplementationSpecification.md` current for important behavior/acceptance/architecture changes, and prefer framework-agnostic wording for future reimplementation reuse.~~
- ~~Completed a specification-hardening pass for cross-framework reuse: removed stale TH2 mode-switch requirements (`text-only/map-only/split` and workspace-mode persistence), aligned spec to synchronized embedded text+map plus detachable map-pane behavior, and added explicit `Help -> User Manual (Full)` requirement with fallback semantics.~~
- ~~Added persistent active-line highlighting in the text editor (`QPlainTextEdit` extra-selection), so map-driven object/vertex selections visibly highlight the resolved source line without relying on text-range selection.~~
- ~~Implemented vertex-level bidirectional map/text sync: selecting line/area vertices (including line control points) now navigates text to the matching coordinate token, and moving the text cursor onto coordinate tokens now selects the corresponding map vertex/control.~~
- ~~Fixed reverse map/text sync gap: selecting map objects now moves the TH2 text cursor to the selected object's source line (cards and geometry items), matching existing text-to-map highlighting behavior.~~
- ~~Updated map-editor geometry presentation to reduce clutter: line/area vertices and control handles now appear only for selected objects, and line accent highlight overlays render only for selected lines.~~
- ~~Refined map-editor vertex visibility behavior: line control handles/connectors now appear only for the selected line vertex (or selected control handle), while non-selected vertices keep their controls hidden.~~
- ~~Removed Qt menu-construction deprecation warnings by replacing deprecated `QMenu::addAction(..., shortcut)` overload usage with explicit `QAction` setup and signal wiring in `MainWindow::buildMenus`.~~
- ~~Updated repository agent instructions to require avoiding deprecated APIs in new or touched code paths.~~
- ~~Expanded `Help` menu with in-app `Quick User Manual` and `User Manual (Full)` viewer actions while keeping contextual map-help panel workflows intact.~~
- ~~Added live appearance-switch handling so runtime OS light/dark changes now reapply app chrome styling and refresh map workspace rendering without relaunch.~~
- ~~Fixed map-theme regression where dark-mode app chrome could still render a light map canvas: map theme selection now cross-checks `QStyleHints::colorScheme` against effective application palette lightness and prefers palette-derived mode when they disagree.~~
- ~~Fine-tuned light-mode map rendering: explicit OS light/dark scheme now takes precedence over scene-local palette overrides, and light-theme canvas/grid/label/stroke values were adjusted for cleaner contrast balance and stronger line readability.~~
- ~~Fixed sidebar-rail and status-bar divider styling to use palette-driven border color (`palette(mid)`) instead of a hardcoded light gray, keeping separators consistent in dark mode while preserving light-mode contrast.~~
- ~~Applied a dark-mode map readability polish pass: increased grid contrast/weight, brightened map label text, slightly strengthened dark-theme geometry base stroke, and raised accent overlay alpha for clearer line visibility at normal zoom.~~
- ~~Fixed dark-mode map-canvas theme detection: map scene theme now prefers `QStyleHints::colorScheme` (OS light/dark) with guarded palette fallback, preventing false light-canvas rendering in dark mode.~~
- ~~Improved map-canvas theme contrast in `MapEditorSceneRenderer`: canvas/frame/grid, geometry strokes/fills, labels, and control-handle visuals now adapt to active light/dark palette so line/area geometry remains visible in OS light mode.~~
- ~~Started Phase 6 encoding workflow: added encoding-aware `DocumentFile` read/write APIs with detection (`UTF-8`/`UTF-16*`/`System`/`Latin1`) and codec-preserving save behavior.~~
- ~~Extended Phase 6 encoding workflow with Therion `encoding ...` directive-aware decoding and codec-name preserving save semantics (including legacy single-byte codecs when supported by Qt).~~
- ~~Added explicit text-editor `Convert to UTF-8` action (visible for non-UTF documents) and wired saves to preserve original encoding until conversion.~~
- ~~Polished conversion UX: `Convert to UTF-8` now requires confirmation, marks conversion as an in-memory pending change, and shows explicit encoding-status notes about save behavior.~~
- ~~Removed hard minimum sidebar width clamp and relaxed sidebar/page/content minimum-size policies so the sidebar can be resized to narrow widths across Files/Structure/Map/Console panes.~~
- ~~Aligned sidebar collapsed rail width with the activity-menu strip by fixing the rail width and matching dock minimum/collapsed width constraints, so the left action menu no longer changes width when collapsing/expanding.~~
- ~~Locked dock width while sidebar is collapsed (min=max=rail width) and restored normal width constraints on expand, preventing empty-strip growth when dragging the sidebar border in collapsed mode.~~
- ~~Made sidebar activity rail width icon-driven (computed from button layout) instead of fixed padding-heavy width, and reused that measured rail width for collapsed dock sizing so no extra white gutter appears next to the left action bar.~~
- ~~Updated activity-rail icon click behavior to treat rail-only manual resize as collapsed state, so clicking a rail icon expands sidebar content even when width was reduced via border drag instead of collapse toggle.~~
- ~~Fixed crash when clicking sidebar rail icon after manual resize-to-rail width: activity-button handler now captures the collapse-state probe lambda by value (not dangling reference), eliminating post-build lambda lifetime invalid access in `buildStructureSidebar` callbacks.~~
- ~~Fixed no-op rail clicks after manual resize-to-zero width: activity icon handlers now detect “effectively collapsed but flag is false”, force sidebar content visibility restore, reset dock width constraints, and restore expanded width before switching panes.~~
- ~~Added a global status-bar top divider border via app theme styling (`main.cpp`) so the bottom window-edge status bar is visually separated from the main content area.~~
- ~~Removed double-divider artifact at the bottom-left edge by clearing `SidebarDock` frame borders and styling `SidebarActivityRail` with only a right-edge border; keeps a single status-bar top divider while extending rail visually to the status edge.~~
- ~~Adjusted divider contrast to match surrounding UI separators by switching sidebar-rail and status-bar border colors from `palette(mid)` to `palette(midlight)`.~~
- ~~Rebalanced divider visibility after the previous contrast reduction: sidebar-rail right border and status-bar top border now use a slightly stronger neutral gray so separators stay visible without looking heavier than surrounding UI lines.~~
- ~~Moved active-document context to the main window status bar: full path + encoding are now shown globally for the focused document tab, with live updates on tab/document state changes.~~
- ~~Removed duplicate per-tab path/encoding rows from map workspace and simplified text-tab inline row to encoding-note / `Convert to UTF-8` only (source `encoding ...` command text remains untouched).~~
- ~~Restored visible left status/message area (`Ready` and transient status messages) by constraining global path widget width and rendering document path with middle elision in the status bar.~~
- ~~Removed extra visual divider line above the status bar by dropping the forced status-bar top border style, eliminating double-separator appearance with existing central-pane borders.~~
- ~~Reintroduced the status-bar top divider (`#bebebe`) after path/encoding status-bar refactor so bottom chrome remains visually anchored; kept `QStatusBar::item { border: 0; }` to avoid the earlier double-line artifact.~~
- ~~Added sidebar auto-snap collapse threshold on manual resize: if dock width is reduced below a minimum usable content width, sidebar now snaps to rail-collapsed mode; expanded-width memory now ignores tiny widths so re-expand restores a practical width.~~
- ~~Fixed sidebar auto-snap regression introduced by initial threshold logic: added collapse-transition guard to prevent collapse/resize race, enabled reliable auto-restore of content when manually widening a collapsed sidebar past threshold, and added hidden-content self-heal during non-collapsed dock resizes.~~
- ~~Reworked sidebar snap/collapse recovery after the resize regression: collapsed sidebars now keep the pane in layout but clamp its width to zero, the rail is fixed to icon width, and manual widening past the snap threshold restores content in-place instead of fighting the drag with saved-width restoration.~~
- ~~Verified `cmake --build build --target TherionStudio` after the sidebar snap/collapse recovery changes.~~
- ~~Disabled the unstable DockWidget auto-snap resize hook after continued rail-floating regressions: manual sidebar resizing is now free-form again, explicit rail collapse remains locked to icon width, and narrow manual widths recover via activity-icon click instead of resize-loop snapping.~~
- ~~Updated `docs/USER_MANUAL.md` to document the restored free-form sidebar resize behavior and verified `cmake --build build --target TherionStudio`.~~
- ~~Replaced the left sidebar `QDockWidget` with a main horizontal splitter pane: the activity rail is now fixed-width, sidebar resizing affects only the content pane, and collapse/expand uses splitter sizes rather than dock constraints.~~
- ~~Removed obsolete DockWidget title-bar helper code from the sidebar implementation after the splitter migration.~~
- ~~Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md` for the fixed-rail/resizable-content sidebar behavior; verified `cmake --build build --target TherionStudio`.~~
- ~~Fixed splitter-sidebar snap artifact by making the activity rail, sidebar content, and editor separate main-splitter panes; collapsing now sets only the content pane to width 0, so no empty left panel remains between rail and editor.~~
- ~~Verified `cmake --build build --target TherionStudio` after the three-pane splitter correction.~~
- ~~Removed the unwanted rail splitter handle by moving the fixed activity rail outside the splitter; the splitter now owns only sidebar content and editor, so the only resize handle is between content and editor.~~
- ~~Verified `cmake --build build --target TherionStudio` after moving the rail outside the splitter.~~
- ~~Restored reliable sidebar auto-snap with the fixed-rail splitter design: dragging content below the usable-width threshold now immediately collapses only the content pane, while the rail keeps its icon-driven width and no rail handle is shown.~~
- ~~Updated `QtReimplementationSpecification.md` and `docs/USER_MANUAL.md` for automatic sidebar content snap behavior; verified `cmake --build build --target TherionStudio`.~~
- ~~Removed visible triangle toggle symbols from text and map contextual-help splitter handles, leaving clean splitter-only help panes; updated the user manual accordingly.~~
- ~~Expanded `DocumentFileEncodingTest` and verified UTF-8 detection plus Latin1 and Windows-1250 byte-preserving save round-trip behavior.~~
- ~~Expanded encoding regression fixtures with `cp1250` alias and Windows-1252 directive coverage, verifying byte-preserving round-trip saves for both paths.~~
- ~~Closed inspector encoding-preservation gap for unopened-source fallback edits: structure rename and line `-close`/`-reverse` rewrites now read and write using detected file encoding instead of forcing UTF-8.~~
- ~~Added `DocumentFileEncodingTest` regression for inspector-style fallback rewrites on `cp1250` input (`readTextFile` + `rewrite...` + `writeTextFile`), verifying byte-level encoded output after map-name rename and line `-close` toggle edits.~~
- ~~Added `DocumentFileEncodingTest` regression for inspector-style fallback rewrites on `windows-1252` input with accented map-name rename and line `-close` toggle, verifying byte-level encoded output preservation.~~
- ~~Added negative `DocumentFileEncodingTest` case for unknown `encoding` directive token fallback, confirming UTF-8 decode fallback and byte-preserving UTF-8 save round-trip.~~
- ~~Expanded `DocumentFileEncodingTest` with broader non-UTF fixture coverage: `cp1252` alias round-trip, `latin2` directive round-trip, and unknown-directive Latin1 fallback round-trip with byte-preserving save verification.~~
- ~~Re-verified Phase 5 regression suite after ongoing map/editor work: built `TherionStudio`, `MapGeometryFeatureParsingTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, `TherionXviParserTest`, and `TherionDocumentEditorTest`, then executed all five map-related binaries plus `TherionDocumentEditorTest`.~~
- ~~Implemented explicit touch-friendly controls mode for map editor workflows: added toolbar toggle, persisted state in `SessionStore`, and routed wheel/touch-pan behavior through tested input policy helpers.~~
- ~~Added `MapEditorInputPolicyTest` and `SessionStoreTest` coverage for mode-aware pan/zoom decision logic and touch-controls persistence round-trip behavior.~~
- ~~Added `docs/ENCODING_QA_CHECKLIST.md` and linked it from the user manual to standardize cross-platform manual validation for open/save/convert/reopen/byte-check encoding workflows.~~
- ~~Populated `docs/USER_MANUAL.md` with current implemented UI/workflow coverage: menus, sidebar panes, map workspace behavior, cross-platform shortcuts, settings/session persistence, platform notes, and troubleshooting.~~
- ~~Updated repository instructions to require maintaining a living `docs/USER_MANUAL.md` and updating it whenever UI layout, workflows, keyboard shortcuts, or settings behavior changes.~~
- ~~Created initial `docs/USER_MANUAL.md` scaffold to track UI structure, workflows, shortcuts, settings, platform notes, and troubleshooting.~~
- ~~Improved map-canvas interaction affordance for editable geometry handles: added hover/selected/drag visual states, stronger contrast, and per-handle tooltips (point and line/area vertices).~~
- ~~Increased map preview handle and stroke readability (larger point/vertex radii and thicker preview line strokes) to improve selection/drag usability at normal fit scale.~~
- ~~Added hover-state visual feedback to map cards and draft-geometry cards for clearer active-target discovery.~~
- ~~Improved map zoom/fit consistency: fit mode now auto-refits on map viewport resize and keeps geometry-only vs fit-with-background mode semantics.~~
- ~~Synced zoom indicator to actual `QGraphicsView` transform scale after fit/manual zoom operations instead of using a stale fixed baseline.~~
- ~~Added middle-mouse panning for map navigation without changing left-drag behavior for selectable/movable map items.~~
- ~~Refined map input-device controls by source type: standard wheel defaults to cursor-centered zoom, precise scrolling devices default to pan, `Cmd/Ctrl` + scroll forces zoom, and right-button drag pans the map viewport.~~
- ~~Added native pinch (`QNativeGestureEvent::ZoomNativeGesture`) cursor-centered zoom handling for touch surfaces.~~
- ~~Added gesture-suppression guardrails so wheel/pinch/touchpan viewport gestures are ignored while a primary pointer interaction is active, reducing accidental pan/zoom during direct geometry edits.~~
- ~~Added select-mode-only two-touch threshold pan handling in the map view event filter to support touchpad touch-tracking panning without interfering with draw/edit modes.~~
- ~~Expanded zoom clamping and normalization to `0.1 .. 50.0` with viewport-anchored zoom application so toolbar zoom actions and gesture zoom use the same bounded semantics.~~
- ~~Implemented line-feature segment parsing for TH2 map preview so `line ... endline` coordinate data is interpreted as linear (`x y`) and cubic Bezier (`c1x c1y c2x c2y x y`) segments instead of a forced polyline.~~
- ~~Updated map-canvas line rendering to draw parsed cubic arcs via `QPainterPath::cubicTo`, preserving straight segments where appropriate and keeping existing vertex-handle editing.~~
- ~~Stopped treating line-block metadata continuations (for example `smooth off` and option-led lines) as drawable geometry coordinates in map feature extraction.~~
- ~~Added `MapGeometryFeatureParsingTest` coverage for Bezier segment extraction and metadata-line exclusion, and wired the new test target into CMake.~~
- ~~Introduced an explicit TH2 line-vertex model in map geometry features (`anchor`, optional `incomingControl`/`outgoingControl`, `isSmooth`, and source-vertex index mapping) to align map parsing/rendering with Therion curve semantics.~~
- ~~Updated TH2 line parsing so `smooth off` marks the current/last parsed line vertex as non-smooth, and cubic control points equal to anchor coordinates are normalized back to nil handles in memory.~~
- ~~Switched map-canvas line rendering to the line-vertex handle rule: segment is straight when both handles are absent, cubic when either adjacent handle exists (`QPainterPath::cubicTo` with anchor fallback).~~
- ~~Added editable control-handle visualization for parsed line curves (incoming/outgoing control points plus anchor-to-handle guide lines), wired to existing coordinate rewrite callbacks through source vertex indices.~~
- ~~Expanded `MapGeometryFeatureParsingTest` with smooth-state and anchor-equivalent handle-normalization assertions for TH2 curve parsing behavior.~~
- ~~Normalized map-editor line-handle writeback so control-handle drags use `line` rewrite semantics (instead of unsupported `line control` kind), removing a persistence gap for control-point edits.~~
- ~~Added smooth-coupled line-handle update behavior in map writeback commands: when a smooth vertex handle is moved, the opposite handle is auto-updated collinearly with preserved/opportunistic mirrored length and committed in the same undoable command.~~
- ~~Added anchor-drag handle transport in line writeback commands: moving an anchor now translates attached incoming/outgoing controls by the same delta and persists all affected coordinates as one undoable command.~~
- ~~Removed scaffold-only map-canvas header text blocks (`TH2 Map Workspace`, geometry preview captions) from the rendered scene to reduce visual noise in the map viewport.~~
- ~~Resized the map geometry preview region to fill the scene canvas (instead of a fixed small top strip), so `Fit` no longer leaves a large empty dark area below geometry.~~
- ~~Updated `mapPreviewBounds` to match the full-canvas preview layout, keeping fit/background placement math consistent with the new map-scene geometry region.~~
- ~~Changed map `Fit` geometry bounds to use only tagged geometry items (paths, anchors, curve handles) instead of the full preview rectangle/scaffold extents, improving framing accuracy.~~
- ~~Updated `Fit + BG` framing behavior to union real geometry bounds with visible background-layer bounds, avoiding oversized empty-canvas margins from non-content scene decorations.~~
- ~~Hardened map-editor pinch zoom handling to prevent viewport lockups: added native-gesture begin/end tracking, short wheel-suppression during active pinch, stale-gesture auto-reset, and finite/clamped per-event zoom delta normalization before applying zoom.~~
- ~~Fixed a map-handle drag crash (`EXC_BAD_ACCESS` in `MapEditableGeometryVertexItem::mouseReleaseEvent`) by preventing post-callback item access in handle `mouseReleaseEvent` when commit callbacks may trigger scene rebuild and item deletion.~~
- ~~Implemented line option parsing for map geometry features (`-close` and `-reverse`) with toggle-value handling (`on/off/yes/no/true/false/1/0` plus bare-flag enable semantics).~~
- ~~Updated map line rendering to honor parsed closed-state by closing the line path when `-close` is enabled.~~
- ~~Surfaced parsed line-state markers in map entry subtitles (`[closed]`, `[reversed]`) for faster state inspection while iterating map parity.~~
- ~~Added `TherionDocumentEditor::rewriteLineOptionToggle` for safe source-line mutation of line toggles (`-close` / `-reverse`) with explicit value rewriting, bare-flag expansion, and comment/line-ending preservation.~~
- ~~Wired line toggle rewrite wrappers through `TextEditorTab` and `MapEditorTab` so line-state changes participate in the current map/text refresh and save flow.~~
- ~~Extended Inspector with line-state controls (`Closed`, `Reversed`) for selected `Lines` objects and connected them to persisted source rewrites plus sidebar/map selection refresh.~~
- ~~Added a corpus-style `TherionDocumentEditorTest` fixture that performs coordinated point/line/area rewrites within a realistic TH2 scrap block containing metadata, comments, and CRLF line endings.~~
- ~~Verified rewrite stability in the fixture across option-led continuation lines (`-subtype`), inline metadata (`-id`), `%` comments, and mixed-precision coordinate tokens.~~
- ~~Added negative corpus-style fixture checks that assert rewrite failures (missing `endline`, incomplete area coordinate tuple) return errors and leave source text unchanged.~~
- ~~Verified `TherionDocumentEditorTest`, `TherionProjectStructureIndexTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, and `TherionXviParserTest`.~~
- ~~Verified `cmake --build build --target TherionStudio TherionDocumentEditorTest TherionProjectStructureIndexTest MapBackgroundPlacementTest TherionBackgroundMetadataTest TherionXviParserTest` plus execution of all five regression binaries after map input-control changes.~~
- ~~Verified `cmake --build build --target TherionStudio MapGeometryFeatureParsingTest TherionDocumentEditorTest TherionProjectStructureIndexTest MapBackgroundPlacementTest TherionBackgroundMetadataTest TherionXviParserTest` plus execution of all six regression binaries.~~
- ~~Expanded `MapGeometryFeatureParsingTest` with line-option assertions for `-close`/`-reverse` parsing behavior, including explicit on/off and bare-flag cases.~~
- ~~Expanded `TherionDocumentEditorTest` with `rewriteLineOptionToggle` coverage: null/non-line rejection, missing-option insertion before comments, bare-option disable normalization, explicit-value rewrite, and CRLF preservation.~~
- ~~Verified `cmake --build build --target TherionStudio TherionDocumentEditorTest MapGeometryFeatureParsingTest` plus execution of `TherionDocumentEditorTest`, `MapGeometryFeatureParsingTest`, `TherionProjectStructureIndexTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, and `TherionXviParserTest`.~~
- ~~Added reusable line-curve editing helpers in map scene support: de Casteljau segment split insertion (`insertLineVertexByDeCasteljau`) and neighbor-control reconnect removal (`removeLineVertexWithReconnect`) on TH2 line-vertex sequences.~~
- ~~Expanded `MapGeometryFeatureParsingTest` with direct curve-edit behavior checks for cubic split insertion and middle-vertex removal reconnect semantics.~~
- ~~Verified `cmake --build build --target TherionStudio TherionDocumentEditorTest MapGeometryFeatureParsingTest TherionProjectStructureIndexTest MapBackgroundPlacementTest TherionBackgroundMetadataTest TherionXviParserTest` plus execution of all six regression binaries.~~
- ~~Added safe source rewrite helper `rewriteLineCoordinateRows` for replacing coordinate rows in `line` blocks while preserving `line`/`endline` lines and CRLF, with explicit guardrails that reject inline-coordinate starts and mixed non-coordinate continuation content.~~
- ~~Wired line coordinate-row rewrite through `TextEditorTab` and map-tab command path, including keyboard-driven line anchor editing (`Insert` or `I` = split/add via de Casteljau, `Delete/Backspace` = remove middle anchor with neighbor-handle reconnect).~~
- ~~Added `MapEditableGeometryVertexItem` accessors (`lineNumber`, `geometryKind`, `vertexIndex`) and map-view focus/key handling needed for selection-driven anchor edit commands.~~
- ~~Enabled dedicated map-window lifecycle parity in `MainWindow`: detached map sessions are tracked in open-document session state, save-all/dirty-close prompts include detached maps, and explicit open actions reuse/focus existing detached sessions by canonical path.~~
- ~~Updated `Close All Tabs` to include detached map sessions (with unsaved-change confirmation) so tab-level close workflows behave consistently across embedded and detached TH2 editors.~~
- ~~Fixed detached map-window parenting/transfer path so `Open Dedicated Window` moves the actual `MapEditorTab` into a true top-level window (preventing empty detached-window opens).~~
- ~~Implemented map-pane detach workflow inside `MapEditorTab`: `Open Map in Window` now moves only the graphical map pane into a second top-level window while keeping text editing in the main tab, and `Return Map Pane` (or closing detached window) reattaches it.~~
- ~~Simplified TH2 workspace UX by removing the `Text Only / Map Only / Split` switcher and standardizing on embedded split view plus detachable map pane window.~~
- ~~Removed obsolete map workspace-mode persistence API from `SessionStore` (`therionMapWorkspaceMode` read/write), since split layout is now fixed by design.~~
- ~~Added `MapEditorDetachedPaneTest` coverage for map-pane detach/reattach lifecycle: open-window transition, single detached-window presence, reattach via toggle, and no leftover detached window after return.~~
- ~~Fixed map-editor undo crash after vertex moves: map command apply now defers scene rebuild during command execution (`mapCommandApplyInProgress_` / `mapSceneRefreshPending_`), preventing `undoStack_->clear()` re-entrancy corruption while text rewrite signals are emitted.~~
- ~~Fixed map-canvas drag-preview regression after undo-crash hardening: editable point/vertex items now use expanded repaint bounds to prevent ghost trails, and line-path/control-connector visuals are recomputed live while dragging anchors/handles so geometry updates in real time before commit.~~
- ~~Fixed map-editor undo-history regression introduced by deferred scene refresh: command-originated refresh now rebuilds map visuals without clearing the undo stack, so vertex/control-point moves can be undone/redone correctly while preserving crash-safe re-entrancy guards.~~
- ~~Fixed text-tab dirty-state false-positive after map undo/redo-to-baseline: dirty marker is now derived from current text+encoding snapshot versus last load/save snapshot, so undoing all edits clears `*modified` as expected.~~
- ~~Fixed remaining map-undo dirty false-positive by preserving coordinate token precision/style in `TherionDocumentEditor` point/line vertex rewrites (including integer tokens), so moving geometry and undoing back restores original source text instead of normalization drift.~~
- ~~Fixed persistent dirty marker on CRLF-backed files after undo-to-baseline: clean-text snapshot now captures the editor-normalized text representation on load, preventing false dirty due to line-ending canonicalization (`\r\n` file bytes vs `\n` in editor).~~
- ~~Improved smooth line-handle editing preview: while dragging an incoming/outgoing control on a smooth vertex, the opposite handle is now mirrored live (collinear update during drag), so curve shape no longer jumps only after mouse release.~~
- ~~Refactored smooth opposite-handle mirroring into shared helper `mirroredSmoothControlPoint` and reused it in both map-preview drag coupling and commit-time line-vertex rewrite coupling so preview and persisted behavior stay mathematically consistent.~~
- ~~Added `MapGeometryFeatureParsingTest` regression coverage for smooth-mirroring math: preserved opposite length, collinear/opposite direction, missing-opposite mirror-length fallback, and zero-vector `nullopt` behavior.~~
- ~~Fixed map anchor-drag preview parity: while dragging a line anchor, attached incoming/outgoing control handles now translate in real time by the same source-space delta (not only after drop), keeping curve shape stable during interactive edits.~~
- ~~Fixed map-handle drag boundary clamp: editable point/vertex/control handles are now constrained by full map preview bounds instead of the fitted geometry rectangle, allowing control points to cross grid lines and move across the full canvas region.~~
- ~~Fixed remaining fitted-rect coordinate clamp during map edit drags: `sceneCoordsPreviewToSource` now converts preview positions without clamping to fitted geometry bounds, so vertex and control-point drags continue updating source coordinates beyond fitted-rect edges and no longer snap back at interior grid-aligned boundaries.~~
- ~~Added regression coverage for unclamped map preview-to-source conversion (`MapGeometryFeatureParsingTest`): points dragged outside fitted geometry bounds now map to out-of-range source coordinates (no implicit fitted-rect clamp), preventing reintroduction of vertex/control snap-back at interior grid-aligned boundaries.~~
- ~~Refactored line-vertex secondary move coupling into shared helper `collectLineSecondaryMovesForVertexDrag` and wired map command generation through it, keeping anchor/control coupling behavior consistent across preview and commit paths.~~
- ~~Added `MapGeometryFeatureParsingTest` regression coverage for anchor-drag coupling: dragging an anchor now produces incoming/outgoing control secondary moves translated by the same source delta.~~
- ~~Expanded `MapGeometryFeatureParsingTest` with line-control coupling regressions: smooth incoming-control drags now assert one mirrored opposite-control secondary move with preserved opposite-handle length, while corner (`smooth off`) drags assert no coupled secondary move.~~
- ~~Unified live-preview and commit-time line coupling behavior by routing map preview coupling through shared `collectLineSecondaryMovesForVertexDrag`, removing duplicated preview-only coupling math and reducing parity drift risk between drag preview and persisted undoable edits.~~
- ~~Added preview-specific coupling helper `collectLinePreviewSecondaryMovesForVertexDrag` with defensive self-target filtering for malformed/shared source-index cases, and routed live preview coupling through it to prevent accidental self-reposition loops during drag callbacks.~~
- ~~Expanded `MapGeometryFeatureParsingTest` with preview-coupling self-filter regression: malformed same-source incoming/outgoing control mapping now yields command-time secondary move but empty preview secondary move set after self-target filtering.~~
- ~~Added current-state-aware preview coupling helper `collectLinePreviewCoupledUpdatesForVertexDrag` and wired live map preview updates through it so anchor-drag control transport is delta-sequenced (no stale absolute reset between drag events).~~
- ~~Expanded `MapGeometryFeatureParsingTest` with live-preview sequencing regression coverage: multi-step anchor drag preview now asserts cumulative control transport, plus explicit no-self-update filtering in malformed shared source-index cases.~~
- ~~Expanded `MapGeometryFeatureParsingTest` with explicit preview-vs-commit parity coverage for representative line edits: anchor move, smooth control move, and corner control move now assert identical coupled-secondary move sets between preview and command-time coupling helpers.~~
- ~~Added `MapEditorDragUndoRedoSmokeTest` as a focused GUI smoke executable for deterministic line-anchor drag + undo + redo, asserting text mutation, undo-to-baseline restoration, and dirty-state transitions through the map command path.~~
- ~~Completed Phase 5 checklist re-verification pass after smoke-test integration: ran `MapGeometryFeatureParsingTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, `TherionXviParserTest`, `TherionDocumentEditorTest`, `MapEditorDetachedPaneTest` (offscreen), and `MapEditorDragUndoRedoSmokeTest` (offscreen).~~
- ~~Separated map-editor Post-MVP parity work into standalone Phase 9 and marked Phase 5 as MVP-complete.~~
- ~~Hardened map-geometry undo/redo dirty-state fidelity: point and line/area vertex move commands now restore exact pre/post command text snapshots through `TextEditorTab::replaceTextForCommand`, eliminating residual dirty flags caused by rewrite-path formatting drift in multi-step map commands.~~
- ~~Extended Phase 5 line-edit parity with smoothness toggling: map canvas now supports `S` to toggle selected line vertex smooth/corner state, line-coordinate rewrite validation now accepts `smooth` continuation rows, and line-row regeneration preserves `smooth off` markers during map-driven line rewrites.~~
- ~~Expanded `TherionDocumentEditorTest` with `rewriteLineCoordinateRows` coverage for null/non-line rejection, inline-start rejection, mixed-content rejection, and successful CRLF-preserving rewrite.~~
- ~~Verified `cmake --build build --target TherionStudio TherionDocumentEditorTest MapGeometryFeatureParsingTest TherionProjectStructureIndexTest MapBackgroundPlacementTest TherionBackgroundMetadataTest TherionXviParserTest` plus execution of all six regression binaries after keyboard vertex-edit wiring.~~
- ~~Closed Phase 4 (`MVP`) and moved additional corpus-scale rewrite expansion into the Post-MVP backlog.~~

### 2026-05-13

- ~~Added `File -> Close` and `File -> Close All Tabs` commands with unsaved-change prompts.~~
- ~~Added `Command/Ctrl+Shift+W` shortcut for `Close All Tabs`.~~
- ~~Persisted open-document session state immediately after tab-close operations.~~
- ~~Verified build and full current regression baseline.~~
- ~~Added project-tree context menu actions for file rows: `Open`, `Open in Map Editor` (for `.th2`), and `Open Externally` for unsupported file types.~~
- ~~Wired `Open Externally` through platform default-app launch with actionable failure feedback.~~
- ~~Verified `cmake --build build --target TherionStudio TherionDocumentEditorTest` plus current regression binaries (`TherionDocumentEditorTest`, `TherionProjectStructureIndexTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, `TherionXviParserTest`).~~
- ~~Extended project-tree context menus with file/folder management actions: `Duplicate`, `Rename`, `Delete`, `Rename Folder`, and `Delete Folder`.~~
- ~~Added project-tree creation actions in directory/root context: `New Folder`, `New .th File`, `New .th2 File`, and `New thconfig`.~~
- ~~Added safety guardrails for destructive file/folder operations by blocking rename/delete when affected documents are currently open in editor tabs.~~
- ~~Verified `cmake --build build --target TherionStudio TherionDocumentEditorTest` plus current regression binaries (`TherionDocumentEditorTest`, `TherionProjectStructureIndexTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, `TherionXviParserTest`).~~
- ~~Added project-tree icon differentiation so supported Therion files (`.th`, `.th2`, `thconfig`) use a distinct Therion-document icon while unsupported files use the generic document icon.~~
- ~~Refined project-tree icon handling so non-Therion files keep their native filetype icons while Therion files get a guaranteed `TH`-badged icon (fix for "all icons look the same").~~
- ~~Extended Therion icon classification to include all `*.thconfig` files (plus `thconfig`, `.th`, and `.th2`).~~
- ~~Expanded `TherionDocumentEditorTest` rewrite coverage for mixed `%` comments, CRLF preservation, quoted numeric noise, and option-heavy multi-line line/area vertex blocks.~~
- ~~Verified `TherionDocumentEditorTest`, `TherionProjectStructureIndexTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, and `TherionXviParserTest` after rewrite-coverage expansion.~~
- ~~Verified `cmake --build build --target TherionStudio TherionDocumentEditorTest` plus current regression binaries (`TherionDocumentEditorTest`, `TherionProjectStructureIndexTest`, `MapBackgroundPlacementTest`, `TherionBackgroundMetadataTest`, `TherionXviParserTest`).~~

### 2026-05-17

- ~~Updated the left sidebar activity-rail icons to Lucide assets: `Files -> folder-open`, `Structure -> network`, `Map -> map`, and `Console/Compiler -> cog`.~~
- ~~Renamed the sidebar rail/menu-facing `Console` pane label to `Compiler` (`Show Compiler` view action, rail tooltip, and user-manual terminology updates).~~
- ~~Refined sidebar activity-rail visuals for dark mode: switched to borderless/flat icon buttons, increased icon/button size for readability, and added palette-aware hover/active highlights with a subtler rail surface.~~
- ~~Made sidebar activity-rail Lucide icons palette-aware by rendering SVG `currentColor` with Qt palette text colors, so icons respect dark/light theme contrast instead of appearing black in dark mode.~~
- ~~Added runtime palette-change handling for activity-rail theming so icon tint and rail hover/active/background colors refresh immediately when the app theme changes.~~
- ~~Refactored embedded TH2 workspace to explicit `Visual` and `Raw` modes: `Visual` now shows map canvas + right-side object-details panel, `Raw` now shows source text + contextual help, and detached-map workflows keep visual editing in the detached window while the main tab keeps raw editing visible.~~
- ~~Added initial map object-details editing surface in `Visual` mode (selection summary, line `-close`/`-reverse` toggles, and coordinate apply for selected point/vertex geometry), wired through existing TH2 rewrite/undo paths.~~
- ~~Updated `QtReimplementationSpecification.md`, `docs/USER_MANUAL.md`, and quick in-app manual copy for the new TH2 mode model and detached-window behavior.~~
- ~~Added a detached-map window status bar with local map zoom and Select/Insert indicators, and hid duplicate map zoom/mode badges in the main-window status bar while the map pane is detached.~~
- ~~Moved the TH2 `Visual/Raw` mode switcher to the main document tab strip (top-right corner) for better vertical space usage, while keeping an inline fallback selector for detached dedicated map-editor windows that do not share the main tab strip.~~
- ~~Moved `.th`/`.thconfig` `Raw/Blocks` mode switching to the same tab-strip top-right corner control and hid the in-content mode row in regular text tabs.~~
- ~~Moved TH2 map-pane detach/reattach action (`Separate Map` / `Return Map`) from the map toolbar into the same top-right workspace controls as TH2 mode switching; map toolbar now remains focused on editing/navigation tools.~~
- ~~Fixed detached-map blank-window regression when detaching from TH2 `Raw` mode: detached window now always forces visual map pane visible while main tab remains in raw editor view.~~
